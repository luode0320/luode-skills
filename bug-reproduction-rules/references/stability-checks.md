# 稳定性检查

- 是否每次都出现。
- 是否只在特定账号、特定数据、特定顺序下出现。
- 是否与并发、时序、缓存、历史数据、网络波动有关。
- 如果复现失败，和成功样本有什么差异。

## 环境特定 Bug：生产才出现时的匹配复现

Bug 只在生产环境出现时，先怀疑环境差异而不是代码差异：

1. 向用户确认环境细节：OS 版本、内存限制、网络延迟、配置项、数据规模。
2. 用匹配约束复现：Docker 内存限制、缩小/放大并发、注入网络延迟、用生产样本数据。
3. 环境差异本身就是证据，记录「哪些约束下出现 / 哪些约束下消失」到当前 Bug 主文档。
