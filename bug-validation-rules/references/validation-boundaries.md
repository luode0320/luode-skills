# 验证边界  - 修复验证聚焦当前缺陷是否关闭。 - 功能验证聚焦当前需求是否实现正确。 - 回归验证聚焦旧能力是否被带坏。 - 三者可能连续出现，但职责不能混写。 - Bug 结论记录统一沉淀到 `doc/4-bugs/` 根目录；实际测试脚本、测试数据和测试报告仍按 `test-strategy-rules 的 test-asset-governance 条件路由` 放在 `doc/5-tests/` 时间戳根目录中；若原本散落在错误位置，还需按 `test-strategy-rules 的 test-asset-governance 条件路由` 收拢。
