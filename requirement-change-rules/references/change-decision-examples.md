# 需求变更处理样例

## 可在当前任务内继续处理

- 只是补一个已有接口必需字段，且不改变范围和验收口径，并在同一份 `ment/` 文档中更新变更说明。
- 补充一个原本就隐含存在的业务限制条件，并在同一份需求文档中更新影响面。

## 应拆出新任务或回流需求域

- 原本做单页展示，临时改成多角色流程。
- 原本只改后端接口，途中要求补管理后台页面。
- 变更已影响原验收标准和测试策略，但仍想继续当前实现。
- 把需求变更另外新开一份独立文档，导致原需求文档不再是单一事实来源。
