# 最小改动总则

## 用途

用于定义哪些改动属于本次任务的最小必要范围，哪些改动属于应当被排除的无关修改。

## 允许的改动

- 直接完成当前需求所需的最小实现。
- 直接修复当前 Bug 所需的最小修正。
- 为了让本次改动可运行、可编译、可验证而必须补的最小支撑代码。
- 与当前改动直接耦合、否则会立刻出错的最小连带调整。

## 应排除的改动

- 与当前目标无关的历史问题修复。
- 与当前目标无关的大范围重构。
- 与当前目标无关的统一命名、统一格式、统一风格清理。
- 与当前目标无关的性能优化、缓存优化、结构优化。

## 默认原则

1. 能不改的旧代码，不改。
2. 能局部修正的，不扩大到整层或整模块。
3. 能新增小范围代码解决的，不先改大面积旧逻辑。
4. 本次不处理的问题，记录并留给后续独立任务。
