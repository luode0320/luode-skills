interface:
  display_name: "测试程序规则"
  short_description: "以 `test-location-rules` 为基准统一测试程序职责与落点的规则"
  default_prompt: "使用 $test-program-rules 判断当前测试程序、模拟程序、验证脚本和数据构造脚本应如何划分职责，并确保统一落在当前 `test/` 任务目录中。"
