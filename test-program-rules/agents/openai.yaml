interface:
  display_name: "测试程序规则"
  short_description: "以 test-location-rules 为基准，统一 ASCII 镜像路径下测试程序的职责拆分与归档"
  default_prompt: "使用 $test-program-rules 判断当前测试程序、模拟程序、验证脚本和数据构造脚本应如何划分职责；真实测试资产统一落在 `test/yyyy-MM-DD_HHmmss/<ASCII 真实代码路径镜像>/` 下，中文说明目录只保留 `README.md`。"
