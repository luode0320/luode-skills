interface:
  display_name: "任务计划断点恢复"
  short_description: "保存并恢复 Codex Desktop 悬浮任务列表"
  default_prompt: "使用 $task-plan-rehydration-rules 优先校验 PROJECT_CURRENT 中的活动任务投影；若托管投影缺失，则先派只读子代理收集当前会话与项目文档证据，再用 synthesize 生成 exact 或 fallback 投影，写回 PROJECT_CURRENT 后调用 update_plan 重建悬浮任务列表，并先核验进行中步骤的中断点。"
