# 实施文档结构模板

## 建议结构

```md
# 需求实施总览：{中文标题}

## 1. 基本信息
- 对应需求文档：
- 对应验收标准文档：
- 对应最终验收文档：
- 当前计划范围：
- 当前优先闭环：
- 当前状态：

## 2. 实施周期总览
- 总周期说明：
- 本次计划拆分的子任务周期数：
- 周期拆分原则：
- 周期 1：
  - 周期目标：
  - 完成标志：
  - 与前后周期衔接：
- 周期 2（按需）：
  - 周期目标：
  - 完成标志：
  - 与前后周期衔接：

## 3. 阶段计划
- 阶段 1：
  - 阶段名称：
  - 阶段目标：
  - 只做这一件事：
  - 输入条件：
  - 输出产物：
  - 验证门槛：
- 阶段 2（按需）：
  - 阶段名称：
  - 阶段目标：
  - 只做这一件事：
  - 输入条件：
  - 输出产物：
  - 验证门槛：

## 4. 现状与落点
- 涉及目录：
- 涉及文件 / 模块：
- 复用点：
- 需要新增的内容：
- 新增文件清单（按需必填，目录树格式）：
  ```text
  app/
  └── exchange/
      ├── bizpolicy/
      │   └── context.go                     # 业务上下文与过滤规则定义
      ├── repository/
      │   ├── exchangeBusinessControl.go     # 配置查询仓储
      │   └── model/
      │       └── exchangeBusinessControl.go # 配置实体模型
      └── service/
          └── business_filter.go             # 过滤服务（链/币/渠道）

  scripts/
  └── 20260416_exchange_business_control.sql # 数据库变更脚本
  ```

## 5. 方案选择
- 方案 A：
- 方案 B：
- 如有必要再补方案 C：
- 推荐方案与原因：

## 6. 实施步骤
1. 第一步：
   - 所属周期：
   - 所属阶段：
   - 本步只做：
2. 第二步：
   - 所属周期：
   - 所属阶段：
   - 本步只做：
3. 第三步：
   - 所属周期：
   - 所属阶段：
   - 本步只做：

## 7. 每步验证点
- 步骤 1 验证：
- 步骤 2 验证：
- 步骤 3 验证：

## 8. 图形化执行路径（复杂任务建议必填）
- 流程图（Mermaid `flowchart`）：
- 时序图（Mermaid `sequenceDiagram`，按需）：
- 泳道表（按需）：

## 9. 风险与阻断项
- 风险：
- 依赖：
- 暂停条件：

## 10. 数据库变更 SQL（按需必填）
- 建表 SQL（`CREATE TABLE`）：
- 字段变更 SQL（`ALTER TABLE`）：

## 11. 自审结论
- 覆盖度检查：
- 实施周期检查：
- 阶段单一目标检查：
- 占位词检查：
- 可执行性检查：
- 图文一致性检查：
- 用户确认状态：
```

## 结构要求

- 必须先写清当前优先闭环，不要把整张大路线图塞成一份笼统计划。
- “实施周期总览”是必填项，必须说明为什么当前计划可以独立成一个周期；若还能继续拆小，就继续拆到更清晰的子任务周期。
- “阶段计划”是必填项，每个阶段只能承载一个清晰目标，并写清输入、输出和验证门槛。
- “现状与落点”必须明确到目录、模块或文件层级，不能只写“后端处理一下”“前端改一下”。
- 若涉及新增文件，必须使用目录树（`text` 代码块）列出新增文件，并在树节点后用 `#` 标注用途；禁止普通条目描述。
- 若涉及数据库新表或字段变更，必须补齐可执行 SQL（`CREATE TABLE` / `ALTER TABLE`），不能只写自然语言。
- “方案选择”只在确实存在明显不同路线时出现；没有分歧时可省略冗余方案节。
- “实施步骤”按执行顺序写，不要按想到什么写什么；每一步都必须标明所属周期和所属阶段。
- 一个步骤只做一件事；若一步骤同时描述多个并列动作，必须继续拆分。
- “每步验证点”必须能指导后续编码审查、功能验证、回归判断与最终验收准备。
- 对复杂任务建议补图，图中步骤命名应与“实施步骤”保持一致，避免图文错位。
- “风险与阻断项”必须写真实阻断，不要写空话。

## 周期子文档补充

当任务已拆出 `实施周期NN_中文主题` 子文档时，周期文档至少包含以下内容：

- 对应需求文档
- 对应实施总览
- 当前周期目标
- 当前周期只做这一件事
- 当前周期步骤顺序
- 当前周期验证点
- 当前周期阻断项
