# Bug 侦察输出模板

结论：{用一句普通中文说明当前最可能的判断或排查结论}；影响：{说明受影响的用户、角色或业务环节}；范围：{说明本次已侦察和会继续确认的内容}；非范围：{说明本次不判断或不处理的内容}；变化：{说明问题对用户或业务造成的可感知变化}；完成标准：{说明侦察何时可以进入复现或根因定位}；术语说明：无技术术语需要解释，或 {术语：它在本次问题中的作用}；验证状态：{说明来源、查询或复现核验进展和依据}。

## 白话正文与附录

先用普通中文说明目前判断、影响、范围和非范围、问题造成的变化、进入下一阶段的条件、术语含义和是否需要继续排查；H1 后单段必须完整包含八个固定字段。来源清单、原始日志、查询、复现输入和证据 ID 统一进入文末连续附录。

## 推荐结构

```markdown
# Bug 侦察：{问题中文简介}

结论：{用一句普通中文说明当前最可能的判断或排查结论}；影响：{说明受影响的用户、角色或业务环节}；范围：{说明本次已侦察和会继续确认的内容}；非范围：{说明本次不判断或不处理的内容}；变化：{说明问题对用户或业务造成的可感知变化}；完成标准：{说明侦察何时可以进入复现或根因定位}；术语说明：无技术术语需要解释，或 {术语：它在本次问题中的作用}；验证状态：{说明来源、查询或复现核验进展和依据}。

## 现象复述

- 用户原始描述：
- 截图 / 报错关键信息：
- agent 当前理解：

## 已侦察来源

| 来源 | 类型 | 结论 | 证据等级 |
| ---- | ---- | ---- | -------- |

## 代码侦察

- 相关模块 / 文件：
- 可疑逻辑 / 分支 / 状态转换：
- 调用链（上游 -> 下游）：

## 数据侦察（本地库，只读）

- 连接的本地库：
- 执行的只读 SQL（脱敏）：
- 期望数据 vs 实际数据对比：
- 数据结论：数据本身错 / 写入逻辑错 / 读取逻辑错 / 数据正常

## 根因候选

| 候选 | 依据 | 证据等级 | 排除方式 |
| ---- | ---- | -------- | -------- |

## 风险与边界

- 影响范围：
- 是否涉及公共方法 / 共享模块 / 兼容性：
- 数据风险：

## 仍需用户确认

1. ...

## 需要手动执行的 SQL（如有）

- 目的：
- 脚本：
- 影响行 / 回滚方式：

## 记忆回写建议

- 需要写入 `PROJECT_MEMORY.md` 的线索：
- 不写入原因：

## 下一步

- 转交 skill：
- 进入条件：

## 执行附录

- local 查询、复现输入、只读 SQL、样本、命令、失败情况和清理说明：

## 追踪附录

- 来源清单、稳定标识、证据定位、根因候选追踪和记忆回写依据：
```

## 输出要求

- 待确认问题不超过 3 个，除非现象本身包含多个互不相关的问题。
- 每个根因候选必须至少有一个证据来源，并标注证据等级。
- 如果没查到有效证据，要明确写出查过哪里、为什么没有结论。
- 连库部分必须体现“只读、仅本地”；任何增删改只能以 SQL 脚本形式交用户手动执行。
- 若已能复现或定位，下一步转 `bug-reproduction-rules` 或 `bug-root-cause-rules`；怀疑运行期状态转 `bug-runtime-debug-rules`；仍缺关键信息转 `bug-gap-rules`。

## 执行附录

- 生成侦察记录时，把 local 查询、复现输入、只读 SQL、样本、命令、失败情况和清理说明放在这里。

## 追踪附录

- 生成侦察记录时，把来源清单、稳定标识、证据定位、根因候选追踪和记忆回写依据放在这里；本节必须紧随执行附录并位于文末。
