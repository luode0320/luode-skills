# 运行时诊断进入条件

## 用途

用于判断当前问题是否已经适合从静态定位升级到运行时诊断。

## 应进入运行时诊断

- 静态证据无法在多个假设之间做排除。
- 关键状态只存在于运行时。
- 偶发问题、时序问题、并发问题无法仅靠阅读代码确认。
- 缺少能直接指向异常位置的稳定日志或 trace。

## 不应过早进入

- 错误日志已经直接指到明显异常点。
- 调用链和代码路径仍然可以继续有效收缩范围。
- 当前只是因为“运行时看起来更快”而想跳过静态分析。
