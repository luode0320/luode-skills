interface:
  display_name: "Bug 问题录入"
  short_description: "标准化录入 Bug 入口并建立 `bug/` 根目录记录的规则"
  default_prompt: "使用 $bug-intake-rules 先把当前 Bug 的现象、影响范围、环境条件、期望结果和实际结果整理清楚，并建立当前 `bug/` 根目录记录；归档时必须同时产出 `README.md`、`flow.svg` 与 `sequence.svg`。"
