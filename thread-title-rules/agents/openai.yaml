interface:
  display_name: "会话标题规则"
  short_description: "在支持改名的环境中按当前任务主题更新会话标题"
  default_prompt: "Use $thread-title-rules to keep this thread title aligned with the current task when a real rename tool is available."
policy:
  allow_implicit_invocation: true
