interface:
  display_name: "新会话预热"
  short_description: "新会话开始时自动压缩最近 3 天项目上下文的触发规则"
  default_prompt: "使用 $recent-context-bootstrap-rules 先扫描近 3 天的 ment、test、bug、doc 和 git 提交，压缩出与当前任务相关的前置上下文，再进入对应主域继续处理。"
