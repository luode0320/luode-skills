<!--
本文件含一份模板：
「全局业务域索引」——复制 `# 业务域索引` 部分到 `internal/README.md`
占位符统一使用尖括号 `<...>` 形式，复制后按项目实际内容替换。
-->

# 业务域索引

> AI 进入项目的第一入口：先看这里定位到目标业务域，再进入对应业务域的 README 与代码，无需扫全仓。

| 业务域 | 一句话职责 | 状态（活跃\|冻结） | README |
|---|---|---|---|
| `<order>` | `<订单履约：下单、支付回调、发货状态流转>` | 活跃 | `./order/README.md` |
| `<user>` | `<用户账户：注册、登录、资料维护>` | 活跃 | `./user/README.md` |

> 维护提示：新增业务域时必须在此登记；业务域之间禁止直接 import，共享结构仅走根 `common/` 与 `global/`。
