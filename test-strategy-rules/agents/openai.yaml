interface:
  display_name: "测试策略"
  short_description: "以 test-task-root-layout-rules 为基准，制定测试优先级并统一写回时间戳任务 README"
  default_prompt: "使用 $test-strategy-rules 为当前任务制定测试策略，说明应优先做哪些验证、哪些路径可后补；策略摘要统一写回 `test/yyyy-MM-DD_HHmmss/<测试任务中文主题>/README.md`（中文目录名按策略摘要主题动态命名），如需多轮独立测试则给出多个时间戳根目录方案。"
