interface:
  display_name: "Swagger / OpenAPI 检查镜像规则"
  short_description: "只读检查 Swagger/OpenAPI 接入、同步和暴露策略"
  default_prompt: "使用 $api-swagger-review-rules 只读检查当前后端 HTTP API 是否满足 api-swagger-rules 的要求，不直接改代码。"
