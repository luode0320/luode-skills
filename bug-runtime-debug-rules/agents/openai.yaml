interface:
  display_name: "Bug 运行时诊断"
  short_description: "在静态证据不足时运行诊断并统一写回 `bug/` 根目录的规则"
  default_prompt: "使用 $bug-runtime-debug-rules 在静态定位不足时，通过断点、变量观察、debug 日志或运行时证据继续缩小 Bug 范围，并写回当前 `bug/` 根目录。"
