# 输出正反例

## 正例（有改动、有验证）

```text
Skill 命中检查：
- 命中 skill：`reasoning-summary-structure-rules`、`skill-compliance-gate-rules`

Skill 执行证据：
- 已读取并执行总结结构模板。

当前要解决的问题：
- 最终总结字段不统一，导致信息缺失。

当前问题的解决方案、问题根因：
- 解决方案：新增总结结构闸门并强制固定顺序输出。
- 根因：缺少独立的总结结构规范，收口依赖个人习惯。

当前解决结果、结论：
- 已完成规则落地，后续可按统一结构输出总结。

本次改动点：
- 新增 skill 文件与 references。
- 更新命中检查规则联动。

验证结果：
- 字典刷新成功，且新 skill 已出现在索引中。

函数注释核对：
- 函数位点 0 个。

下一步建议：
1. 在真实任务中跑 2-3 次回归验证总结模板可用性。
2. 如发现字段歧义，再补充边界示例。
```

## 正例（无改动、无验证）

```text
Skill 命中检查：
- 命中 skill：`reasoning-summary-structure-rules`

Skill 执行证据：
- 已按模板完成结构化总结。

当前要解决的问题：
- 需要给出本轮问题的结构化结论。

当前问题的解决方案、问题根因：
- 解决方案：按固定顺序输出必填字段。
- 根因：自由格式总结可读性和可复核性不足。

当前解决结果、结论：
- 已完成结构化总结收口。

下一步建议：
1. 进入下一阶段任务。
```

## 反例（缺必填项）

```text
结论：已完成。
```

不通过原因：

- 缺“Skill 命中检查”。
- 缺“Skill 执行证据”。
- 缺“当前问题 / 解决方案与根因 / 结果结论 / 下一步建议”。
