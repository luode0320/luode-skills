# 条件字段判定规则

## 1. 本次改动点

- 触发条件：本轮存在任何文件改动（代码或文档）。
- 输出要求：列出本轮核心改动点，避免流水账。

## 2. 验证结果

- 触发条件：本轮执行了验证动作（测试命令、构建检查、人工验证步骤等）。
- 输出要求：写清“验证了什么、结果如何、是否通过、是否有残留风险”。

## 3. 函数注释核对

- 触发条件：本轮存在代码改动。
- 输出要求：
  - 若改动涉及函数/方法：列出已核对的函数/方法位点。
  - 若代码改动不涉及函数/方法：明确写“函数位点 0 个”。

## 4. 不适用处理

- 条件字段未触发时，允许不输出该节。
- 不允许伪造“验证结果”或“函数注释核对”内容。
