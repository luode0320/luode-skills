# 项目当前状态

## 当前任务

- 目标：调整 Git 提交流程，保留必要的审查与验收步骤，但不因提交自动生成审查或验收文档。
- 范围：`git-collaboration-rules`、`project-change-review-rules`、`final-acceptance-rules` 及其 Git 提交引用、提示和项目记忆。
- 非范围：不删除显式总审查、正式最终验收或其他独立研发流程的文档归档；不执行 Git 提交、推送或历史改写。
- 状态：规则修改与跨文件一致性验证已完成。

## 已完成

- Git 提交前改为直接检查 staged 改动的格式、注释、安全性、并发安全性、系统崩溃风险、边界条件和验证适用性，并在提交证据中记录结论。
- 明确 Git 提交不自动触发完整 `project-change-review-rules` 或 `final-acceptance-rules`，不创建 `doc/6-审查/`、`doc/7-验收/` 文档。
- 保留用户显式总审查、正式最终验收和非 Git 正式放行的原有文档归档职责。
- 同步更新 Git 协作引用材料、证据模板、Agent 默认提示与项目长期记忆。

## 阻断

- 无。

## 验证

- `git diff --check`：通过。
- Git 审查/验收关键语义断言：6/6 通过；Agent YAML 可解析。
- 修改后的 Skill front matter、章节结构和描述/二级标题保护检查：通过。
- 项目记忆新增条目与机器索引结构断言：通过；既有索引含 Markdown 围栏，完整 YAML 解析不适用。

## 交接点

- 后续 Git 提交只输出基础审查与验收的提交证据；只有显式总审查或正式最终验收才生成相应归档文档。
