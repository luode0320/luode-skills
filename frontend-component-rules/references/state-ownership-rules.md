# 状态归属规则

## 基本判断

- 只被当前组件使用：优先留在局部 state
- 需要父子共享：优先上提到最近公共父层
- 跨页面或跨模块共享：再考虑 store 或全局状态
- 只是派生结果：优先计算得到，不要重复存一份状态

## 受控与非受控

- 表单和输入组件要明确是受控还是非受控
- 不要同一组件内部一半靠外部值，一半靠内部缓存，最后状态来源不清

## 常见反模式

- 本地能解决的问题直接上全局状态
- 同一份业务信息在多个层级重复维护
- 用 effect / watch 去同步本来应该单向流动的数据
