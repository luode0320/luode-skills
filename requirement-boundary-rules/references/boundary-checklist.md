# 需求边界检查清单

## 用途

用于明确当前需求到底覆盖哪些内容，不覆盖哪些内容。

## 检查项

- 当前需求直接目标是什么。
- 本次允许改动哪些模块、接口、页面或流程。
- 本次不允许顺手处理哪些历史问题。
- 本次是否涉及兼容性调整、上下游联动或旧数据影响。

## 判断原则

- 直接服务于当前目标的，优先纳入。
- 只是顺带暴露出来、且不属于当前目标的，优先拆出。
- 只要会明显扩大改动范围，就需要单独确认。
- 边界判定结论必须更新到当前需求对应的同一份 `ment/` 文档中。
