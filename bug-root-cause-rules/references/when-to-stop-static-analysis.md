# 何时停止静态分析

## 用途

用于判断当前是否已经不适合继续做纯静态定位。

## 应停止静态分析的信号

- 关键状态只能在运行时观察到。
- 静态证据不足以在多个假设之间做排除。
- 日志和 trace 信息缺失到无法继续收缩范围。
- 再继续读代码只是重复原地打转。

## 下一步

- 转入 `bug-runtime-debug-rules`。
- 明确记录静态分析做到哪一步、卡在什么地方、缺什么运行时证据。
- 上述停止点和升级原因统一写回当前 Bug 根目录，继续沿用同一个 `bug/yyyy-MM-DD_HHmmss_问题中文简介/` 目录。
