interface:
  display_name: "接口请求检查镜像规则"
  short_description: "只读检查请求DTO、body 结构和校验边界"
  default_prompt: "使用 $api-request-review-rules 只读检查当前请求参数、DTO、path、query 与 body 结构是否满足 api-request-rules 的要求，不直接改代码。"
