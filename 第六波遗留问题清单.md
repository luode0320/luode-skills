# 第六波遗留问题清单

## 非阻塞遗留项

- `frontend-component-rules`、`test-strategy-rules`、交付域相关 skill 尚未作为本轮首版闭环的一部分单独落地。
- `auth-security-rules` 已接入中文安全 seeds，但后续仍可继续按具体框架把更多细则内化到独立 references 中。
- `integration-debugging-rules` 当前聚焦联调证据与分流，后续还可补更细的跨团队协作模板和联调记录模板。

## 后续演进注意事项

- 继续新增 skill 时，优先沿用当前“小 skill、强边界、自动触发 description、SKILL 精简、细则进 references”的模式。
- 如果后续发现某个 skill 触发过宽，应优先收窄 description 和边界，不要用总控层兜底吞并。
- 如果要扩展到具体前端框架、部署域、评审域、测试策略域，应优先补边界判定，再开始拆 skill。
