# 路径命名与 HTTP 语义

## 用途

用于统一路径命名、资源表达和 HTTP 方法选择。

## 路径原则

- 优先用资源语义命名，而不是动作堆砌。
- 路径层级应反映资源关系，不应反映内部实现细节。
- 能用资源 + 方法表达的，不额外塞动词。

## 方法语义

- `GET`：查询、读取
- `POST`：创建或触发非幂等动作
- `PUT`：整体替换或明确幂等更新
- `PATCH`：局部更新
- `DELETE`：删除

## 提醒

- 不要把所有操作都塞进 `POST`。
- 语义不清时先澄清资源对象和动作，再选方法。
