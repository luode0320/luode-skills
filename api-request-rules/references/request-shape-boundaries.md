# 请求结构边界

## 用途

用于区分 path、query、body 和 DTO 在接口中的职责。

## 常见边界

- `path`
  适合表达资源定位和层级关系。
- `query`
  适合表达筛选、分页、排序、非主体查询条件。
- `body`
  适合表达创建、更新、批量提交等主体输入载荷。
- `DTO`
  适合作为请求层稳定输入模型，不直接暴露内部持久化模型。

## 原则

- 定位资源优先放 path。
- 查询过滤优先放 query。
- 主体载荷优先放 body。
- DTO 服务于接口契约，不服务于偷懒复用内部结构。
