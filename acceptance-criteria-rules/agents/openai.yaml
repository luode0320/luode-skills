interface:
  display_name: "验收标准"
  short_description: "把需求目标细化成实施前可验证完成标准，并单独落到 doc/7-验收 的验收标准文档"
  default_prompt: "使用 $acceptance-criteria-rules 为当前需求补齐实施前可验证的验收标准，覆盖主路径、异常路径和边界条件，并生成或更新 doc/7-验收 下独立的验收标准文档。"
