# 调试日志样例

## 合理

- 为某个可疑分支补一条条件日志，记录输入摘要和命中结果。
- 在上下游边界各打一条日志，用来比较入参与出参是否失真。
- 将本轮日志落点、观察结果和删除状态统一记录到 `bug/2026-03-28_171500_订单退款状态错乱排查/README.md`。

## 不合理

- 把整个请求对象、整个数据库结果全量打印。
- 为了找一个偶发问题，在整条链路每层都加日志。
- 问题已经修复，但调试日志还留在交付代码中。
- 同一个 Bug 的日志记录写在 `analysis/`，断言记录写在其他目录，导致排查链路分散。
