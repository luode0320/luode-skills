# 公共工具落点

## 用途

用于决定通用工具、常量和公共组件应放在哪一层。

## 建议

- 小型无状态工具放 `utils` 或语言生态等价目录。
- 稳定常量放 `constants`。
- 通用 UI 组件放明确的公共组件目录。
- 仍带业务语义的代码留在业务层，不放公共层。

## 原则

- 落点应与职责一致。
- 公共层不反向依赖业务层。
- 不要为了一个函数就新建模糊公共目录。
