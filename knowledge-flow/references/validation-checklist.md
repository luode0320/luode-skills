# 验证清单

## 目录可达性

- [ ] `D:\谷歌云盘\知识库\` 存在且可读
- [ ] 写入后回读一致
- [ ] 笔记总数与迁移基线一致（迁移后基线为 59 篇，变更时同步更新本项与 PROJECT_MEMORY 记录）

## 文件操作

- [ ] 路径安全规则验证（裸相对路径，不以 知识库/ 开头，无 ..、盘符、UNC）
- [ ] 知识库根下不存在嵌套 知识库\ 子目录
- [ ] UTF-8 编码验证
- [ ] 写后回读验证

## 机器索引

- [ ] `knowledge_index.py build` 覆盖笔记数与实际 `.md` 数一致
- [ ] `knowledge_index.py query` 对标题、别名、标签、topics、主题、路径六类字段均可命中
- [ ] 新增笔记后直接 query 能命中（索引自动重建生效）

## 主题合规

- [ ] `20-Knowledge` 下目录全部在固定 7 主题 + 3 契约落点清单内
- [ ] `knowledge_index.py stats` 的 themes 中无 `unknown`
- [ ] 巡检与索引对同一路径推导出的主题一致（两处共用 `note_theme`）

## frontmatter 契约

- [ ] `knowledge_index.py check` 退出码为 0（活动区必填字段齐全、状态在枚举内）
- [ ] 归档区 `90-Archive/` 按豁免处理，不计入违规
- [ ] 带 BOM 的笔记不被误判为无 frontmatter

## 冲突候选质量

- [ ] 巡检候选抽样确认无误报（归组要求同主题 + 共享标签 >= 2，或标题相似度达阈值）
- [ ] 沉淀前已用 `query` 查过同主题近似笔记，而不是留给事后巡检

## 检索

- [ ] Select-String / rg 能命中已知关键词
- [ ] wikilink 引用可扫描
- [ ] 引用台账完整

## 捕获/沉淀

- [ ] 三态判定正确
- [ ] 取代时双向接替关系写入
- [ ] INDEX.md 和 MOC 更新

## 执行案例

- [ ] 目录不可移动、不可删除
- [ ] 状态事件追加式
- [ ] 脱敏验证
