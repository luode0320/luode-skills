interface:
  display_name: "Windows WSL Execution Rules"
  short_description: "Windows 仓库默认通过 WSL 执行启动、调试与测试规则"
  default_prompt: "Use $windows-wsl-execution-rules to keep ordinary Windows repo commands on Git Bash/bash, route execution commands through WSL, apply PowerShell fallback patterns only for Windows-specific shell work, and record verified cross-environment command failures in the recovery case library."
