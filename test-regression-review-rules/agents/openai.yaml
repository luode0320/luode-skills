interface:
  display_name: "回归验证审查镜像规则"
  short_description: "只读检查回归范围、用例选取、兼容性影响和留痕"
  default_prompt: "使用 $test-regression-review-rules 只读检查回归验证是否满足 test-regression-rules 的回归范围、用例选取、兼容性影响、结果留痕和测试隔离要求，不直接执行回归或写回归结论。"
