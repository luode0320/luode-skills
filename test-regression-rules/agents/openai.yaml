interface:
  display_name: "回归验证规则"
  short_description: "以 `test-location-rules` 为基准统一回归范围与结论留痕的规则"
  default_prompt: "使用 $test-regression-rules 判断当前改动需要回归哪些旧能力、应选择哪些回归用例，并把回归结论统一记录到当前 `test/` 任务目录。"
