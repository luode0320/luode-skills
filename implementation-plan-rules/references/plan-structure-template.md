# 实施计划文档结构模板

## 建议结构

```md
# 需求实施计划：{中文标题}

## 1. 基本信息
- 对应需求文档：
- 对应验收文档：
- 当前计划范围：
- 当前优先闭环：
- 当前状态：

## 2. 现状与落点
- 涉及目录：
- 涉及文件 / 模块：
- 复用点：
- 需要新增的内容：
- 新增文件清单（按需必填，目录树格式）：
  ```text
  app/
  └── exchange/
      ├── bizpolicy/
      │   └── context.go                     # 业务上下文与过滤规则定义
      ├── repository/
      │   ├── exchangeBusinessControl.go     # 配置查询仓储
      │   └── model/
      │       └── exchangeBusinessControl.go # 配置实体模型
      └── service/
          └── business_filter.go             # 过滤服务（链/币/渠道）

  scripts/
  └── 20260416_exchange_business_control.sql # 数据库变更脚本
  ```

## 3. 方案选择
- 方案 A：
- 方案 B：
- 如有必要再补方案 C：
- 推荐方案与原因：

## 4. 实施步骤
1. 第一步：
2. 第二步：
3. 第三步：

## 5. 每步验证点
- 步骤 1 验证：
- 步骤 2 验证：
- 步骤 3 验证：

## 6. 风险与阻断项
- 风险：
- 依赖：
- 暂停条件：

## 7. 数据库变更 SQL（按需必填）
- 建表 SQL（`CREATE TABLE`）：
- 字段变更 SQL（`ALTER TABLE`）：

## 8. 自审结论
- 覆盖度检查：
- 占位词检查：
- 可执行性检查：
- 用户确认状态：
```

## 结构要求

- 必须先写清当前优先闭环，不要把整张大路线图塞成一份笼统计划。
- “现状与落点”必须明确到目录、模块或文件层级，不能只写“后端处理一下”“前端改一下”。
- 若涉及新增文件，必须使用目录树（`text` 代码块）列出新增文件，并在树节点后用 `#` 标注用途；禁止普通条目描述。
- 若涉及数据库新表或字段变更，必须补齐可执行 SQL（`CREATE TABLE` / `ALTER TABLE`），不能只写自然语言。
- “方案选择”只在确实存在明显不同路线时出现；没有分歧时可省略冗余方案节。
- “实施步骤”按执行顺序写，不要按想到什么写什么。
- “每步验证点”必须能指导后续编码审查、功能验证或回归判断。
- “风险与阻断项”必须写真实阻断，不要写空话。
