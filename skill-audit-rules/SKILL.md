---
name: skill-audit-rules
description: 【强制自动触发】当主任务存在多 skill 组合、并行拆分或规则收口风险时触发。负责只读审计当前任务是否漏触发应有的 skill，以及已触发 skill 是否还有未执行完的规则；默认优先并行；输出补漏提醒和遗漏清单。只有存在未完成的必需规则、阻断缺口或用户明确要求建议时，才输出必要后续动作；原始目标已完成且无必需动作时不得输出下一步类占位文案。本 skill 不改代码、不写文件、不做最终收口。
---

# Skill 审计规则

## 目标

并发检查主任务是否漏触发该触发的 skill，以及已触发 skill 是否还有未执行完的规则。

## 核心职责

1. 识别当前主任务是否漏触发应有的 skill。
2. 识别已触发 skill 是否还有未执行完的规则。
3. 输出遗漏清单和风险点；只有存在未完成的必需规则、阻断缺口或用户明确要求建议时，才输出必要后续动作。
4. 只读审计，不改代码、不写文件、不做收口。

## 输入

- 主线程当前的任务摘要
- 已命中的 skill 列表
- 当前 diff、计划或执行摘要

## 输出

- 漏触发的 skill
- 已触发但未执行完的规则
- 必要后续动作（仅未完成 / 阻断 / 用户明确要求建议时输出）

## 边界

- 不替代 `skill-hit-check-rules`
- 不替代 `parallel-task-dispatch-rules`
- 不替代 `skill-compliance-gate-rules`
- 不执行写入型任务
- 只看当前轮次，不继承历史轮次的 skill 推断结果。
- 只看主线程本轮已声明的 skill，不靠历史记忆补全未声明项。
- 原始目标已完成且无必需动作时，不输出“下一步建议 / 下一步状态 / 等待用户新指令 / 无需继续动作 / 无下一步”等占位文案。

## 使用时机

- 主任务包含多个 skill 组合
- 主线程已执行，但担心遗漏规则
- 需要并发复核“是否触发齐全”与“是否执行完整”
