# 结构变更正反例

## 正例

- 新增可空字段，先上线兼容代码，再逐步回填，再改为非空。
- 新增索引前先确认查询场景和写入影响，不把所有字段都机械加索引。
- 删除旧字段前先确认没有旧代码、旧接口和旧任务仍在依赖。
- 把结构变更迁移和数据修复迁移分成独立步骤。

## 反例

- 在同一条迁移里同时删列、回填、改语义、改查询逻辑。
- 直接把高频表字段从宽类型改成窄类型，却没有兼容或回滚说明。
- 给关键字段加危险默认值，只为了绕过现有脏数据。
- 查询慢就直接改 schema，但没有确认瓶颈到底在结构还是查询实现。
