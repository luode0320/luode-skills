# 需求实施总览：知识库从 Obsidian 迁移到谷歌云盘

结论：将知识库从 D:\obsidian_data（Obsidian vault + CLI bridge）迁移到 D:\谷歌云盘\知识库\（Google Drive 同步的普通 Markdown 文件夹），废除 obsidian_cli_bridge.py 桥接层，改为纯文件系统读写；影响：所有依赖知识库的 skill 规则、引用路径、首条中间进度状态字段、自举脚本、记忆四件套和字典需同步更新；范围：数据迁移、目录改名、bridge 废除、skill 重写、10 个联动 skill 引用更新、规则文件与自举模板同步、记忆四件套更新、测试改造与字典重生成；非范围：保留 D:\obsidian_data 备份、不动 doc/ 历史文档、不动 ercel-react-best-practices/AGENTS.md；变化：首条中间进度 Obsidian:<...> 改为 知识库:<...>，bridge 链路废除，读写改为标准文件工具；完成标准：65 篇笔记齐全无丢失、全仓库 obsidian 关键字 grep 结果为 0、三个测试通过、字典更新、AGENTS.md/CLAUDE.md/bootstrap_agents.sh 逐字一致；术语说明：知识库——Google Drive 同步的普通 Markdown 文件夹；验证状态：计划已就绪待执行。

## 1. 当前计划最终方案简要说明
- 推荐方案：把知识库从 Obsidian 迁移到谷歌云盘，废除 bridge 层，改用纯文件读写
- 主落点：知识库/ skill 更名 knowledge-flow/，首条中间进度字段改为 知识库:<...>
- 为什么走这条路线：谷歌云盘多端同步更方便，去掉 Obsidian 应用依赖后链路更短更稳

## 2. 基本信息
- 对应需求文档：用户提供的粘贴文本
- 来源对象标识：知识库迁移
- 当前实施文档命名主干：2026-08-12_102514_知识库迁移
- 需求与实施计划全量顺序实施方案：N/A（单来源对象）
- 实施计划完成条件所在章节：见下方各周期完成条件
- 对应 6-review 风格回归记录：N/A（本仓库非代码仓库，以文件校验代替）
- agent 理解的问题/目标：将知识库从 Obsidian 迁到谷歌云盘，废除 bridge 层
- 当前计划范围：4 个实施周期，覆盖数据迁移、skill 重写、联动更新、测试收口
- 明确不在范围：不清除 D:\obsidian_data、不改 doc/ 历史文档、不改 vercel-react-best-practices/AGENTS.md
- 当前优先闭环：已全部收口（CYCLE 1~5 完成）
- 关键假设/待确认点：D:\谷歌云盘 已由 Google Drive 接管并做本地实盘同步（2026-08-12 已实测写入-回读通过）
- 外部项目代码引用：N/A，本项目完全自包含
- 当前状态：CYCLE 1~5 全部完成，44 项测试全绿，改动停在已改动未提交状态
- 已获得开始实施授权：是
- 续做记录（2026-08-12 第二会话）：CYCLE 1 由首个会话完成；本会话发现 `知识库/` 前缀叠加导致嵌套目录的根因，新增 CYCLE 5 修复，并补完首个会话遗留的 CYCLE 2/3/4

## 3. 实施周期总览
- 总周期说明：4 个实施周期，按依赖关系顺序执行
- 周期拆分原则：按依赖关系，CYCLE 1 为基础，CYCLE 2/3 依赖 CYCLE 1，CYCLE 4 依赖所有前置周期
- 周期排序说明：CYCLE 1→CYCLE 2→CYCLE 3→CYCLE 4

### 周期 1：数据迁移与新 skill 主体
- 周期序号：第 1 期
- 周期目标：完成存量数据迁移、目录改名、bridge 删除、SKILL.md 重写、references 改写、脚本改造、agents 同步
- 包含的最小任务：T1-1~T1-8
- 执行顺序：T1-1,T1-2,T1-3,T1-7,T1-8 可并行 → T1-4,T1-5,T1-6 依赖前置
- 进入条件：无
- 收口条件：T1-1~T1-8 全部完成
- 完成标志：D:\谷歌云盘\知识库\ 下 65 篇笔记齐全，obsidian-knowledge-flow/ 已更名为 knowledge-flow/

### 周期 2：联动 skill 引用更新
- 周期序号：第 2 期
- 周期目标：更新 10 个联动 skill 的引用与规则表述
- 包含的最小任务：各 skill 引用更新
- 执行顺序：按 skill 列表依次执行
- 进入条件：CYCLE 1 完成
- 收口条件：所有 10 个 skill 引用已更新
- 完成标志：全仓库 grep "obsidian" 结果减少

### 周期 3：规则文件、自举模板与记忆四件套
- 周期序号：第 3 期
- 周期目标：更新 AGENTS.md、CLAUDE.md、bootstrap_agents.sh 模板、记忆四件套
- 包含的最小任务：T3-1~T3-3
- 执行顺序：T3-1→T3-2→T3-3
- 进入条件：CYCLE 2 完成
- 收口条件：AGENTS.md/CLAUDE.md/bootstrap_agents.sh 逐字一致，记忆四件套已更新
- 完成标志：全仓库 grep "obsidian" 结果趋于 0

### 周期 4：测试与字典收口
- 周期序号：第 4 期
- 周期目标：删除废弃测试、改造巡检测试、新增残留扫描测试、字典重生成
- 包含的最小任务：T4-1~T4-6
- 执行顺序：T4-1→T4-2→T4-3→T4-4→T4-5→T4-6
- 进入条件：CYCLE 3 完成
- 收口条件：所有测试通过、字典已重生成
- 完成标志：全仓库 grep "obsidian" 结果为 0（活动资产口径）

### 周期 5：路径前缀根因修复与知识库内容治理（2026-08-12 第二会话新增）
- 周期序号：第 5 期
- 周期目标：修掉 `知识库/` 前缀叠加这条错误约定，消除嵌套目录，并完成知识库内容治理
- 新增原因：用户在文件管理器发现 `D:\谷歌云盘\知识库\知识库\` 嵌套目录。根因是规则仍要求笔记路径以 `知识库/` 开头，而根本身已经是 `知识库`，前缀叠加多出一层。该错误约定在 Obsidian 时代即已存在（原 `D:\obsidian_data\知识库\知识库\` 是同一成因），迁移只是把它带了过来。
- 包含的最小任务：
  - T5-1 修 4 处前缀约定（`knowledge-flow/SKILL.md`、`references/file-operations.md`、`references/validation-checklist.md`、`scripts/render_execution_case.py`），路径基准改为相对知识库根的裸相对路径
  - T5-2 `knowledge-layout.md` 新增「禁止嵌套知识库目录」条款与谷歌云盘冲突副本处置
  - T5-3 嵌套层 1 篇实质笔记合并到 `20-Knowledge/研发流程/`，删除嵌套残留 INDEX.md 与空目录
  - T5-4 删除 4 篇已作废 Obsidian CLI 笔记与 Obsidian 默认欢迎页（删前逐篇 Grep 反链取证）
  - T5-5 2 处散落文件归位到标准布局，`INDEX.md` 死链小节清理并登记 3 篇迁移笔记
  - T5-6 治理 5 篇活动笔记的过期口径；blog-data 系列的 "Obsidian vault" 作为源 vault 真实来源事实保留
  - T5-7 新增 `path_prefix_contract_test.py` 与 `legacy_reference_scan_test.py` 常驻回归
- 进入条件：CYCLE 1 已完成
- 收口条件：嵌套目录消除、前缀约定归零、全库 wikilink 无新增死链、两个新测试通过
- 完成标志：按修正后规则真实写入笔记，落点为 `D:\谷歌云盘\知识库\00-Inbox\` 而非嵌套层
- 实测结果：笔记 65→59；`path_prefix_contract_test` 5/5、`legacy_reference_scan_test` 7/7、`vault_audit_contract_test` 14/14、`knowledge_citation_contract_test` 20/20，合计 46 项全绿；死链 17 处全为既有实体占位链接，指向本轮已删笔记的死链为 0
- 独立审计追加发现（另派只读子代理交叉审计，11 项阻断级 + 多项建议级，全部已修）：
  - 巡检脚本 `find_orphans` 的判定条件恒为真，孤儿治理静默归零且假阴性；修复后在 39 篇范围内检出 18 篇真实孤儿
  - `knowledge-flow/SKILL.md` 只链接 1 个 references，其余 8 个规则文件对执行方不可发现；已恢复 9 条按场景索引
  - `project-memory-sync.md` 从 6789 字节缩到 734 字节，丢失 4 条初判标准与整节复核标准，而 3 处规则委派它为唯一事实源；已按新架构恢复五项完整内容
  - `execution-case-notes.md` 6 行状态转换表被压缩成一句错误概括，与 owner 侧 4 处冲突；已恢复
  - `conflict-staleness.md` 分级判据被改写成只按引用数分档，与本轮写入的 `PROJECT_MEMORY` 冲突；已恢复按剩余价值分档 + 引用数作前置守卫，并补回来源优先级、状态含义与敏感信息处置
  - `hit-checklist.md` 指向已改名文件、`validation-checklist.md` 写死 65 篇、`agents/openai.yaml` 调用 token 被 sed 吞成 `-flow`、机器索引 `evidence.source` 指向已删目录、接替关系类型不对称、`note-schema.md` 丢失 `deprecated` 枚举与状态互斥规则，均已修
  - 首版 6-review 漏检本轮改动量最大的 `audit_vault_knowledge.py`（三段式 0 处、11 处英文输出、函数体内导入），已补入受检范围并修复
- 过程中发现并修复的额外缺口：
  - `capture-retrieve-distill.md` 在首个会话重写时丢失 4 条引用台账契约（立即登记、入表门槛、笔记名来源、台账落点），由契约测试暴露后补回
  - `render_execution_case.py` 产出 `storage: obsidian` / `obsidian_path:`，与 `case-template.md` 已改的 `storage: knowledge-base` / `note_path:` 契约不一致，已对齐并新增契约测试
  - `conditional-sections-rules.md` 与 `output-examples.md` 残留 3 处 `verified=true` bridge 判据
  - `test/continuous-code-quality-supervisor-rules/supervisor_state_test.py` 残留旧 skill 名
  - `PROJECT_MEMORY.md` 机器索引区在改写中引入 YAML 双引号转义缺陷（`\谷` 非法转义），已修复并回读校验 43 实体可解析

## 4. 最小任务清单

### 最小任务 T1-1：存量迁移
- 所属周期：周期 1
- 周期内顺序：1
- 本任务只做这一件事：将 D:\obsidian_data\知识库\ 复制到 D:\谷歌云盘\知识库\
- 真实测试是否必需：是
- 真实测试入口：文件数量对比+抽样 5 篇内容比对
- 任务完成条件：65 篇笔记齐全，抽样 5 篇逐字一致
- 任务停止条件：D:\谷歌云盘 不可写或 Google Drive 流式模式

### 最小任务 T1-2：目录改名
- 所属周期：周期 1
- 周期内顺序：2
- 本任务只做这一件事：git mv obsidian-knowledge-flow → knowledge-flow
- 真实测试是否必需：否（纯目录重命名）

### 最小任务 T1-3：删除 bridge 脚本
- 所属周期：周期 1
- 周期内顺序：3
- 本任务只做这一件事：删除 obsidian_cli_bridge.py、obsidian_cli_windows.ps1、distill_vault.py

### 最小任务 T1-4：重写 SKILL.md
- 所属周期：周期 1
- 周期内顺序：4
- 本任务只做这一件事：重写 knowledge-flow/SKILL.md

### 最小任务 T1-5：重写 references
- 所属周期：周期 1
- 周期内顺序：5
- 本任务只做这一件事：重写 10 个 references 文件

### 最小任务 T1-6：改造 audit 脚本
- 所属周期：周期 1
- 周期内顺序：6
- 本任务只做这一件事：去掉 bridge 依赖，改为 pathlib 遍历

### 最小任务 T1-7：render 脚本文案
- 所属周期：周期 1
- 周期内顺序：7
- 本任务只做这一件事：把 "通过 bridge create/append 写入" 改为 "通过文件写入"

### 最小任务 T1-8：agents 同步
- 所属周期：周期 1
- 周期内顺序：8
- 本任务只做这一件事：更新 agents/openai.yaml 描述

## 5. 真实测试安排
- 真实测试入口：见各周期测试说明
- 依赖环境：D:\谷歌云盘 已挂载、Python 环境
- 通过标准：3 个测试脚本全部通过

## 6. 风险与阻断项
- D:\谷歌云盘 流式模式导致读写不稳 → 先做写入-回读探测
- bootstrap_agents.sh 漏改 → 由 T4-4 残留扫描覆盖
- 谷歌云盘同步冲突产生副本 → 在 knowledge-layout.md 写入冲突副本识别约定

## 7. 任务完成条件
- D:\谷歌云盘\知识库\ 下 65 篇笔记齐全，抽样逐字一致
- 全仓库 grep "obsidian" 结果为 0
- 三个测试全部通过
- 字典已重新生成
- AGENTS.md/CLAUDE.md/bootstrap_agents.sh 逐字一致

## 8. 任务停止/结束条件
- D:\谷歌云盘 不可用 → 停止
- 存量笔记校验不一致 → 停止
- 字典生成失败 → 停止

## 9. 最大推进边界
- 推进到 CYCLE 4 全部完成、测试通过、字典刷新为止
- 不提交 git
- 不清理 D:\obsidian_data
- 不动 doc/ 历史文档
- 不动 vercel-react-best-practices/AGENTS.md
