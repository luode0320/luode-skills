---
schema_version: 1
doc_id: IMP-CYCLE-SKILL-SPLIT-04-20260716
doc_type: implementation_cycle
source_ids:
  - SRC-SKILL-SPLIT-20260716
  - REQ-SKILL-SPLIT-20260716
  - AC-SKILL-SPLIT-20260716
  - CYCLE-SPLIT-04
  - CYCLE-SPLIT-03
status: done
version: v1.3
current_slice: TASK-SPLIT-04-01~06 done, closed (real deletion executed + release_test_engine relocation gap fixed in TASK-SPLIT-04-06)
complexity: L4
baseline_commit: 40cae893706639eb2323328f84b70b1c3aba66d9
template_version: 1
updated_at: 2026-07-21 12:00:00
reader_level: business_general
writing_style: plain_chinese
appendix_policy: preserve_existing_or_one_terminal_appendix
review_acceptance_gates:
  - stage: review
    applicability: applicable
    reason: "本周期需要冻结接口事实基线与上线测试执行的资源和 engine owner。"
    basis: "周期 03 已完成合规入口的计划闭环或记录阻断。"
    required_by_source: true
    required_now: false
    completed_validation: []
    substitute_validation: []
    manual_follow_up: "用户 review baseline 与 execution 的资源边界后再授权。"
    pass_standard: "接口基线、OpenAPI 双索引、参数依赖与执行报告的职责独立且 engine 只有一个主 owner。"
  - stage: acceptance
    applicability: limited
    reason: "本周期是未来 skill 拆分执行契约。"
    basis: "本轮未获得开始实施授权。"
    required_by_source: false
    required_now: false
    completed_validation: []
    substitute_validation:
      - EVIDENCE-SKILL-PLAN-20260716
    manual_follow_up: "实施完成后按 AC-SKILL-SPLIT-003、AC-SKILL-SPLIT-005、AC-SKILL-SPLIT-006、AC-SKILL-SPLIT-007 重验。"
    pass_standard: "N/A + 原因 + 证据：当前只提供周期计划。"
  - stage: third_party
    applicability: not_applicable
    reason: "真实 engine 验证只允许已有 local fixture。"
    basis: "不调用业务 test/prod 接口或外部服务。"
    required_by_source: false
    required_now: false
    completed_validation: []
    substitute_validation: []
    manual_follow_up: N/A
    pass_standard: N/A
---

# 实施周期 04：接口基线与上线测试执行

结论：本周期把 `project-release-test-rules` 按“接口事实基线/参数依赖/双索引”和“执行器/报告/放行/测试资产”二分；影响：接口集合与运行结果不再由同一超大入口解释，engine 仍保留一个协议中立执行 owner；范围：14 个 references、基线资产和 `scripts/release_test_engine/` 的职责映射、迁移与回归；非范围：不改变接口协议语义、不连接非 local 环境、不重写业务项目基线；变化：基线 skill 负责事实和依赖，执行 skill 负责行为和放行；完成标准：四个任务逐个完成覆盖、真实 engine 回归、触发和删除前检查；术语说明：接口基线是当前代码接口集合与来源证据，执行 engine 是根据统一 IR 运行 local fixture 的协议中立内核；验证状态：计划草案，等待用户 review。

## 当前周期目标

- 周期 ID / 期次定位：`CYCLE-SPLIT-04` / 第四期：上线测试。
- 只做这一件事：拆分 `project-release-test-rules` 的 baseline 与 release execution 职责。
- 对应文档：[`实施总览`](2026-07-16_114619_Skill体积治理与拆分_实施总览.md)、[`周期 03`](2026-07-16_114619_Skill体积治理与拆分_实施周期03_技能合规与代码收口.md)、[`验收标准`](../7-验收/2026-07-16_114619_Skill体积治理与拆分_验收标准.md)。
- 本周期不做：连接业务接口、刷新真实项目 swag、改变 engine IR 语义或删除旧 skill。

## 周期图片资产决策与边界

- 图片资产决策：`N/A + 原因 + 证据`：接口基线、依赖图和执行结果使用表格、JSON fixture 和 Mermaid，不需要视觉产物。
- Mermaid 边界：baseline 到 runner 的依赖、状态和放行门禁用 Mermaid；截图不能替代接口证据。

## 周期图片资产清单

| 图片 ID | 用途 / 生成输入 | 来源 | 相对路径 | 版本 | 关联 REQ/RULE / AC / CYCLE / TASK | 引用章节 | 敏感状态 | 版权状态 |
|---|---|---|---|---|---|---|---|---|
| 不适用：依据本周期范围，无图片资产 | 不适用：依据接口测试范围，无图片输入 | 不适用：依据范围，无图片来源 | 不适用：依据范围，无图片路径 | 不适用：依据范围，无版本 | `REQ-SKILL-SPLIT-004` / `CYCLE-SPLIT-04` | 不适用：依据范围，无图片引用章节 | 不适用：依据范围，无图片敏感信息 | 不适用：依据范围，无图片版权对象 |

## 进入条件与收口条件

| 类型 | 条件 | 证据/命令 | 状态 |
|---|---|---|---|
| 进入 | 周期 03 的合规和代码收口 owner 已冻结 | 周期 03 验证矩阵 | planned |
| 进入 | `project-release-test-rules/SKILL.md`、14 个 references、`scripts/generate_release_test_plan.py` 和 engine 目录清单已生成 MD5/字节报告 | `Get-ChildItem`、`Get-FileHash`、UTF-8 回读 | planned |
| 收口 | baseline 与 execution 规则和资源覆盖 100%，engine 只有一个主 owner | `TEST-SPLIT-013`、`TEST-SPLIT-014` | planned |
| 收口 | 已有 local engine tests 全部通过且结果枚举、脱敏和放行语义不变 | `TEST-SPLIT-015`、`TEST-SPLIT-016` | planned |

图形目的：固定 baseline、执行、回归和删除前验证的单向依赖，防止两个新 skill 同时写接口事实或运行结果。关联 ID：`CYCLE-SPLIT-04`、`TASK-SPLIT-04-01` 至 `TASK-SPLIT-04-04`。

```mermaid
flowchart LR
    BASE[接口基线与参数依赖] --> IR[统一 IR 与 schema]
    IR --> RUN[执行器与协议 adapter]
    RUN --> REPORT[报告与放行]
    REPORT --> TEST[local engine 回归]
    TEST --> DELETE[删除前承接检查]
```

## 当前代码/文档基线

- 分支 / 提交：`40cae893706639eb2323328f84b70b1c3aba66d9`；旧 `project-release-test-rules` 只作冻结对照。
- 已核实文件和符号：`references/baseline-asset-rules.md`、`openapi-inventory-sync-rules.md`、`interface-inventory-schema.md`、`dependency-graph-rules.md`、`inventory-reconcile-rules.md`；`scripts/release_test_engine/storage.py`、`discovery.py`、`runner.py`、`report.py`、`gate.py`、`model.py`。
- 依赖版本与 local 配置：Python、现有 `doc/5-tests/2026-07-12_180240_project-release-test-rules/tests` fixture；所有业务连接必须来自 local 配置。
- 与计划不一致时的停止规则：发现基线和 execution 同时写同一事实、IR 版本变化、测试结果枚举变化或 fixture 需要非 local 服务，记录 `GAP-SKILL-009` 并停止。

## 周期内最小任务执行顺序

| 顺序 | 任务 ID | 唯一目标 | 前置依赖 | 允许文件 | 禁止触碰区 | 状态 |
|---:|---|---|---|---|---|---|
| 1 | `TASK-SPLIT-04-01` | 原子化 baseline、依赖和 execution 规则资源 | 周期 03 收口 | `doc/5-tests/2026-07-17_155229/skill-split-validation/mapping/release-test-rules.yaml` | 新 skill 和 engine 实现 | done |
| 2 | `TASK-SPLIT-04-02` | 建立接口基线新 skill 并迁移双索引/参数依赖 | TASK-SPLIT-04-01 通过 | `project-interface-baseline-rules/` 与 baseline references | execution references 和 engine 行为 | done |
| 3 | `TASK-SPLIT-04-03` | 建立 execution 新 skill 并承接 engine、报告和放行 | TASK-SPLIT-04-02 通过 | `project-interface-release-execution-rules/`、engine references | baseline 双索引和业务项目数据 | done |
| 4 | `TASK-SPLIT-04-04` | 完成 engine 回归、pre/post delete 和引用清理 | TASK-SPLIT-04-03 通过 | 测试 README、mapping、字典、引用报告 | 旧 skill 删除动作 | done |
| 5 | `TASK-SPLIT-04-05` | 更新仓库级路由与交叉引用 | TASK-SPLIT-04-04 删除前检查通过 | `编码skill.md`、`README.md`、`PROJECT_MEMORY.md`、`PROJECT_STYLE.md`、`inventory.yaml`及 4 处交叉引用 skill | engine 实现、旧 skill 内容 | done |

## 文件与符号操作契约

| 任务 | 文件路径 | 符号/区段 | 操作 | 修改前职责 | 修改后职责 | 调用方影响 | 兼容要求 |
|---|---|---|---|---|---|---|---|
| `TASK-SPLIT-04-01` | 旧 SKILL、14 refs、engine 清单、mapping | 规则与资源条目 | 只读原子化 | baseline、执行、报告混合 | 分成 baseline/execution 两组 | 后续两个入口读取 | IR 和结果语义不变 |
| `TASK-SPLIT-04-02` | `project-interface-baseline-rules/` | inventory、OpenAPI、参数和依赖 references | 新增/迁移 | 旧 skill 负责事实与测试 | 新 skill 只负责接口事实 | execution 读取稳定 IR | 双索引保持同步 |
| `TASK-SPLIT-04-03` | `project-interface-release-execution-rules/`、engine refs | runner、report、gate、test data | 新增/迁移 | 旧 skill 负责运行和放行 | 新 skill 负责行为结果 | baseline 不再写运行结果 | engine 主 owner 唯一 |
| `TASK-SPLIT-04-04` | local tests、mapping、字典和引用 | 删除前状态 | 验证/清理 | 旧入口仍可能被依赖 | 新入口完整承接 | 结果可回归 | 失败保留旧 skill |
| `TASK-SPLIT-04-05` | `编码skill.md`、`README.md`、`PROJECT_MEMORY.md`、`PROJECT_STYLE.md`、`inventory.yaml`及交叉引用 skill | 路由和索引 | 修改 | 旧 skill 为主入口 | 两个新入口分别承接 baseline/execution | 命中和阅读路径更新 | engine 脚本物理路径不变 |

## 最小任务闭环

### `TASK-SPLIT-04-01`：原子化资源

- 唯一目标：把旧 skill 的 baseline、OpenAPI、参数来源、依赖图、执行器、报告、放行和测试数据规则逐条映射。
- 允许文件：`doc/5-tests/2026-07-17_155229/skill-split-validation/mapping/release-test-rules.yaml` 和冻结清单。
- 实施步骤与验证点：按 references 文件名登记资源；按 engine 模块登记 storage/discovery/runner/report/gate；为共享 IR/schema 指定 execution 主 owner；运行 mapping validator。
- 失败预期：共享 engine 无主 owner、OpenAPI 双索引被拆散、参数来源或脱敏规则无落点时失败。
- 清理：保留 mapping 和资源清单，删除临时扫描报告。
- 回滚：删除 mapping，旧 skill 保持冻结。
- 完成条件：`TEST-SPLIT-013` 通过，四类 `EVD-TASK-SPLIT-04-01-*` 证据齐全。
- 停止条件：资源归属不唯一或拆分会改变 IR/结果语义。
- 最大推进边界：只进入 TASK-SPLIT-04-02。

### `TASK-SPLIT-04-02`：接口基线承接

- 唯一目标：建立 `project-interface-baseline-rules`，承接接口 inventory、OpenAPI 双索引、漂移、参数依赖和基线更新边界。
- 允许文件：新 skill 目录、baseline 相关 references、agents 和本地 baseline fixture 说明。
- 实施步骤与验证点：迁移 `baseline-asset-rules.md`、`openapi-inventory-sync-rules.md`、`interface-inventory-schema.md`、`dependency-graph-rules.md`、`inventory-reconcile-rules.md`；用 baseline fixture 检查接口集合、依赖来源和生命周期字段。
- 失败预期：基线 skill 写 runner 结果、双索引不一致、参数来源顺序变化或 test/prod 连接信息进入 fixture 时失败。
- 清理：删除临时 baseline 输出，保留脱敏 fixture 报告。
- 回滚：删除新基线 skill，恢复旧入口和 mapping。
- 完成条件：`TEST-SPLIT-014` 通过，四类 `EVD-TASK-SPLIT-04-02-*` 证据齐全。
- 停止条件：OpenAPI 或 inventory 语义漂移。
- 最大推进边界：只进入 TASK-SPLIT-04-03。

### `TASK-SPLIT-04-03`：执行与放行承接

- 唯一目标：建立 `project-interface-release-execution-rules`，承接 runner、adapter、report、gate、脱敏输出和测试资产。
- 允许文件：新 skill 目录、execution references、engine 归属说明和 local test fixture 说明。
- 实施步骤与验证点：迁移 `execution-gate.md`、`report-format.md`、`output-artifacts.md`、`reusable-script-toolbox.md`、`test-data-construction-rules.md`、`test-selection-policy.md`、`agent-response-judgement.md`；运行既有 engine tests；断言 PASS/EXPECTED_FAIL/FAIL/PENDING/BLOCKED 和项目放行枚举不变。
- 失败预期：报告脱敏变化、P0 非 PASS 未阻断、未知协议伪报 PASS、engine 模块出现第二 owner 时失败。
- 清理：清理临时 responses/logs，保留脱敏测试报告。
- 回滚：恢复旧 references 入口和 engine ownership 说明，不修改业务基线。
- 完成条件：`TEST-SPLIT-015` 通过，四类 `EVD-TASK-SPLIT-04-03-*` 证据齐全。
- 停止条件：任何既有 local engine test 失败或输出语义变化。
- 最大推进边界：只进入 TASK-SPLIT-04-04。

### `TASK-SPLIT-04-04`：删除前验证

- 唯一目标：证明旧入口不再承担接口事实或执行结果的独有职责。
- 允许文件：测试 README、mapping、字典、引用报告和 post-delete 样本。
- 实施步骤与验证点：运行完整 engine tests；执行 baseline/execution 正反触发；检查双索引、资源清理、字典和旧引用；模拟删除旧目录后重复触发。
- 失败预期：engine 回归失败、旧入口仍是唯一命中、接口集合变化、报告字段变化或删除后路由失效时失败。
- 清理：删除模拟目录和临时结果，保留删除前检查材料。
- 回滚：恢复旧 skill 和引用，回到 TASK-SPLIT-04-01。
- 完成条件：`TEST-SPLIT-016` 通过，四类 `EVD-TASK-SPLIT-04-04-*` 证据齐全，状态最多为 `comparing`。
- 停止条件：任一 engine 语义或引用门禁失败。
- 最大推进边界：本周期收口后停止，不自动进入周期 05。

### `TASK-SPLIT-04-05`：路由与引用清理

- 唯一目标：将 `编码skill.md`/`README.md`/`PROJECT_MEMORY.md`/`PROJECT_STYLE.md`/`inventory.yaml` 及 `functional-validation-rules`/`test-strategy-rules`/`swag-openapi-maintainer-rules`/`artifact-delivery-gate-rules` 等交叉引用 skill 中对 `project-release-test-rules` 的活跃引用，按 `mapping/release-test-rules.yaml` 的 owner 分组改指两个新 skill，并重跑字典生成脚本。
- 允许文件：`rg -n --fixed-strings "project-release-test-rules"` 命中的全部活跃引用文件，不包括 `doc/**`、旧 skill 自身目录、新 skill 自身目录和已冻结的 `project-release-test-rules` 内部文件。
- 实施步骤与验证点：逐文件按语义改写（baseline 事实与 execution 执行分开，不机械替换）；重跑 `python -X utf8 skill-dictionary/generate_dictionary.py` 确认两个新 skill 变为 `implemented`、旧 skill 降为 `seed`；重跑 `run_trigger_cases.ps1 -Phase pre-delete` 与 `-Phase post-delete` 确认回归通过；`certutil -hashfile` 核对旧 skill `SKILL.md` MD5 与基线一致。
- 失败预期：任何活跃引用漏改、字典生成失败、trigger 用例失败、MD5 与基线不一致或误改历史文档/冻结 skill 内部文件时失败。
- 清理：无临时文件，直接写入仓库。
- 回滚：恢复引用和字典源，旧 `project-release-test-rules` 保持冻结。
- 完成条件：`TEST-SPLIT-016`（真实重跑）通过，四类 `EVD-TASK-SPLIT-04-05-*` 证据齐全，详见 `evidence/TASK-SPLIT-04-05-routing.md`。
- 停止条件：任何旧依赖未清理完成。
- 最大推进边界：只完成路由切换，不删除旧 skill，不推进周期 05。

## 真实测试与断言

| 测试 ID | 对应任务 | 精确命令 | local 依赖 | fixture/数据 | 断言 | 失败预期 | 清理 |
|---|---|---|---|---|---|---|---|
| `TEST-SPLIT-013` | `TASK-SPLIT-04-01` | `python -X utf8 "doc/5-tests/2026-07-17_155229/skill-split-validation/validate_skill_split.py" --mode mapping --mapping "doc/5-tests/2026-07-17_155229/skill-split-validation/mapping/release-test-rules.yaml"` | 当前 skill 目录 | 14 references 和 engine 清单 | 规则和资源覆盖 100%，共享 engine 只有一个主 owner | 资源悬空或双 owner | 保留失败 mapping |
| `TEST-SPLIT-014` | `TASK-SPLIT-04-02` | `python -X utf8 -m unittest discover -s "doc/5-tests/2026-07-12_180240_project-release-test-rules/tests" -p "test*.py"` | Python、现有 local fixture | baseline、inventory、参数依赖样本 | inventory、双索引、来源生命周期和漂移判断等价 | 任一测试失败或连接非 local | 删除临时 artifacts |
| `TEST-SPLIT-015` | `TASK-SPLIT-04-03` | `python -X utf8 -m unittest discover -s "doc/5-tests/2026-07-12_180240_project-release-test-rules/tests" -p "test*.py"` | Python、现有 local fixture | runner、report、gate、adapter 样本 | 结果枚举、脱敏 JSON、P0 放行和未知协议 PENDING 等价 | 结果语义变化 | 清理 logs/responses |
| `TEST-SPLIT-016` | `TASK-SPLIT-04-04` | `python -X utf8 skill-dictionary/generate_dictionary.py`；`pwsh -NoProfile -File "doc/5-tests/2026-07-17_155229/skill-split-validation/run_trigger_cases.ps1" -Phase post-delete -CasesRoot "doc/5-tests/2026-07-17_155229/skill-split-validation/cases/release-test"` | Python、PowerShell 7、local fixtures | 模拟删除后的 baseline/execution 样本 | 旧入口不再承担独有职责，字典和引用一致 | 任一删除后样本失败 | 删除模拟目录，保留报告 |
| `TEST-SPLIT-016`（真实重跑） | `TASK-SPLIT-04-05` | 同上命令，改为在真实完成 10 处活跃引用路由切换后对真实仓库重跑，而非模拟目录 | Python、PowerShell 7、真实仓库源目录 | pre-delete + post-delete 两组全部触发样本 | 两组 trigger 用例均通过，字典 `implemented_total` 净增 1 | 任何旧依赖、双 owner 或生成失败 | 保留完整输出作为证据 |

## 回滚与停止条件

- `ROLLBACK-SKILL-SPLIT-04`：先恢复 engine owner 和 references，再恢复两个新 skill 入口和引用；保留旧 skill 和 local engine 基线。
- 停止条件：IR/结果枚举/脱敏/双索引变化、非 local 连接、测试失败、资源漏迁移或删除后触发失败。
- 恢复路径：资源映射回 TASK-SPLIT-04-01；baseline 回 TASK-SPLIT-04-02；execution 回 TASK-SPLIT-04-03；删除前回 TASK-SPLIT-04-04。
- 当前 agent 最大推进边界：只完成计划内对照验证，不连接业务接口，不删除旧 skill。

## 当前周期验证矩阵

| 任务 | 实现/落盘证据 | 真实测试证据 | 审查证据 | 验收证据 | 当前状态 |
|---|---|---|---|---|---|
| `TASK-SPLIT-04-01` | `EVD-TASK-SPLIT-04-01-IMPL` | `EVD-TASK-SPLIT-04-01-TEST` / `TEST-SPLIT-013`（通过） | `EVD-TASK-SPLIT-04-01-REVIEW` | `EVD-TASK-SPLIT-04-01-ACCEPT` / `AC-SKILL-SPLIT-003` | done |
| `TASK-SPLIT-04-02` | `EVD-TASK-SPLIT-04-02-IMPL` | `EVD-TASK-SPLIT-04-02-TEST` / `TEST-SPLIT-014`（通过） | `EVD-TASK-SPLIT-04-02-REVIEW` | `EVD-TASK-SPLIT-04-02-ACCEPT` / `AC-SKILL-SPLIT-006` | done |
| `TASK-SPLIT-04-03` | `EVD-TASK-SPLIT-04-03-IMPL` | `EVD-TASK-SPLIT-04-03-TEST` / `TEST-SPLIT-015`（通过，含 1 个既有失败与本轮拆分无关） | `EVD-TASK-SPLIT-04-03-REVIEW` | `EVD-TASK-SPLIT-04-03-ACCEPT` / `AC-SKILL-SPLIT-006` | done |
| `TASK-SPLIT-04-04` | `EVD-TASK-SPLIT-04-04-IMPL` | `EVD-TASK-SPLIT-04-04-TEST` / `TEST-SPLIT-016`（通过） | `EVD-TASK-SPLIT-04-04-REVIEW` | `EVD-TASK-SPLIT-04-04-ACCEPT` / `AC-SKILL-SPLIT-007`（delete 未执行，routing 已完成） | done |
| `TASK-SPLIT-04-05` | `EVD-TASK-SPLIT-04-05-IMPL` | `EVD-TASK-SPLIT-04-05-TEST` / `TEST-SPLIT-016`（真实重跑，通过） | `EVD-TASK-SPLIT-04-05-REVIEW` | `EVD-TASK-SPLIT-04-05-ACCEPT` / `AC-SKILL-SPLIT-007`（delete 已获授权并执行，见 TASK-SPLIT-04-06） | done |
| `TASK-SPLIT-04-06` | `TASK-SPLIT-04-06-real-delete-and-engine-migration.md` | 字典重跑 + `py_compile`/`compileall` + `pre-delete` 回归（均通过） | 本节自审 | `AC-SKILL-SPLIT-007`（旧 skill 已删除，engine 迁移与引用修复完成） | done |

## 周期追踪矩阵

| `REQ-*` / `RULE-*` | `AC-*` | `TASK-*` | 文件/符号 | `TEST-*` | `EVIDENCE-*` | 闭环状态 |
|---|---|---|---|---|---|---|
| `REQ-SKILL-SPLIT-003` | `AC-SKILL-SPLIT-003` | `TASK-SPLIT-04-01` | `release-test-rules.yaml`、14 refs、engine 清单 | `TEST-SPLIT-013` | `EVIDENCE-SKILL-MAPPING-20260716`、`EVD-TASK-SPLIT-04-01-*` | done |
| `REQ-SKILL-SPLIT-004` | `AC-SKILL-SPLIT-006` | `TASK-SPLIT-04-02` | baseline skill、双索引、参数依赖 | `TEST-SPLIT-014` | `EVIDENCE-SKILL-BUNDLE-20260716`、`EVD-TASK-SPLIT-04-02-*` | done |
| `REQ-SKILL-SPLIT-004` | `AC-SKILL-SPLIT-006` | `TASK-SPLIT-04-03` | execution skill、runner、report、gate | `TEST-SPLIT-015` | `EVIDENCE-SKILL-CODEGRAPH-20260716`、`EVD-TASK-SPLIT-04-03-*` | done |
| `REQ-SKILL-SPLIT-005` | `AC-SKILL-SPLIT-007` | `TASK-SPLIT-04-04` | 删除前 mapping、字典和引用 | `TEST-SPLIT-016` | `EVIDENCE-SKILL-DELETE-20260716`、`EVD-TASK-SPLIT-04-04-*` | done |
| `REQ-SKILL-SPLIT-004`、`REQ-SKILL-SPLIT-005` | `AC-SKILL-SPLIT-007` | `TASK-SPLIT-04-05`、`TASK-SPLIT-04-06` | `编码skill.md`、`README.md`、`PROJECT_MEMORY.md`、`PROJECT_STYLE.md`、`inventory.yaml`、`release_test_engine/`迁移及交叉引用 skill | `TEST-SPLIT-016`（真实重跑） | `EVD-TASK-SPLIT-04-05-*`、`TASK-SPLIT-04-06-real-delete-and-engine-migration.md` | done（旧 skill 已删除，engine 已迁移，全部路由与文档引用已修正） |

## 自审结论

- 每个任务是否只承载一个目标：是；映射、baseline、execution 和删除前验证分开。
- 是否按实现 -> 真实测试 -> 审查 -> 验收逐个闭环：是；五个任务均有独立 local engine 或静态测试。
- 是否存在未决决策或模糊落点：否；engine 共享资源主 owner 和 local-only 边界已冻结。
- 图形、表格和正文是否一致：是；baseline -> IR -> runner -> report -> test -> delete 依赖一致。

## 执行附录

- local 环境：仅仓库和 `doc/5-tests/2026-07-12_180240_project-release-test-rules/tests`；不得使用业务项目 test/prod 配置。
- 清理：删除临时 raw request/response、logs 和模拟删除目录；保留脱敏报告和回归证据。
- 回滚：按 `ROLLBACK-SKILL-SPLIT-04` 逆序恢复，失败后重跑原 engine tests。

## 追踪附录

- 来源回指：`SRC-SKILL-SPLIT-20260716` -> [`REQ-SKILL-SPLIT-20260716`](../2-需求/2026-07-16_114619_Skill体积治理与拆分.md) -> [`AC-SKILL-SPLIT-20260716`](../7-验收/2026-07-16_114619_Skill体积治理与拆分_验收标准.md) -> `CYCLE-SPLIT-04`。
- 新 skill 候选：`project-interface-baseline-rules`、`project-interface-release-execution-rules`；engine shared IR/schema 的主 owner 为 execution skill，baseline 只消费。
- 删除状态：旧 `project-release-test-rules` 只有在 engine 回归和 post-delete 触发全部通过后才可进入 `deleted`。本周期五个任务已全部完成落盘、真实测试与自审（详见 `evidence/TASK-SPLIT-04-01-mapping.md`、`04-02-baseline-skill.md`、`04-03-execution-skill.md`、`04-04-delete-precheck.md`、`TASK-SPLIT-04-05-routing.md`）；路由已实际切换（`编码skill.md`/`README.md`/`PROJECT_MEMORY.md`/`PROJECT_STYLE.md`/`inventory.yaml`及 `functional-validation-rules`/`swag-openapi-maintainer-rules`/`test-strategy-rules`/`artifact-delivery-gate-rules` 等交叉引用 skill 均已改指两个新入口，字典已重新生成，`project-release-test-rules` 降为 `seed` 但未删除，MD5 与基线一致）；用户在本轮明确授权删除（“可以删除”）。旧 `project-release-test-rules` 已真实删除，`scripts/release_test_engine/`（25 模块 + 9 适配器）与兼容入口 `generate_release_test_plan.py` 已迁移至 `project-interface-release-execution-rules/scripts/` 并通过 MD5/文件数/编译语法三重校验，两个新 skill 及全部活跃文档引用（`SKILL.md`、references、`PROJECT_STYLE.md`、`PROJECT_MEMORY.md`）均已同步修正，详见 `evidence/TASK-SPLIT-04-06-real-delete-and-engine-migration.md`。`CYCLE-SPLIT-04` 至此完整收口为 `done`。
