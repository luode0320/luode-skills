---
template_version: "v1.0"
schema_version: 1
doc_id: "CYCLEDOC-BLK-001"
doc_type: "implementation_cycle"
source_ids: ["REQDOC-BLK-001", "ACDOC-BLK-001", "PLAN-BLK-001"]
status: "in_progress"
version: "v1.0"
current_slice: "CYCLE-BLK-001"
updated_at: "2026-07-14 01:13:25"
complexity: "L2"
baseline_commit: "a89f812"
reader_level: "business_general"
writing_style: "plain_chinese"
appendix_policy: "preserve_existing_or_one_terminal_appendix"
unresolved_decisions: []
review_acceptance_gates: []
---

# 任务阻断收口与恢复计划实施周期 01

结论：本周期先建立可被所有阶段消费的阻断契约和校验器。影响：缺失恢复计划的正式阻断文档不能放行。范围：共享 reference、文档门禁和测试。非范围：最终回复与业务恢复逻辑。变化：完成共享契约和文档校验两个最小任务。完成标准：对应真实测试通过。术语说明：阻断记录是可去重、可恢复的任务事实。验证状态：实施中。

## 当前代码/文档基线

基线为 `a89f812`。当前没有共享的任务阻断收口契约；部分 Skill 仅说明阻断原因或泛化后续内容。图片资产决策：N/A；原因：本周期只产生规则和 Python 校验；证据：无视觉验收。

## 当前周期目标、边界与进入条件

目标：创建共享 `BLK-*` 契约并让文档 validator 能识别真实阻断。进入条件：REQDOC-BLK-001、ACDOC-BLK-001、PLAN-BLK-001 已存在。边界：不修改最终总结、运行时、审查或验收 producer 的业务规则。

## 周期内最小任务执行顺序

图形目的：说明 TASK-BLK-001 与 TASK-BLK-002 的串行闭环。
关联 ID：TASK-BLK-001、TASK-BLK-002、TEST-BLK-004。

```mermaid
flowchart LR
  A[TASK-BLK-001 契约] --> B[TASK-BLK-002 Validator]
  B --> C[TEST-BLK-004]
  C --> D[审查与验收周期收口]
```

| 顺序 | 任务 | 文件/符号操作契约 | 完成条件 |
|---|---|---|---|
| 1 | TASK-BLK-001 | 新增 `references/task-blocker-closure-contract.md`；更新共享门禁引用。 | 9 字段、状态边界、去重键和重入点明确。 |
| 2 | TASK-BLK-002 | 更新 profile、`validate_engineering_docs.py` 与 tests。 | blocked 文档缺字段失败，完整和受限文档通过。 |

## 最小任务闭环

每项完成后立即运行 `python -m unittest artifact-delivery-gate-rules/tests/test_validate_engineering_docs.py`；失败时停止，不推进下一个任务。审查：检查文档状态触发条件只覆盖 `blocked/manual_handoff`。验收：AC-BLK-004 与 AC-BLK-005 均通过。

## 文件/符号操作契约

| 文件 | 操作 | 回滚 |
|---|---|---|
| `task-blocker-closure-contract.md` | 定义 `BLK-*` 字段和渲染/生产边界。 | 删除新增文件并撤回引用。 |
| `validate_engineering_docs.py` | 增加受控触发及稳定错误码。 | 恢复原校验分支。 |
| `test_validate_engineering_docs.py` | 增加正负例。 | 删除本周期测试。 |

## 当前周期验证矩阵

| TEST | 样本 | 断言 | 失败预期 |
|---|---|---|---|
| TEST-BLK-004 | `status: blocked` 缺字段文档 | `blocker.closure_missing` | 校验退出为 BLOCKED。 |
| TEST-BLK-004 | 完整阻断、limited、not_applicable fixture | 正确通过或阻断 | limited 不要求收口。 |

## 周期阻断、停止与回滚

停止条件：字段不能被文档与最终回复共同消费、或受限状态误报。回滚：仅撤回本周期新增 reference、validator 分支和测试。N/A；原因：无数据迁移、API 和外部环境，改动为本地规则资产；证据：文件列表。

## 周期追踪矩阵

| TASK | REQ/AC | TEST | 证据 |
|---|---|---|---|
| TASK-BLK-001 | REQ-BLK-002, AC-BLK-002 | TEST-BLK-002 | 共享契约文件 |
| TASK-BLK-002 | REQ-BLK-004, AC-BLK-004, AC-BLK-005 | TEST-BLK-004 | validator 单测 |

## 自审结论

本周期目标单一、文件职责不重叠、每项均有真实测试和停止边界。
