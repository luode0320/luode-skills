# TASK-SPLIT-06-03 生产交接 skill 建立证据

- 新建 skill：`game-asset-production-handoff-rules/`（生产交接组），目录总大小 40,850B（`SKILL.md` 12,568B + 7 个 `references/*.md` + 3 个 `scripts/*.py` + `agents/openai.yaml`），来源为 `2d-asset-design/SKILL.md`（基线 29,096B，MD5 `FB4E06AC8891F17FAB00CAE5D6462FE3`）按 `mapping/2d-asset-rules.yaml` 中标注 `owner=group_production` 与 `owner=shared_duplicate` 的规则条目重述后的拆分产物；旧 `2d-asset-design/` 目录未做任何改动（保持冻结基线）。
- `SKILL.md`：12,568B，MD5 `C75F924B1E1ECF8611F83B94832A99ED`，低于预算矩阵 `normal_max_bytes=16000`。内容迁移自原文核心原则第 5 条"面向 Godot 交付"、执行流程第 4/5/6 步（生成原始素材/执行后处理/交付Godot可用结果）、资产类型约束中"角色怪物Boss"“投射物命中特效法术效果”“地图瓦片场景道具”三个子节、默认交付规范整节、额外强约束整节、输出模式后三项（生成方案/后处理方案/Godot交付物）；设计阶段专属的参考筛选、Tier 判定、视觉基线锁定、素材 brief 撰写与预览确认闭环判定为 `group_design`，未纳入本 skill，仅在文中以指向 `game-asset-design-gate-rules` 的引用形式衔接。
- `references/` 迁移清单（7 个文件，按 mapping 标注 `owner=group_production` 或 `owner=shared_duplicate`）：`image-generation-workflow.md`(1,515B)、`character-animation-production-gate.md`(992B)、`map-strategies.md`(860B)、`layered-map-contract.md`(1,399B)、`prop-pack-contract.md`(2,544B)、`postprocess-workflow.md`(1,723B)、`asset-modes.md`(1,747B，`shared_duplicate`，与 `game-asset-design-gate-rules` 各自保留一份，内容一致）。
- `scripts/` 原样迁移（3 个文件，未修改任何语义或参数契约）：`postprocess_sprite_sheet.py`(13,323B)、`make_layout_guide.py`(2,179B)、`compose_layered_preview.py`(1,355B)。
- `agents/openai.yaml`：沿用被拆分源文件 `interface:` 包裹 schema，聚焦"生产交接"职责：真实生成、动作/地图/道具生产、后处理、Godot 交付，明确设计未确认前应转交 `game-asset-design-gate-rules`。
- `TEST-SPLIT-023` 真实脚本执行验证（离线 fixture-only，未调用任何真实图像生成模型或外部服务）：
  1. 环境准备：默认 `python`（`hermes-agent` venv，3.11.15）缺少 `pip` 模块，导致 `postprocess_sprite_sheet.py` 因 `import numpy` 失败；已用同一命令对旧 `2d-asset-design/scripts/postprocess_sprite_sheet.py` 复验，确认是拆分前就存在的环境缺口而非本次拆分引入。执行 `python -m ensurepip` 补齐 `pip`（安装 `pip-24.0`/`setuptools-79.0.1`），再执行 `python -m pip install numpy`（安装 `numpy-2.4.6`），环境补齐后两份脚本均可正常导入。
  2. `python -X utf8 game-asset-production-handoff-rules/scripts/postprocess_sprite_sheet.py --help`、`sprite --help`、`prop-pack --help`，以及 `make_layout_guide.py --help`、`compose_layered_preview.py --help` 全部 `exit=0`，参数契约与原脚本一致。
  3. 用 PIL 在 `doc/5-tests/2026-07-17_155229/skill-split-validation/cases/2d-production-fixture/` 下生成一张 128x128、洋红底色、2x2 网格、每格一个纯色圆形主体的离线测试 sprite sheet（`test-sheet.png`，872B），模拟"已确认设计后的原始素材图"，不使用任何真实 imagegen 调用。
  4. `postprocess_sprite_sheet.py sprite --input test-sheet.png --output-dir sprite-out --rows 2 --cols 2 --anchor bottom --chroma-key ff00ff --gif` → `exit=0`，产出 `frame_000~003.png`（4 帧全部 `non_empty_frames`，`qa.json` 记录 `frame_count=4`、`edge_touch_frames=[]`）、`sheet_aligned.png`(657B)、`preview.gif`(713B)、`qa.json`(1,877B)，验证洋红去背、切帧、脚底对齐、重新拼表、GIF 导出、QA 元数据全链路真实可用。
  5. `make_layout_guide.py --rows 2 --cols 2 --cell-width 64 --cell-height 64 --output layout-guide.png` → `exit=0`，产出 `layout-guide.png`(431B)。
  6. `compose_layered_preview.py --base layout-guide.png --layout layout.json --output layered-preview.png`（`layout.json` 引用上一步 `frame_000.png`/`frame_001.png` 两个 prop 及偏移坐标）→ `exit=0`，产出 `layered-preview.png`(848B)，验证分层预览合成脚本可正确读取 base 图与 prop 图并按坐标叠加。
  7. 全部测试产物保留在 `doc/5-tests/2026-07-17_155229/skill-split-validation/cases/2d-production-fixture/` 下作为证据，不涉及 `screenshot-cleanup` 类强制清理规则（该规则仅约束浏览器截图），未产生任何需要清理的敏感或临时占位内容。
- 结论：PASS。生产交接 skill 结构、体积、三个脚本的真实端到端执行（含此前缺失的 `numpy` 依赖修复）均已验证；旧 `2d-asset-design` 目录未删除、未改动；`agents/openai.yaml` 已按源文件 schema 补齐。
