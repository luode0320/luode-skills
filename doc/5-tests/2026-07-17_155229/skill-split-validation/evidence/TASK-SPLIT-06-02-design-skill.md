# TASK-SPLIT-06-02 设计闸门 skill 建立证据

- 新建 skill：`game-asset-design-gate-rules/`（设计确认闸门组），目录总大小 42,281B（`SKILL.md` + 7 个 `references/*.md` + `agents/openai.yaml`），来源为 `2d-asset-design/SKILL.md`（基线 29,096B，MD5 `FB4E06AC8891F17FAB00CAE5D6462FE3`，与实施周期06计划文档记录一致）按 `mapping/2d-asset-rules.yaml` 中标注 `owner=group_design` 与 `owner=shared_duplicate` 的规则条目重述后的拆分产物；旧 `2d-asset-design/` 目录未做任何改动（保持冻结基线）。
- `SKILL.md` 体积核验：写入即为 16,712B，MD5 `0040F51CC0D5956B3125E2544ED3427B`，低于预算矩阵（`mapping/candidate-matrix.yaml`）的 `normal_max_bytes=16000` 附近、远低于 `review_max_bytes=20000` 与 `hard_warning_bytes=24000`，不需要像 CYCLE-SPLIT-05 核心组那样做二次去重瘦身；核心原则、执行流程中的"设计确认"步骤、Tier 0-3 质量阶梯、参考图筛选与原创性核验、项目视觉基线核对、预览确认闭环、素材 brief 与 image spec 交接口径均已迁移，生产阶段专属的真实生成、后处理脚本、Godot 交付规范整段判定 `group_production` 未纳入本 skill。
- `references/` 迁移清单（7 个文件，按 mapping 标注 `owner=group_design` 或 `owner=shared_duplicate`）：`design-preview-confirmation-gate.md`(2,618B)、`art-direction-quality-gate.md`(5,711B)、`image-spec-contract.md`(2,285B)、`prompt-rules.md`(5,822B)、`reference-only-policy.md`(2,423B)、`project-style-consistency-contract.md`(4,361B)、`asset-modes.md`(1,747B，`shared_duplicate`，`game-asset-production-handoff-rules` 后续会复制同一份用于后处理默认锚点判断，两份内容一致、无需再拆分)。
- `agents/openai.yaml`：沿用被拆分源文件 `2d-asset-design/agents/openai.yaml` 的 `interface:` 包裹 schema（而非 `project-interface-baseline-rules` 一类新版扁平结构），`display_name`/`short_description`/`default_prompt` 聚焦"设计确认闸门"职责：参考筛选与原创性核验、Tier 0-3 质量阶梯判定、预览确认闭环、产出素材 brief 后交给 `game-asset-production-handoff-rules`。
- `TEST-SPLIT-022` 离线触发验证（fixture-only，未调用任何真实模型或外部服务）：
  1. 新建 `doc/5-tests/2026-07-17_155229/skill-split-validation/cases/2d-design/trigger_cases.json`（12 条：6 条 `pre-delete` + 6 条 `post-delete`，覆盖"设计闸门单独命中"“生产交接单独命中”“全流程两者都命中”“无关任务两者都不命中”“外部素材直接使用询问命中设计闸门”“已生成素材仅需后处理命中生产交接”六类场景）与 `delete_cases.json`（1 条 `pre-delete` + 1 条 `post-delete`，`old_skill=2d-asset-design`，`new_skills=[game-asset-design-gate-rules, game-asset-production-handoff-rules]`，`mapping_status=frozen`）。
  2. 执行 `run_trigger_cases.ps1 -Phase pre-delete -RepoRoot "D:\luode\luode-skills" -CasesRoot ".../cases/2d-design"`，`mode=pre-delete` 全部 7 条样本（6 条 trigger + 1 条 delete）判定 `[通过]`，脚本最终输出 `[PASS]` 且明确 `[DONE] No real skill directory was deleted or modified`。
  3. `post-delete` 阶段留待 TASK-SPLIT-06-04（`game-asset-production-handoff-rules` 建立完成后）与 pre-delete 阶段一并复跑，当前 fixture 数据已预先构造好两阶段样本，避免重复建目录。
- 结论：PASS。设计确认闸门 skill 结构、体积、triggers fixture 均已验证；旧 `2d-asset-design` 目录未删除、未改动；`agents/openai.yaml` 已按源文件 schema 补齐。
