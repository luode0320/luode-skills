# TASK-SPLIT-08-01 实施规划职责复评证据

- 复评对象：`implementation-planning-rules/SKILL.md`（39,147B）+ 12 个 `references/*.md`（含 `plan-structure-template.md` 17,774B、`plan-output-gate.md` 10,420B、`plan-review-checklist.md` 8,363B、`implementation-overview-template.md` 7,524B、`task-granularity-and-order.md` 7,573B、`implementation-cycle-template.md` 6,457B、`minimum-task-execution-contract.md` 5,100B、`full-sequence-master-plan.md` 3,640B、`plan-entry-checklist.md` 2,816B、`visualization-standard.md` 2,302B、`plan-boundaries-and-examples.md` 1,655B、`source-notes.md` 547B）。体积证据：SKILL.md 39,147B 超过预算矩阵 `hard_warning_bytes=24000` 达 15,147B（约 63%），体积压力真实存在，量级接近已拆分的 `agent-browser`（36,228B），与 CYCLE-SPLIT-07 的 `mcp-installation-rules`（仅超 `normal_max` 880B）性质不同。
- 候选职责组：按计划文档指定的四个复评对象划分——`GROUP-AUTHORING`（单来源对象计划正文撰写，核心组）、`GROUP-PLANMODE`（Plan Mode 第一层路由外壳）、`GROUP-CYCLE-TASK`（实施周期/最小任务契约）、`GROUP-MASTERPLAN`（全量顺序总表），落盘于 [mapping/planning-route-matrix.yaml](/D:/luode/luode-skills/doc/5-tests/2026-07-17_155229/skill-split-validation/mapping/planning-route-matrix.yaml)。
- 路由样本：10 条，全部锚定 SKILL.md 自身"Skill 作用与适用场景"/"进入后先做什么"一节已写明的真实触发句式，未额外发明场景。
- 数字门槛表面结果：候选组数量 4（达标，门槛 ≥2）；平均命中数 13/10=1.3（达标，门槛 ≤3）。
- 独立性代理指标（本轮复评新增，非源计划文档预先给定数字，已在矩阵文件 `independence_proxy` 字段中如实标注来源）：以 `GROUP-AUTHORING` 为核心组，检验 `GROUP-PLANMODE`/`GROUP-CYCLE-TASK`/`GROUP-MASTERPLAN` 三个非核心组是否存在"脱离核心组单独命中"的真实样本——实测 `observed_value=0/3`：全部 10 条样本中，只要命中任一非核心组，必然同时命中 `GROUP-AUTHORING`；反之 `GROUP-AUTHORING` 可以脱离全部三个非核心组独立命中（`PLAN-ROUTE-003`/`004`/`009` 三条样本）。这证明三个非核心组只是核心组下的条件子模式（Plan Mode 路由后必然回流到本 skill 自己的入口检查；周期/任务契约只是"极致详细"要求下的补充模板；全量顺序总表命中后 SKILL.md 明确要求"再回到单个来源对象的实施总览"），不构成真正独立的用户意图。
- 架构风险：本仓库 `AGENTS.md` 已把 `GROUP-PLANMODE` 的路由职责硬编码为"Plan Mode 硬闸门"，若按候选组拆成多个 `auto-trigger` skill，三个非核心新入口在真实使用中仍会与承载 `GROUP-AUTHORING` 的入口同时触发，平均并发加载不会下降，反而增加路由跳转和 `AGENTS.md` 多处路由规则的同步改动风险，与本项目"降低单份文档体积、避免截断"的治理目标相悖。
- `TEST-SPLIT-026` 真实执行：`python -X utf8 doc/5-tests/2026-07-17_155229/skill-split-validation/validate_skill_split.py --mode route-matrix --mapping doc/5-tests/2026-07-17_155229/skill-split-validation/mapping/planning-route-matrix.yaml` → 通过，输出 `4 个候选组、10 条样本、平均命中 1.3、共同命中占比 0.5，结论 decision=no_split`；校验器对矩阵声明的 `skill_md_bytes`（39147）与磁盘实测字节数做了交叉核对，并重新从 `samples` 逐条重算命中统计，与矩阵自报告的 `metrics` 字段逐项比对一致才算通过（复用 CYCLE-SPLIT-07 建立的通用 `route-matrix` 校验模式，未修改校验器逻辑）。
- 结论：`decision=no_split`。体积问题真实存在但"按候选组拆成独立 auto-trigger skill"这一具体整改形状不成立，不代表体积问题已解决；更合适的后续路径很可能是"SKILL.md 内部去重压缩"（参照 CYCLE-SPLIT-05 对 `browser-session-automation-rules` 的处理方式），但该整改形状与本项目现有"按独立职责拆分"范式不同，需要新建来源对象、验收标准和独立实施总览才能授权推进，不在本周期或本项目现有实施总览范围内直接展开。理由与下次复评触发条件均已写入 `mapping/planning-route-matrix.yaml` 的 `conclusion` 字段。
- 边界确认：本任务未修改 `implementation-planning-rules` 任何文件（SKILL.md 与 12 个 references 均未改动），未创建新 skill 目录，未进入编码；仅新增 `mapping/planning-route-matrix.yaml` 一份复评产物，复用 CYCLE-SPLIT-07 已建立的 `route-matrix` 校验模式，未再次修改共享校验器脚本。
