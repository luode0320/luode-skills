---
schema_version: 1
template_version: 1
doc_id: "TEST-DVT-20260818-01"
doc_type: "test"
source_ids:
  - "REQ-DVT-20260818-001"
  - "CYCLE-DVT-01"
status: accepted
version: "v1.0"
current_slice: "TASK-DVT-06"
updated_at: "2026-08-18 14:45:00"
reader_level: business_general
writing_style: plain_chinese
appendix_policy: preserve_existing_or_one_terminal_appendix
---

# 目录树多版本改造 · 去除 rpc：测试主文档

结论：本轮真实测试覆盖三份工程文档机器校验、`placement_catalog.py` 去 rpc 与版本级条目断言、`micro_business.py` 脚手架/隔离/候选新项目判定断言、以及全量 Python 测试，全部相关断言通过；影响：本改造可继续推进收口；范围：两个 skill 的机器事实源、脚本与规则文档，以及落盘的工程文档；非范围：不判断业务正确性，不修改其他兄弟 skill；变化：catalog 删 `business-rpc`、`router`/`controller`/`entity`/`service` 改版本级（`<source-root>/<domain>/<artifact>/<v?>`）；`micro_business.py` 新增四类版本化骨架与 `--detect-new`、移除 `--with-rpc` 与域级 crontask；完成标准：文档机器校验通过、脚本断言通过、全量测试中与本次改动无关的既有基线问题已记录；术语说明：测试主文档指每轮真实测试的扁平 md 说明；验证状态：本次相关断言全部通过，全量测试仍有 4 个既有失败与 12 个既有错误（均为环境/历史基线问题，与本次改动无关）。

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 关联任务 | `TASK-DVT-01` 至 `TASK-DVT-06` |
| 关联周期 | `CYCLE-DVT-01` |
| 测试时点 | 实现完成后、6-review 前 |

## 测试对象与断言

| 测试对象 | 真实测试命令 | 断言 / 通过标准 | 结果 |
| --- | --- | --- | --- |
| 需求文档 | `validate_engineering_docs.py --profile requirement --doc doc/2-需求/2026-08-18_目录树多版本改造_去除rpc.md` | `valid: true` | PASS |
| 实施总览 | `validate_engineering_docs.py --profile implementation_overview --doc doc/3-实施/2026-08-18_目录树多版本改造_去除rpc_实施总览.md` | `valid: true` | PASS |
| 实施周期 | `validate_engineering_docs.py --profile implementation_cycle --doc doc/3-实施/2026-08-18_目录树多版本改造_去除rpc_实施周期01_版本化与去rpc收口.md` | `valid: true` | PASS |
| Catalog 去 rpc | `placement_catalog.py query --artifact business-rpc` | 非 0 退出码 | PASS |
| Catalog 版本级 | `placement_catalog.py query --artifact router` / `--artifact controller` / `--artifact entity` / `--artifact service` | 返回版本级条目（`<source-root>/<domain>/<artifact>/<v?>`） | PASS |
| 脚手架 | `micro_business.py scaffold order --root <tmp>` | 产出 `internal/order/{entity,router,controller,service}/v1` + 域级 api/base/constant/util + `internal/order/init.go`，无 crontask | PASS |
| 跨域隔离 | `micro_business.py check --root <tmp>` | 跨域 import 报违规退出非 0 | PASS |
| 候选新项目判定 | `micro_business.py check --detect-new --root <tmp>` | candidate_new / guard / skip 三态 | PASS |
| 脚本编译 | `py_compile` 编译 `placement_catalog.py` 与 `micro_business.py` | 无异常 | PASS |
| 全量 Python 测试 | `python -B test/run_python_tests.py` | `Ran 386 tests`；failures/errors 与本次改动无关 | PASS（见下） |

## 全量测试既有基线问题记录

`python -B test/run_python_tests.py` 结果为 `Ran 386 tests`，`FAILED (failures=4, errors=12, skipped=2)`。逐项核对均与本次目录树改造无关：

- 4 个失败：`bootstrap_agents_test`（找不到 Python 解释器，环境）、`knowledge_citation_contract_test`（`引用台账` 计数 4 != 3，历史基线）、`asset_location_test`（`doc/5-tests/` 历史指纹基线）、`go_blackbox_layout_test`（未找到 Go，环境）。
- 12 个错误：`validate_engineering_docs_test`（历史 fixture 缺失）、`browser_use_cloud_preflight_test`（环境变量超长）、`supervisor_state_test` ×5（`Could not determine home directory`，环境）、`pre_commit_gate_test` ×4（`系统找不到指定的文件`，环境）、`plan_output_contract_test`（`stderr` 为 None 的测试自身 bug）。
- `N/A + 原因 + 证据`：无与本次目录树改造相关的失败项；原因是所有失败/错误均为环境缺依赖或历史基线指纹，证据是逐项 traceback 定位到与本改造无关的 skill 测试与环境错误。

## 完成标准

- 三份工程文档机器校验全部 `valid: true`；
- `business-rpc` 查询失败关闭、`router`/`controller`/`entity`/`service` 返回版本级条目；
- `scaffold` 产出四类版本化骨架（`entity/v1`、`router/v1`、`controller/v1`、`service/v1`）+ 单文件 `init.go`、`check` 拒绝跨域、`--detect-new` 三态判定可用；
- 全量测试中与本次改动无关的既有失败已逐项记录。

图片资产决策：N/A + 原因 + 证据：本轮是脚本、规则文档与工程文档的测试，没有 UI、截图、原型或位图需要视觉留证。
