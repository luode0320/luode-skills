# EVD-RT-C15-01-IMPL

- 结论：完成原生 RFC 6455 握手、鉴权头、收发、顺序、关闭和重连运行时。
- 代码：`transports/websocket.py` 和 `scenario_runner.py` 的 `ws.*` 动作分派。
- 生命周期：连接对象只保存在单次场景运行时，步骤输出不包含连接或原始鉴权对象。
- 故障：关闭码、超时和重复消息被归一化为稳定场景 FAIL。
- 依赖：使用隔离工具环境的 `websockets==15.0.1`，被测项目依赖零变更。
