---
schema_version: 1
template_version: 1
doc_id: "TEST-PSCTL-20260814-01"
doc_type: "test"
source_ids:
  - "REQ-PSCTL-20260814-001"
  - "CYCLE-PSCTL-01"
status: accepted
version: "v1.0"
current_slice: "TASK-PSCTL-07"
updated_at: "2026-08-14 22:43:33"
reader_level: business_general
writing_style: plain_chinese
appendix_policy: preserve_existing_or_one_terminal_appendix
---

# PowerShell 控制继续优化：测试主文档

结论：本轮真实测试覆盖需求、实施总览、实施周期三份文档的机器校验，以及标准调用前缀在 reference 和三份 SKILL.md 中的引用断言，全部通过；影响：本轮规则文档可继续推进收口；范围：PowerShell 三 skill 的文档修改与文档证据；非范围：不判断 PowerShell 脚本运行时的业务正确性，也不修改脚本实现；变化：reference 新增 5.1 / 7 双轨调用前缀，三份 SKILL.md 补齐交叉引用；完成标准：文档机器校验通过、前缀引用断言通过、全量测试中与本次改动无关的既有基线问题已记录；术语说明：测试主文档指每轮真实测试的扁平 md 说明；验证状态：本次相关断言全部通过，全量测试仍有 4 个既有失败与 1 个既有错误（与本次改动无关）。

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 关联任务 | `TASK-PSCTL-01` 至 `TASK-PSCTL-07` |
| 关联真实测试 | `TEST-PSCTL-01` 至 `TEST-PSCTL-07` |
| 检查时点 | 全部实现完成后、6-review 前 |
| 检查环境 | Windows PowerShell / Python 3 |

## 测试范围

本轮真实测试覆盖以下内容：

| 任务 | 落点 | 测试方式 |
| --- | --- | --- |
| `TASK-PSCTL-01` | 需求、实施总览、实施周期文档 | `validate_engineering_docs.py` 机器校验 |
| `TASK-PSCTL-02` | `wsl-windows-bridge/references/powershell-fallback-patterns.md` | `Select-String` 检索 5.1 / 7 前缀 |
| `TASK-PSCTL-03` | `wsl-windows-bridge/SKILL.md` | `Select-String` 检索引用 |
| `TASK-PSCTL-04` | `windows-encoding-rules/SKILL.md` | `Select-String` 检索引用 |
| `TASK-PSCTL-05` | `windows-powershell-environment-rules/SKILL.md` | `Select-String` 检索引用 |
| `TASK-PSCTL-06` | `PROJECT_MEMORY.md`、`skill-dictionary/data.js`、`字典.md` | 文件读取与字典脚本 |
| `TASK-PSCTL-07` | 全量测试、测试主文档、6-review | 全量测试与文档校验 |

## 真实测试结果

### 文档机器校验

执行命令：

```powershell
python -B artifact-delivery-gate-rules/scripts/validate_engineering_docs.py --profile requirement --doc "doc/2-需求/2026-08-14_223800_PowerShell控制继续优化.md" --root F:\luode-skills
python -B artifact-delivery-gate-rules/scripts/validate_engineering_docs.py --profile implementation_overview --doc "doc/3-实施/2026-08-14_223800_PowerShell控制继续优化_实施总览.md" --root F:\luode-skills
python -B artifact-delivery-gate-rules/scripts/validate_engineering_docs.py --profile implementation_cycle --doc "doc/3-实施/2026-08-14_223800_PowerShell控制继续优化_实施周期01_标准调用前缀与三skill衔接.md" --root F:\luode-skills
```

结果：三份文档均返回 `valid: true`、`status: PASS`、退出码 0。

### 前缀与引用断言

执行命令：

```powershell
Select-String -LiteralPath "wsl-windows-bridge/references/powershell-fallback-patterns.md" -Pattern 'powershell.exe -NoProfile -ExecutionPolicy Bypass -Command' -SimpleMatch -Quiet
Select-String -LiteralPath "wsl-windows-bridge/references/powershell-fallback-patterns.md" -Pattern 'pwsh -NoLogo -NoProfile -ExecutionPolicy Bypass -Command' -SimpleMatch -Quiet
Select-String -LiteralPath "wsl-windows-bridge/SKILL.md" -Pattern 'powershell-fallback-patterns.md' -SimpleMatch -Quiet
Select-String -LiteralPath "windows-encoding-rules/SKILL.md" -Pattern 'powershell-fallback-patterns.md' -SimpleMatch -Quiet
Select-String -LiteralPath "windows-powershell-environment-rules/SKILL.md" -Pattern 'powershell-fallback-patterns.md' -SimpleMatch -Quiet
```

结果：五个断言全部命中。

### 字典刷新

执行命令：

```powershell
python skill-dictionary/generate_dictionary.py
```

结果：退出码 0，`data.js` 与 `字典.md` 同步刷新，`implemented_total: 69`、`planned_missing: 2`、`seed_total: 35`。

### 全量 Python 测试

执行命令：

```powershell
python -B test/run_python_tests.py
```

结果：`Ran 386 tests`，`FAILED (failures=4, errors=1, skipped=1)`。失败项均与本次 PowerShell 规则改动无关：

| 测试 | 失败原因 | 是否本次改动 |
| --- | --- | --- |
| `validate_engineering_docs_test.test_strict_trace_report_fields` | fixture 引用不存在的历史需求文档 `2026-07-12_033322_需求与实施文档极致完备化.md` | 否 |
| `plan_output_contract_test.test_wait_loop_behavior_model_passes` | 引用不存在的历史测试脚本 `doc/5-tests/2026-07-26_040607/plan_mode_wait_loop/test_plan_mode_wait_loop.py` | 否 |
| `path_prefix_contract_test.test_actual_dirs_are_within_declared_list` | 知识库存在未登记主题目录 `skill-governance` | 否 |
| `asset_eol_health_test.test_lf_enforced_files_have_no_crlf` | 仓库存在既有 CRLF 文件 | 否 |
| `asset_location_test.test_repository_legacy_manifest_matches_current_history` | 历史可执行资产基线漂移 | 否 |

## 测试结论

本轮与 PowerShell 优化直接相关的断言全部通过：三份工程文档机器校验通过，5.1 / 7 前缀模板与三份 SKILL.md 引用全部命中，字典刷新成功。全量测试的 5 项既有问题已定位为历史基线/知识库/CRLF 问题，不阻塞本轮规则文档收口。

## 通过标准

- 需求、实施总览、实施周期文档通过对应 profile 校验；
- `powershell-fallback-patterns.md` 同时包含 5.1 与 7 前缀；
- 三份 SKILL.md 均引用 `powershell-fallback-patterns.md`；
- 字典刷新退出码 0；
- 测试主文档与 6-review 文档落盘后可被后续会话读取。

## 图片资产决策

图片资产决策：N/A + 原因 + 证据：本轮是规则文档与测试收口，没有 UI、截图、原型或位图需要视觉留证。

## 执行附录

### 执行环境

- Windows PowerShell
- Python 3（`python` 命令可用）

### 精确命令

- 文档校验：`python -B artifact-delivery-gate-rules/scripts/validate_engineering_docs.py --profile <profile> --doc <路径> --root F:\luode-skills`
- 引用断言：`Select-String -LiteralPath <路径> -Pattern <模式> -SimpleMatch -Quiet`
- 字典刷新：`python skill-dictionary/generate_dictionary.py`
- 全量测试：`python -B test/run_python_tests.py`

### 清理

无临时文件需要清理。

## 追踪附录

- 来源：`REQ-PSCTL-20260814-001`
- 周期：`CYCLE-PSCTL-01`
- 任务：`TASK-PSCTL-01` 至 `TASK-PSCTL-07`
- 证据：`EVD-TASK-PSCTL-01-TEST` 至 `EVD-TASK-PSCTL-07-TEST`
