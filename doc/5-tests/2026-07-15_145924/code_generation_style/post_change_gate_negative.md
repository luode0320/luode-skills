# TEST-08：写后风格闸门负例驳回

## 结论

PASS。写后闸门包含可检查的风格跳变、接口未对照和多余代码驳回条件，同时明确正确性、安全、兼容、错误处理和接口契约要求的必要代码不能被风格规则删除。

## 输入样本

正例：新增代码与既有注册区段完全同构，只替换业务对象名称。

负例：

```go
func registerXX() { /* 新增 helper */ }
var exchangeName = "xx" // 新增临时变量
for _, item := range items { /* 改成循环 */ }
log.Infof("register xx") // 新增额外日志
if err := validateXX(); err != nil { /* 无需求校验 */ }
```

接口负例：未查找既有实现，或方法顺序、构造/注册方式、错误处理、日志和注释明显跳变。

必要代码边界：需求、正确性、安全、兼容、错误处理或接口契约明确要求的代码，即使超出局部模板，也必须保留并在契约中写明原因。

## 命中规则

- `RULE-05`、`RULE-06`：局部一致性、接口参考和最小新增内容。
- `DEC-08`：风格一致性不得删除正确性、安全、兼容、错误处理或接口契约必需代码。
- `TASK-02-05`：写后闸门正反例检查。

## 通过标准

- 正例通过。
- helper、临时变量、循环、额外日志、无需求校验和陌生抽象等负例逐项驳回。
- 接口实现未对照或结构明显跳变时驳回。
- 必要硬性代码不会因风格一致被误删。

## 实际结果

```text
TEST-08 PASS: 风格跳变/多余代码驳回且保留必要硬性代码
```

## 清理与回滚

负例仅为文档中的受控样本，未执行生产代码；无运行时数据需要清理。若闸门不能形成明确正反例判定，回滚 `post-change-style-gate.md` 的本轮新增条目并回到 `TASK-02-05`。

## 追踪

`SRC-STYLE-FB-04 -> REQ-07/REQ-08/RULE-05/RULE-06/DEC-08 -> AC-08 -> CYCLE-02 -> TASK-02-05 -> TEST-08 -> EVD-02-05-NEGATIVE-GATE`
