# TEST-07：接口实现风格对照正例

## 结论

PASS。规则资产要求按同文件、同目录/模块、同语言项目样例的顺序查找接口实现，契约记录接口名称、参考文件/符号、沿用结构和必要差异；无参考实现时必须记录 `N/A + 原因 + 证据`。

## 输入样本

- 已存在接口：`Starter`。
- 目标：新增一个实现 `Starter` 的类型。
- 参考实现样本：`same/module/existing_starter.go:ExistingStarter.Start`。

## 命中规则

- `RULE-06`：接口实现必须记录参考实现；无参考时明确降级依据。
- `DEC-07`：参考顺序为同文件、同目录/模块、同语言项目样例；冲突时采用最近且稳定的多数写法。
- `TASK-02-04`：把参考选择和必要差异写入风格契约。

## 契约预期字段

```text
接口参考实现: Starter -> same/module/existing_starter.go:ExistingStarter.Start
沿用内容: 接收者形式、方法顺序、构造方式、错误处理、日志、注释和测试组织
参考差异: 仅替换目标类型和当前需求必需参数
无参考实现: N/A + 原因 + 证据
```

## 实际结果

```text
TEST-07 PASS: 接口实现记录参考实现并支持无参考降级
```

## 失败预期

- 未查找既有实现就直接套用个人或外部模板：驳回。
- 候选实现存在无法按最近同模块和稳定多数收敛的冲突：记录 `GAP-STYLE-INTERFACE-001` 并停止。
- 无参考实现但未记录降级依据：驳回。

## 清理与回滚

样本和契约字段仅用于本地规则断言，未写入业务代码、数据库或外部服务；无数据清理。若规则未通过，回滚范围为 `TASK-02-04` 对应 priority 和 contract reference 新增段。

## 追踪

`SRC-STYLE-FB-04 -> REQ-08/RULE-06 -> AC-07 -> CYCLE-02 -> TASK-02-04 -> TEST-07 -> EVD-02-04-INTERFACE-STYLE`
