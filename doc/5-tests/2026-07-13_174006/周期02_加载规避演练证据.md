# 周期02 写码前加载与规避 演练证据

- 对应周期：`doc/3-实施/2026-07-13_174006_代码风格体系反馈驱动持续迭代_实施周期02_加载与规避.md`
- 覆盖测试：TEST-03（写码前加载反例库并规避）
- 覆盖任务：TASK-02-01、TASK-02-02

## EVD-TASK-02-01-IMPL / EVD-TASK-02-01-TEST：写码前加载

- 改动文件：`code-generation-style-rules/SKILL.md`、`references/pre-coding-checklist.md`。
- 脚本级校验（bash grep）：
  - SKILL.md 中「全局用户风格反例库 / active 反例 / user-style-feedback-library」相关引用计数为 7。
  - pre-coding-checklist.md 中「反例库」计数为 2（必读来源第 4 步 + 判断问题）。
- 结论：写码前必读来源已包含加载全局反例库 active 条目。

## EVD-TASK-02-02-IMPL / EVD-TASK-02-02-TEST：禁用写法纳入与规避

- 改动文件：`references/style-contract-template.md`、`code-generation-style-rules/SKILL.md`。
- 脚本级校验：契约模板含「用户反例规避」字段计数为 2（字段表 + 最小输出示例）。
- 行为演练（模拟写一段 Go 常量代码）：
  - 写码前加载反例库，命中 active 条目 STYLE-CASE-GO-002（去重键 go|常量枚举|iota）。
  - 本轮契约「禁用写法」并入该反例；「用户反例规避」字段登记 STYLE-CASE-GO-002。
  - 产出代码采用显式常量赋值 `StatusInit = 0` 等，而非 `iota`，与该反例的正例一致。
  - 若产出误用 `iota`，写后闸门（post-change-style-gate 联动）命中反例并驳回，按正例改写。

## EVD-TASK-02-02-REVIEW：审查

- 加载步骤位于优先级裁决之前，不破坏既有五级优先级（用户要求 > 当前文件 > 同目录 > PROJECT_STYLE > 通用 skill）。
- consistency-rules 作为反例库唯一 owner 的边界在 generation-rules「与相邻 Skill 的边界」已声明。

## EVD-TASK-02-02-ACCEPT：周期02验收

- 契约来源含反例库、禁用写法含 active 反例、演练产出正例改写（符合 AC-03 通过标准）。
- 周期02收口条件满足。
