# 周期01 捕获与确认写入 演练证据

- 对应周期：`doc/3-实施/2026-07-13_174006_代码风格体系反馈驱动持续迭代_实施周期01_捕获与确认写入.md`
- 覆盖测试：TEST-01（捕获与 candidate 回显）、TEST-02（active 写入与去重）
- 覆盖任务：TASK-01-01、TASK-01-02、TASK-01-03

## EVD-TASK-01-01-IMPL / EVD-TASK-01-01-TEST：模板与库骨架

- 新增文件：`code-style-consistency-rules/references/style-case-template.md`、`user-style-feedback-library.md`、`style-feedback-workflow.md`。
- 指纹校验（bash）：三文件 `md5sum` 与 `wc -c` 均成功，含「去重键」字段，首行中文标题无乱码。
  - style-case-template.md：md5 75be5508f090627786b4280b9a4c317f，2714 字节。
  - user-style-feedback-library.md：md5 已随后续写入变化，初始 1818 字节。
  - style-feedback-workflow.md：md5 a1c48d5fbfbde55ff2e16c3826e6ca05，2882 字节。

## EVD-TASK-01-03-TEST：反馈捕获到写入 active 端到端演练

- 演练输入（模拟用户文字反馈）：「Go 常量不要用 iota，要显式写出每个常量值」。
- 步骤 1 定位被否定写法：`const ( StatusInit = iota; ... )`，场景为常量与枚举。
- 步骤 2 提取正例：用户期望显式赋值 `StatusInit = 0` 等。
- 步骤 3 去重键：`go|常量枚举|iota`；比对库中不存在该键，判定为新增。
- 步骤 4 组装 candidate：按 `style-case-template.md` 生成 status=candidate 条目并回显。
- 步骤 5 确认：模拟用户确认。
- 步骤 6 写入 active：条目改 status=active 写入 `user-style-feedback-library.md`，记为 STYLE-CASE-GO-002。
- 结果校验（bash grep）：库中 `status: active` 计数为 2；条目 id 为 STYLE-CASE-GO-001、STYLE-CASE-GO-002；两条去重键各出现 1 次，唯一。

## EVD-TASK-01-03-TEST：去重机制验证

- 去重预期：若再次输入同语义「不要用 iota」反馈，去重键 `go|常量枚举|iota` 命中已有条目，按流程只更新出现次数与确认时间，不新增正文。
- 当前库中 `go|常量枚举|iota` 仅一条，符合「同键不新增」预期。

## EVD-TASK-01-03-REVIEW：审查

- consistency SKILL.md description 已追加反馈学习语义（系统已加载新 description）。
- 新增「## 用户风格反馈捕获与学习」节，references 读取规则补 3 份新文件。
- 触发词「这样写不行 / 不能这么写 / 这个风格不对」在 SKILL.md 中命中计数为 4。

## EVD-TASK-01-03-ACCEPT：周期01验收

- 库中出现格式合规 active 条目，去重键唯一，重复反馈同键只增计数（符合 AC-01、AC-02、AC-04 通过标准）。
- 周期01收口条件满足。
