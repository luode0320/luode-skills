---
schema_version: 1
doc_id: "TEST-OBS-KB-README-001"
doc_type: "test"
source_ids: ["REQ-OBS-ITERATIVE-KB-001", "IMP-OBS-KB-001", "CYCLE-OBS-22-001", "CYCLE-OBS-23-001"]
status: accepted
version: "v1.0"
current_slice: "CYCLE-22 与 CYCLE-23"
updated_at: "2026-08-05"
template_version: 1
reader_level: business_general
writing_style: plain_chinese
appendix_policy: preserve_existing_or_one_terminal_appendix
---

# 知识库可迭代更新 测试证据

结论：两期七项测试全部通过，桥接八个新命令实机可用，三档处置规则有断言锁定，巡检脚本对真实知识库出报告且零写入。影响：证明知识库已从只能增量补充变为可迭代更新，且每次写操作都有回读验证。范围：桥接参数校验单元测试、巡检脚本测试、八命令实机验证、分级处置实机演练、字典刷新与六档文档校验。非范围：根测试目录的全量启动器，它在本轮开始前就已存在既有故障。变化：新增两个测试文件共 40 个用例；实机验证覆盖只读五命令与写操作三命令的完整链路。完成标准：单元测试全绿，实机链路每步返回成功且回读通过，巡检前后知识库文件总数与孤儿数不变。术语说明：`回读验证` 指写入后再读一次以证明确实写成功；`零写入` 指巡检执行前后知识库内容与结构完全未变。验证状态：全部通过，无受限项。

## 文档信息

| 项目 | 内容 |
|---|---|
| 文档编号 | `TEST-OBS-KB-README-001` |
| 所属需求 | `REQ-OBS-ITERATIVE-KB-001` |
| 所属周期 | `CYCLE-OBS-22-001`、`CYCLE-OBS-23-001` |
| 覆盖任务 | `T22-01` 至 `T22-03`、`T23-01` 至 `T23-04` |
| 执行环境 | Windows Git Bash 与仓库 Python 3.11；实机部分使用 Obsidian 1.13.4（安装版 1.12.7）与固定知识库 `D:\obsidian_data` |
| 关联风格回归 | `STYLE-OBS-ITERATIVE-KB-001` |

## 图片资产决策

图片资产决策：N/A + 原因：本次测试只产生命令行文本与 JSON 输出，不包含界面、截图或视觉验收对象 + 证据：`doc/data/images/` 下无本任务图片引用。

## 测试资产落点

| 资产 | 路径 | 说明 |
|---|---|---|
| 桥接参数校验测试 | `test/obsidian-knowledge-flow/bridge_request_contract_test.py` | 29 个用例，覆盖只读组、写操作组、文档组、分级组、检测组 |
| 巡检脚本测试 | `test/obsidian-knowledge-flow/vault_audit_contract_test.py` | 11 个用例，覆盖只读约束与候选判定纯函数 |
| 本目录 | `doc/5-tests/2026-08-05_164800_知识库可迭代更新/` | 只保存本 README 与执行结论，不含任何可执行资产 |

## 通过标准

| 项目 | 内容 |
|---|---|
| 单元测试 | 两个文件共 40 个用例全部通过，0 failed、0 error |
| 实机只读 | 五个只读命令均返回成功且已验证，枚举返回的中文路径可回传并命中同一笔记 |
| 实机写入 | 写属性、移动、删除三类操作每步返回成功且回读通过；执行案例路径被拒绝 |
| 巡检零写入 | 巡检前后知识库文件总数 65 与孤儿数 41 完全不变 |
| 字典刷新 | 退出码为零，缺项数与基线一致，新技能说明已入库 |
| 文档门禁 | 六份研发文档各自档位均判定通过 |
| 环境阻断 | N/A + 原因：本次测试不需要数据库、缓存或外部服务 + 证据：单元测试只读仓库文本，实机验证只使用本地固定知识库 |

## 执行结论

| 测试 ID | 入口 | 结果 | 关键数字 |
|---|---|---|---|
| `TEST-OBS-KB-DOC` | 文档完整性校验器 requirement、implementation_overview、implementation_cycle 两份 | 通过 | 四档均 `valid=True`、`error_codes=[]` |
| `TEST-OBS-KB-001` | 桥接测试只读组，加五条桥接只读实机命令 | 通过 | 单测 6 项全绿；五条实机命令均 `ok=true`、`verified=true` |
| `TEST-OBS-KB-002` | 桥接测试写操作组，加一次性笔记实机链路 | 通过 | 单测 7 项全绿；实机 14 步链路全部成功 |
| `TEST-OBS-KB-003` | 桥接测试文档组 | 通过 | 3 项全绿；命令模板与白名单 16 项逐项一致 |
| `TEST-OBS-KB-004` | 桥接测试分级组 | 通过 | 7 项全绿；旧禁令已消失 |
| `TEST-OBS-KB-005` | 桥接测试检测组 | 通过 | 6 项全绿；四态判定与固定根目录条款逐字未变 |
| `TEST-OBS-KB-006` | 巡检脚本测试，加真实知识库实跑 | 通过 | 单测 11 项全绿；实跑 64 秒，出四类候选，前后 65/41 不变 |
| `TEST-OBS-KB-007` | 字典生成脚本，加六档文档校验，加全量单元测试 | 通过 | 字典退出码 0、`implemented_total 69`、`planned_missing 2` 与基线一致；六档校验全部通过；60 项断言全绿 |

## 八命令实机验证细节

| 命令 | 参数形态 | 实机结果 |
|---|---|---|
| 读单个属性 | `path=` + `name=` | 返回 `active`、`superseded` 与中文双链值，与写入值逐字一致 |
| 读整篇属性 | `path=` + `format=json` | 返回完整 frontmatter JSON，中文标题零乱码 |
| 查反向链接 | `path=` + `total` | 返回引用计数，用作三档处置前置证据 |
| 枚举文件 | `folder=` 可选 | 返回路径列表；中文路径可直接回传并命中 |
| 枚举孤儿 | 无路径参数 | 返回 41 篇孤儿清单 |
| 写属性 | `path=` + `name=` + `value=` + `type=` | 返回 `Set <名>: <值>`；其余 frontmatter 字段、顺序与中文全部保留；列表类型可写中文双链 |
| 移动 | `path=` + `to=` | 返回 `Moved: <旧> -> <新>`；目标目录缺失时失败但原文件无损；`to=` 可给完整路径或仅给目录 |
| 删除 | `path=` | 返回 `Moved to trash: <路径>`；已在 Windows 回收站中枚举确认可恢复 |

## 分级处置实机演练

本轮用真实场景验证了「标记取代」档：上一周期沉淀的 `官方CLI回显编码限制.md` 把桥接自身的编码缺陷误判为官方命令行的固有限制，本轮实测推翻该归因。

| 步骤 | 动作 | 结果 |
|---|---|---|
| 1 | `backlinks` 前置检查 | 引用数 0，当前状态为当前有效 |
| 2 | 内容复核 | 归因错误但踩坑过程有参考价值，判定为「标记取代」档而非删除 |
| 3 | `create` 新笔记记录真实根因与分层取证方法 | 成功且已验证 |
| 4 | 旧笔记 `property-set status=superseded` | 成功且回读一致 |
| 5 | 旧笔记 `property-set superseded_by --type list` | 成功且回读一致 |
| 6 | 新笔记 `property-set supersedes --type list` | 成功且回读一致 |
| 7 | 双向关系与状态互斥复核 | 旧笔记为已取代且指向新笔记，新笔记为当前有效且指向旧笔记 |

## 巡检首次真实报告

| 候选类别 | 数量 | 说明 |
|---|---|---|
| 已巡检笔记 | 35 篇 | 长期知识目录下，已排除执行案例目录 |
| 同主题候选冲突组 | 8 组 | 只是线索，需读过内容才能判定是补充、矛盾还是取代 |
| 状态非当前有效 | 2 篇 | 含本轮刚标记为已取代的那篇 |
| 孤儿笔记 | 39 篇 | 无任何笔记链接过去 |
| 标了已取代但缺接替者 | 0 篇 | 双向关系目前无断链 |
| 缺少 frontmatter | 5 篇 | 拿不到状态字段，无法参与状态治理，需先补齐头部 |

巡检性能：初版逐篇同时查属性与反向链接耗时 163 秒；改为用全库孤儿清单推导零引用后降至 64 秒，调用数减半，未触及五分钟降级阈值。

## 既有失败项说明

根测试启动器与另外两个既有测试在本轮开始前就已失败，与本轮改动无关，上一周期已用干净基线复跑证明：

| 项目 | 现象 | 判定 |
|---|---|---|
| `test/run_python_tests.py` | 在既有目录上抛 `ImportError: Start directory is not importable` | 既有故障，已开出独立后台任务处理 |
| `test/artifact-delivery-gate-rules/validate_engineering_docs_test.py` | 历史 fixture 指向 `../7-验收/` 的链接已失效 | 既有故障，不在本轮范围 |
| `test/test-asset-governance/asset_location_test.py` | `doc/5-tests/` 历史可执行资产指纹与基线清单不一致 | 既有故障，本轮未改动该目录下任何可执行资产 |

## 清理与提交状态

| 项目 | 内容 |
|---|---|
| 临时文件 | 参数探测、写链路验证与巡检报告使用的临时文件已全部删除，`D:\tmp` 下无残留 |
| 知识库状态 | 实机测试笔记已随链路删除并复原为 65 篇；本轮新增一篇知识笔记并把一篇旧笔记标记为已取代，属于真实治理结果 |
| 回收站 | 测试笔记与已删除条目保留在 Windows 回收站，是否清空由用户决定 |
| 工作树 | 停在已改动未提交状态，本轮未执行任何写入版本历史的动作 |
