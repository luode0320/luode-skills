# 上线测试基线复用方案当前改动总审查

审查结论: 通过
审查范围: project-release-test-rules/SKILL.md；project-release-test-rules/references/；project-release-test-rules/scripts/generate_release_test_plan.py；PROJECT_MEMORY.md；PROJECT_STYLE.md；skill-dictionary/data.js；字典.md
是否允许提交: 是
阻断问题: 无

## 审查摘要

- 本轮将上线测试规则从一次性接口测试扩展为项目级长期基线资产机制，覆盖接口清单、参数来源、依赖图、可复用参数、场景目录、脚本适配和执行历史。
- 参数构造已统一为 `reusable_param -> upstream_api -> local_database -> local_cache -> fixture -> rule`，并要求依赖追踪与阻断分类，避免 provider 失败被误判为 consumer 接口失败。
- 通用脚本优先复用现有 `generate_release_test_plan.py` 总入口，新增基线初始化、依赖图构建、可复用参数校验、参数解析和基线回写子命令。

## 风险核对

- 未发现明文密钥、token、手机号、身份证号、银行卡号或非 local 环境连接信息写入基线模板。
- 未发现把业务项目专属字段硬编码进通用脚本；项目差异由 `script-adapter.yaml` 和参数来源文件承接。
- 未发现接口明细报告继续要求 test / 预发环境；报告格式已改为 local 本地环境。

## 验证证据

- `generate_release_test_plan.py --help` 通过，新增子命令可发现。
- `init-baseline-assets` 临时目录验证通过，生成 9 个基线资产文件。
- `validate-reusable-params` 临时样例验证通过。
- `build-dependency-graph --validate` 临时样例验证通过。
- `resolve-test-data` 临时样例验证通过，生成 resolved params 与 dependency trace。
- `update-baseline-assets` 临时事件验证通过，完成可复用参数和执行历史回写。
- `.system/skill-creator/scripts/quick_validate.py project-release-test-rules` 通过。
- `skill-dictionary/generate_dictionary.py` 通过并刷新字典。
- `git diff --check` 通过；仅有 CRLF 将被 Git 规范化的提示。
