# PowerShell 默认 UTF-8 当前改动审查

审查结论: 通过
审查范围: AGENTS.md、PROJECT_MEMORY.md、PROJECT_STYLE.md、README.md、project-agents-bootstrap/SKILL.md、project-agents-bootstrap/scripts/bootstrap_agents.sh、skill-dictionary/data.js、windows-encoding-rules/SKILL.md、windows-encoding-rules/scripts/enable_powershell_utf8.ps1、windows-wsl-execution-rules/SKILL.md、windows-wsl-execution-rules/references/command-templates.md、windows-wsl-execution-rules/references/recommended-workflow.md、字典.md
是否允许提交: 是
阻断问题: 无

## 审查背景

- 本轮剩余改动集中在 Windows 默认 shell 策略收敛：只有 PowerShell 经过用户级 UTF-8 永久化后，才允许把它作为 Windows 默认 shell；普通命令留在 PowerShell，执行类命令继续走 `wsl.exe --cd` 进入 WSL。
- 同主题变更已经同步写入仓库级 `AGENTS.md`、长期上下文文档、自举 skill、自举脚本、`windows-encoding-rules`、`windows-wsl-execution-rules` 和字典产物，并新增了可重复执行的永久化脚本。

## 发现

- 未发现 P0 / P1 阻断问题。
- `windows-encoding-rules/scripts/enable_powershell_utf8.ps1` 同时覆盖 Windows PowerShell 5.1 与 PowerShell 7 的当前用户 profile，能够写入 UTF-8 初始化片段，并在脚本尾部通过新子进程校验 `InputEncoding`、`OutputEncoding`、`$OutputEncoding` 与 `chcp` 是否全部切到 UTF-8。
- `windows-wsl-execution-rules` 与 `AGENTS.md` 口径已经从“Windows 默认 Git Bash”切换为“先完成 PowerShell UTF-8 永久化，再让 PowerShell 承接普通命令；执行类命令再进 WSL”，没有留下旧口径冲突。
- `README.md` 末行改动日志已经与本次提交标题 `docs: [PowerShell默认UTF-8] 收敛Windows默认shell与WSL执行前提` 保持一致。

## 已覆盖

- Diff 范围: `git diff --name-only` 当前输出的 12 个文件
- 已读规则: `AGENTS.md`、`git-collaboration-rules/SKILL.md`、`project-change-review-rules/SKILL.md`
- 已执行命令: `git status --short`、`git diff --name-only`、`git diff --stat`、`Get-Content windows-encoding-rules/scripts/enable_powershell_utf8.ps1`

## 剩余风险

- 本轮为规则 / 脚本 / 文档改动，未在当前线程内重复执行新脚本做真实环境验证；该脚本本体已包含新子进程 UTF-8 验证逻辑，足以作为提交门禁依据。
