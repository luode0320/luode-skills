# UTF-8 文件写入规则实现自审与当前改动总审查

## 统一判定

- 审查结论: 通过
- 审查范围: UTF-8 文件写入规则相关的仓库规则、skill、bootstrap 脚本、长期记忆/风格与 skill 字典生成产物
- 是否允许提交: 是
- 阻断问题: 无

## 发现

未发现阻断项:

- 未发现 P0/P1 问题。
- 未发现旧 `## 中文编码规则` 章节名或旧 `sync_section "中文编码规则"` 同步目标残留。
- 未发现 UTF-8 规则只覆盖 PowerShell 终端而遗漏代码、文档、配置、脚本、测试资产或生成文本写入的情况。

## 检查范围

- `AGENTS.md`
- `PROJECT_MEMORY.md`
- `PROJECT_STYLE.md`
- `project-agents-bootstrap/SKILL.md`
- `project-agents-bootstrap/scripts/bootstrap_agents.sh`
- `windows-encoding-rules/SKILL.md`
- `windows-wsl-execution-rules/SKILL.md`
- `vercel-react-best-practices/AGENTS.md`
- `skill-dictionary/data.js`
- `字典.md`

说明: `windows-wsl-execution-rules/references/command-templates.md`、`windows-wsl-execution-rules/references/path-mapping.md`、`windows-wsl-execution-rules/references/recommended-workflow.md` 在当前工作区已有未提交改动，属于既有 WSL 路径显示规则调整，本次 UTF-8 审查未把它们认定为本轮新增规则落点。

## 已覆盖

- Diff 范围: 当前工作区可见 diff 与相关未跟踪审查/测试资产边界。
- 已读规则: `AGENTS.md`、`skill-hit-check-rules`、`parallel-task-dispatch-rules`、`project-agents-bootstrap`、`windows-encoding-rules`、`windows-wsl-execution-rules`、`implementation-review-rules`、`project-change-review-rules`、`artifact-delivery-gate-rules`。
- 已联动 skill: `skill-compliance-gate-rules`、`skill-evolution-rules`、`skill-audit-rules`、`project-memory-rules`、`project-style-rules`。
- 已执行命令:
  - `codegraph sync`
  - `bash project-agents-bootstrap/scripts/bootstrap_agents.sh --repo .`
  - `python skill-dictionary/generate_dictionary.py`
  - `rg -n '## 中文编码规则|sync_section ".*中文编码规则' ...`
  - `rg -n '文件编码与写入规则|GBK|ANSI|系统默认编码|编辑器默认编码|显式指定 UTF-8' ...`
  - `git diff --check`
  - `git status --short`
  - `git diff --stat`
  - `git diff --name-only`

## 实现自审

通过项:

- 规则落点完整: 已覆盖仓库总规则、自举 skill、自举脚本、Windows 编码 skill、Windows/WSL 执行 skill、长期记忆和长期风格。
- 触发条件更新完整: `windows-encoding-rules` 已从终端乱码扩展为任意代码、文档、配置、脚本、测试资产和生成文本写入场景；`windows-wsl-execution-rules` 已明确执行层选择不改变 UTF-8 文件写入口径。
- 自举同步完整: `project-agents-bootstrap/scripts/bootstrap_agents.sh` 已把受管章节同步目标改为 `文件编码与写入规则`，并通过 bootstrap 脚本同步到根规则与已有子规则文件。
- 字典同步完整: 已重新运行 `skill-dictionary/generate_dictionary.py`，刷新 `skill-dictionary/data.js` 与 `字典.md`。
- 注释与编码: 本轮为规则/文档/脚本文本修改，无函数位点；新增中文规则文本按 UTF-8 回读可读。

未通过项:

- 无。

受影响运行路径清单:

| 路径 / 入口 | 触发方式 | 预期验证方式 | 当前状态 |
| --- | --- | --- | --- |
| 仓库规则加载 | 新会话或当前仓库任务读取 `AGENTS.md` | 搜索新章节与旧章节残留 | 已静态验证 |
| 自举同步脚本 | `bash project-agents-bootstrap/scripts/bootstrap_agents.sh --repo .` | 脚本运行并检查受管章节同步结果 | 已脚本验证 |
| skill 字典生成 | `python skill-dictionary/generate_dictionary.py` | 生成脚本完成并刷新产物 | 已脚本验证 |
| Windows/WSL 编码规则触发 | 命中相关文件写入或 WSL 执行边界任务 | 搜索 description 与正文 UTF-8 约束 | 已静态验证 |

## 总审查结论

当前 UTF-8 文件写入规则改动与用户目标一致：规则不再只约束 Windows PowerShell 终端显示或命令执行，而是扩展到跨 Windows、Linux、WSL、容器和远程服务器的代码、文档、配置、脚本、测试资产与生成文本写入。

未发现阻断提交的问题。当前仍有两点非阻断剩余风险:

- `git diff --check` 提示 `skill-dictionary/data.js` 与 `字典.md` 下一次被 Git 触碰时 CRLF 会替换为 LF；这是生成产物换行规范提示，不是空白错误。
- 工作区存在认证 URL 路由与 WSL 路径显示相关的既有未提交改动，本次审查只确认未将其误判为 UTF-8 规则阻断项。
