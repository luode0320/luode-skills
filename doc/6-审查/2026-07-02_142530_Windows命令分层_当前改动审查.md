# Windows命令分层当前改动审查

审查结论: 通过
审查范围: AGENTS.md、PROJECT_MEMORY.md、PROJECT_STYLE.md、project-agents-bootstrap/SKILL.md、project-agents-bootstrap/scripts/bootstrap_agents.sh、vercel-react-best-practices/AGENTS.md、windows-encoding-rules/SKILL.md、windows-wsl-execution-rules/SKILL.md、windows-wsl-execution-rules/references/command-templates.md、windows-wsl-execution-rules/references/recommended-workflow.md、skill-dictionary/data.js、字典.md
是否允许提交: 是
阻断问题: 无

## 审查背景

- 用户目标：把 Windows 下默认执行口径从“先固化 PowerShell UTF-8 再默认 PowerShell”收敛为“普通仓库命令优先 Git Bash / bash，执行类命令再进 WSL，PowerShell 只用于 Windows 专项场景”。
- 本轮策略：统一回写仓库根规则、长期记忆、长期风格、Windows 相关 skill、自举模板和已存在子项目规则文件，并刷新字典产物。

## 发现

- 未发现 P0 / P1 阻断问题。

## 已覆盖

- 已检查当前 diff，主题集中在 Windows / WSL 命令分层，没有无关扩散到业务无关 skill。
- 已确认 `windows-encoding-rules/SKILL.md` 与 `windows-wsl-execution-rules/SKILL.md` 的 `description` 同步改为 Git Bash / bash 优先口径；`skill-dictionary/data.js` 与 `字典.md` 已通过 `python .\skill-dictionary\generate_dictionary.py` 刷新。
- 已执行 `bash -n project-agents-bootstrap/scripts/bootstrap_agents.sh`，脚本语法通过。
- 已执行 `git diff --check`，未发现 whitespace error；仅有 `skill-dictionary/data.js` 与 `字典.md` 的换行提示，不构成阻断。
- 已检查 `AGENTS.md`、`PROJECT_MEMORY.md`、`PROJECT_STYLE.md` 与 `vercel-react-best-practices/AGENTS.md`，口径一致，均改为“普通命令 Git Bash / bash，执行类命令进 WSL，PowerShell 仅用于专项场景”。

## 剩余风险

- 本轮改动为规则、模板和文档收敛，未涉及运行时代码测试。
- 若后续还有其他子项目规则文件未同步自举模板，仍需按同一口径补齐。
