# Windows 命令优先 bash 规则当前改动总审查

发现:
- 未发现 P0/P1 阻断问题。

未发现阻断项:
- 当前改动集中在 Windows 下普通仓库命令入口从 PowerShell 默认切换为 Git Bash / bash 优先，执行类命令仍保持 `wsl.exe --cd` 进入 WSL 的既有边界。
- PowerShell 相关内容已保留为专项场景，包括 `.ps1`、Windows 专用 cmdlet、PowerShell profile / 编码初始化、`UseBasicParsing` 和显式 UTF-8 写文件模板。

统一判定:
- 审查结论: 通过
- 审查范围: AGENTS.md、PROJECT_MEMORY.md、PROJECT_STYLE.md、project-agents-bootstrap/SKILL.md、project-agents-bootstrap/scripts/bootstrap_agents.sh、skill-dictionary/data.js、vercel-react-best-practices/AGENTS.md、windows-encoding-rules/SKILL.md、windows-wsl-execution-rules/SKILL.md、windows-wsl-execution-rules/references/command-templates.md、windows-wsl-execution-rules/references/recommended-workflow.md、字典.md
- 是否允许提交: 是
- 阻断问题: 无

已覆盖:
- Diff 范围: 已执行 `git diff --stat`、`git diff --name-only`、`git diff --numstat`，确认本轮修改集中在规则文档、模板脚本和生成字典。
- 已读规则: `skill-hit-check-rules`、`parallel-task-dispatch-rules`、`subagent-dispatch-rules`、`skill-evolution-rules`、`skill-compliance-gate-rules`、`skill-audit-rules`、`implementation-review-rules`、`project-change-review-rules`、`artifact-delivery-gate-rules`、`reasoning-summary-structure-rules`。
- 已联动 skill: `windows-wsl-execution-rules`、`windows-encoding-rules`、`project-agents-bootstrap`、`project-memory-rules`、`project-style-rules`、`skill-evolution-rules`、`skill-compliance-gate-rules`、`skill-audit-rules`、`project-change-review-rules`。
- 已执行命令: `bash -n project-agents-bootstrap/scripts/bootstrap_agents.sh`、`bash project-agents-bootstrap/scripts/bootstrap_agents.sh --repo . --target codex`、`python skill-dictionary/generate_dictionary.py`、`PYTHONUTF8=1 python .system/skill-creator/scripts/quick_validate.py windows-wsl-execution-rules`、`PYTHONUTF8=1 python .system/skill-creator/scripts/quick_validate.py windows-encoding-rules`、`PYTHONUTF8=1 python .system/skill-creator/scripts/quick_validate.py project-agents-bootstrap`、旧口径 `rg` 残留检查、新口径 `rg` 覆盖检查、PowerShell 专项保留复核、`git diff --check`、`git status --short --untracked-files=all`。

验证结论:
- 旧口径残留检查未命中已废弃的普通仓库命令入口表达。
- 新口径覆盖检查已命中 AGENTS、自举模板、长期记忆 / 风格、两个 Windows 规则 skill 和字典生成物。
- `bash -n` 退出码为 0；命令过程中出现 WSL `/etc/fstab` 警告，但不影响脚本语法检查结论。
- 三个修改过的 skill 在 `PYTHONUTF8=1` 环境下通过 quick validate。
- `git diff --check` 未发现空白错误；仅提示 `skill-dictionary/data.js` 与 `字典.md` 下次 Git 触碰时会从 CRLF 转为 LF。

未覆盖/剩余风险:
- 本轮为纯规则、文档、模板脚本和生成字典改动，不涉及业务功能代码；免真实运行功能测试。
- 工作树中存在本轮开始前已有的未跟踪 `authenticated-url-routing-rules/`、`doc/5-tests/2026-07-02_114217/` 和若干审查文档，本轮未清理、未回退。
- `skill-dictionary/data.js` 与 `字典.md` 由生成脚本刷新，包含当前工作树可见 skill 状态；未手工编辑生成物。
