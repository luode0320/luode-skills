# Obsidian 跨宿主 CLI 桥接当前改动总审查

## 审查判定

- 审查结论: 通过
- 审查范围: `obsidian-knowledge-flow/scripts/obsidian_cli_bridge.py`、`obsidian-knowledge-flow/scripts/obsidian_cli_windows.ps1`、`doc/5-tests/2026-07-13_134643/skills/obsidian-knowledge-flow/`、CYCLE-OBS-01 执行记录与测试 README。
- 是否允许提交: 否
- 阻断问题: 无；Git 提交仍须用户在当前轮明确授权。

## 已覆盖

- 边界：仅新增 bridge、adapter 与同一时间戳测试资产；未迁移 `distill_vault.py`、SKILL.md 或 references。
- 安全：adapter 二次固定 `D:\obsidian_data` 与 `知识库/` 校验；拒绝 traversal、盘符、UNC、控制字符、非法字符、重复 vault 注册和旧 nested-vault 根。
- 协议：保留 adapter 非零退出的结构化错误码；WSL 响应由 bridge 标记为 `wsl-powershell-interop`；CLI 零退出的 `Error:` 仅在 version、无结构 vault listing 与 create/append 的无正文输出中视为失败，read/search 正文不作前缀猜测。
- 可靠性：request/response 走 UTF-8 临时 JSON；写入正文不进入公开 bridge 参数；长 stdout 异步 drain；10KB 中文分块读回由离线契约测试覆盖。
- 验证：bridge 单测 `16/16 PASS`、adapter 契约测试 `17/17 PASS`、Python 编译和 PowerShell parser PASS；Windows/WSL doctor 与 smoke create/read/append/read 均取得 `verified=true`。

## 发现

- AC-OBS-003 已补证：用户关闭 Obsidian 后，Windows `doctor` 返回 `started_app=true`、`attempts=3`、`ok=true`、`verified=true`；未杀用户既有进程。

- 实机检索入口已修复：PowerShell 参数数组改为独立构造 `query=`/`limit=`，Windows search/search-context 与 WSL search 均返回 `ok=true`、`verified=true`；fake CLI 现会断言两个 key=value token 未拆分，adapter 也拒绝绕过 bridge 的非整数或范围外 `limit`。

## 未发现阻断项之外的缺陷

- 未发现路径越界、结构化错误码丢失、WSL transport 标记错误、10KB stdout 死锁、`Error:` 正文误拒绝、语义错误误分流或真实 smoke readback 不一致。
- PowerShell adapter 按实施计划保持 ASCII-only；动态中文仅从 UTF-8 JSON 输入读取，不以脚本源代码承载。

## 受影响运行路径

| 路径 | 触发方式 | 验证方式 | 状态 |
| --- | --- | --- | --- |
| Windows doctor | `bridge.py doctor --json` | 官方 CLI version + vault selector | 已真实运行 |
| WSL doctor | WSL interop 调用 bridge | Windows PowerShell interop | 已真实运行 |
| Windows create/read | bridge create/read smoke path | CLI readback `verified=true` | 已真实运行 |
| WSL read/append | WSL bridge read/append smoke path | Windows readback | 已真实运行 |
| 自动启动恢复 | 应用停止后的 doctor | `started_app=true` | 阻断，禁止杀进程 |
