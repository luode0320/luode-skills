# Imagegen 错误案例演进：当前改动总审查

## 发现

未发现阻断项：当前改动没有发现 P0/P1 缺陷；已将瞬态重试案例的适用范围收窄为脚本实际覆盖的 `generate-batch`，避免把单图路径误写成已支持能力。

## 统一判定

- 审查结论: 通过
- 审查范围: `imagegen/SKILL.md`、`imagegen/references/error-casebook.md`、`skill-dictionary/data.js`、`字典.md`、`PROJECT_CURRENT.md`、`PROJECT_MEMORY.md`、`PROJECT_HISTORY.md`，以及当前新增审查文档。
- 是否允许提交: 是（仅表示当前改动通过审查，不代表本轮已执行 Git 提交）
- 阻断问题: 无

## 已覆盖

- Diff 范围：未提交工作树中的 7 个已修改文件和 1 个新增案例库文件；已读取实际 diff 与新增文件全文。
- 已读规则：`project-change-review-rules`、`artifact-delivery-gate-rules`、`skill-compliance-gate-rules`、`project-memory-rules`、`project-style-rules`、`reasoning-summary-structure-rules`、`skill-hit-check-rules`。
- 已联动 skill：`imagegen`、`skill-dictionary` 生成链路、项目记忆与文档落盘闸门；本轮未修改运行脚本和业务代码，因此不适用代码注释双 skill 的函数头检查。
- 证据核对：逐项对照 `image_gen.py` 的 gpt-image-2 尺寸/透明度/`input_fidelity` 校验、batch 瞬态错误重试、入口脚本 `check` 行为和 `references/cli.md` / `references/image-api.md`。
- 已执行命令：合法 generate/edit dry-run；非法尺寸、`background=transparent`、`input_fidelity=high` 负向 dry-run；`run_imagegen.ps1 -Action check`；`quick_validate.py imagegen`；skill 字典生成器；案例库敏感信息扫描；`PROJECT_MEMORY.md` YAML 索引解析；`git diff --check`；伪客户端重试上限与非瞬态错误验证。
- 安全与编码：未发现密钥、token、密码、完整鉴权响应、完整 prompt、输入图片内容或未经脱敏本机路径；关键新增文本按 UTF-8 回读。
- 文档落盘：本报告已真实写入 `doc/6-审查/`，符合来源对象标识和时间戳命名约定。

## 未覆盖/剩余风险

- 未调用真实图像 API，未验证真实上游响应、实际限流响应头或网络波动；这是本轮本地验证边界，不将线上现象伪装成本地通过。
- 未运行内置 `image_gen` 的真实生成与复杂透明主体的 alpha 质量检查。
- `image_gen.py` 的单图 `generate` / `edit` 当前没有自动重试；案例库已明确仅适用于 `generate-batch`，后续若脚本能力变化应更新原案例并重新验证。
- Linux/macOS 入口脚本未在本机执行，仅核对其规则与路径；跨平台差异需要在对应环境复核。
