# 本地连接调试测试红线当前改动审查

审查结论: 通过
审查范围: AGENTS.md、PROJECT_MEMORY.md、test-strategy-rules/SKILL.md、requirement-discovery-rules/SKILL.md、bug-discovery-rules/SKILL.md、bug-runtime-debug-rules/SKILL.md、bug-reproduction-rules/SKILL.md、bug-gap-rules/SKILL.md、bug-validation-rules/SKILL.md、functional-validation-rules/SKILL.md、test-regression-rules/SKILL.md、test-program-rules/SKILL.md、project-release-test-rules/SKILL.md、agent-browser/SKILL.md、windows-wsl-execution-rules/SKILL.md、project-agents-bootstrap/SKILL.md、project-agents-bootstrap/scripts/bootstrap_agents.sh、vercel-react-best-practices/AGENTS.md、skill-dictionary/data.js、字典.md
是否允许提交: 是
阻断问题: 无

## 审查背景

- 用户要求：需求、Bug、测试相关以及启动调试相关 skill 中，强制禁止连接 test / prod 数据库或其他服务，只允许 local 本地数据库和本地服务用于调试、启动、测试和查询。
- 本轮策略：根规则写入 `AGENTS.md`，长期事实写入 `PROJECT_MEMORY.md`，中央单一真相源写入 `test-strategy-rules/SKILL.md`，相关入口 skill 写最小引用和阻断条件。
- 追加要求：`project-agents-bootstrap` 的最小模板和脚本也必须增加该规则，确保其他项目通过自举脚本同步时不会漏掉本地连接红线。

## 发现

- 未发现 P0 / P1 阻断问题。

## 已覆盖

- 已检查本轮 diff，确认未修改 skill `description`；`project-agents-bootstrap/SKILL.md` 的最小模板新增了 `## 本地连接调试测试红线（最高优先级，强制）`，已执行 `python .\skill-dictionary\generate_dictionary.py` 刷新 `skill-dictionary/data.js` 与 `字典.md`。
- 已用 `rg` 检查旧口径，原 `requirement-discovery-rules` 中“写库 / 调生产接口仍需暂停确认”的口子已改为非 local 连接阻断。
- 已将 `agent-browser` 中 staging / production diff 示例改为 local URL 示例，避免测试/监控示例与新红线冲突。
- 已将 `project-agents-bootstrap/scripts/bootstrap_agents.sh` 增加 `BODY_LOCAL_ONLY_CONNECTION` 和受管章节同步调用，并执行 `bash ./project-agents-bootstrap/scripts/bootstrap_agents.sh --repo . --target codex` 验证会同步根 `AGENTS.md` 与已有子项目 `vercel-react-best-practices/AGENTS.md`。
- 已执行 `bash -n ./project-agents-bootstrap/scripts/bootstrap_agents.sh`，脚本语法通过。
- 已执行 `git diff --check`，未发现 whitespace error；仅有当前仓库 `core.autocrlf` 相关 CRLF 提示。
- 已执行 `codegraph sync` 后复查 `codegraph status .`，索引可用且状态为 up to date。

## 剩余风险

- 本轮只修改规则文档，未运行业务测试。
- 仓库中其他非需求 / Bug / 测试 / 调试启动类 skill 若未来新增远端联调示例，需要继续按本红线补齐。
