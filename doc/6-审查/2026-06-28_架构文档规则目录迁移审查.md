# 架构文档规则目录迁移审查

## 审查范围

- 本次审查覆盖 `doc/1-架构` 到 `doc/7-验收` 编号活动目录迁移。
- 覆盖新增 `architecture-doc-rules` 及其 references。
- 覆盖 `artifact-storage-rules` 路径真相源、README、PROJECT_MEMORY、PROJECT_STYLE、项目设计、编码skill、字典和活动 skill 旧路径引用同步。

## 发现

- 未发现 P0/P1 阻断项。
- 未发现旧活动目录双写要求残留。
- 未发现 `doc/需求`、`doc/架构`、`doc/实施`、`doc/验收`、`doc/bugs`、`doc/tests`、`doc/审查` 的严格边界旧路径引用残留。

## 已验证

- `PYTHONUTF8=1 python .\skill-dictionary\generate_dictionary.py` 通过，结果为 `implemented_total=78`、`planned_missing=0`、`seed_total=22`。
- 旧活动路径回归搜索无命中。
- 编号目录顺序残留搜索无命中。
- 提交前回退检查通过：提交标题格式、README 日志、staged Go 测试文件位置、`internal/service/*.go` 禁放均通过。

## 剩余风险

- 仓库内未找到 `quick_validate.py`，因此未执行该验证脚本。
- Windows Git 输出仍提示部分文件未来可能发生 CRLF/LF 转换，属于现有换行策略提示；本次未额外扩大换行策略调整。

## 结论

本次目录迁移和 `architecture-doc-rules` 新增可以提交。
