# 多 skill 平台判定澄清与 Bug 域图示规范当前改动总审查

审查结论: 通过
审查范围: `.system/plugin-creator/SKILL.md`、`.system/skill-creator/SKILL.md`、`.system/skill-installer/SKILL.md`、`imagegen/SKILL.md`、`mcp-installation-rules/SKILL.md`、`mcp-installation-rules/references/config-bootstrap.md`、`subagent-dispatch-rules/SKILL.md`、`project-agents-bootstrap/SKILL.md`、`godot-project-bootstrap-rules/SKILL.md`、`skill-dictionary/data.js`、`字典.md`、`artifact-delivery-gate-rules/SKILL.md`、`artifact-storage-rules/SKILL.md`、`artifact-storage-rules/references/naming-templates.md`、`artifact-storage-rules/references/path-map.yaml`、`bug-intake-rules/SKILL.md`、`bug-intake-rules/agents/openai.yaml`、`bug-intake-rules/references/bug-description-template.md`、`PROJECT_MEMORY.md`（"文档落盘闸门"词条的 Mermaid/SVG 措辞更新部分）
是否允许提交: 是
阻断问题: 无

## 背景

工作区在完成 WSL 工具 PATH 误用排查批次提交后，仍存在两组与该批次无关、但彼此各自内聚的既有改动，本次一并审查后收口提交：

1. **平台判定澄清批**：`.system` 三件套（plugin-creator/skill-creator/skill-installer）、`imagegen`、`mcp-installation-rules`、`subagent-dispatch-rules`、`project-agents-bootstrap`、`godot-project-bootstrap-rules` 等 skill 补充"当前运行环境是 Codex 还是 Claude Code"的判定分支，避免把 Codex 专属机制（`$CODEX_HOME`、`./codex/config.toml`、`codex mcp add` 等命令）误当成两个平台通用的假设。其中 `mcp-installation-rules`、`subagent-dispatch-rules`、`imagegen` 新增了 `##` 级标题，已同步重新生成 `skill-dictionary/data.js` 与 `字典.md`。
2. **Bug 域图示规范批**：`artifact-delivery-gate-rules`、`artifact-storage-rules`（含两个 references）、`bug-intake-rules`（含 agent 与 references）把 Bug 域"README.md + flow.svg + sequence.svg"三件套统一改为"README.md 正文内嵌 Mermaid 流程图与时序图"，与需求域既有的"正文内嵌 Mermaid、不另建配套图文件"约定对齐；`PROJECT_MEMORY.md` 中"文档落盘闸门"词条同步更新表述。

## 发现

未发现 P0/P1 问题。

- [P3] `mcp-installation-rules/SKILL.md`、`.system/skill-creator/SKILL.md` 等 - Claude Code 具体机制标注为"待确认"
  证据: 多处新增文案明确写"当前尚未实测确认""本次未实测验证"，仅做判定与提示，不下具体命令结论。
  风险: 后续若有 agent 在 Claude Code 环境照此执行，可能因缺少具体命令而卡在"需要用户确认"这一步，属已知且明确暴露的限制，不是被掩盖的缺陷。
  建议: 待用户或后续会话实测确认 Claude Code 的 `.mcp.json` / 插件安装机制后再补一版明确结论；无需现在阻断。
  关联 skill: `mcp-installation-rules`

## 已覆盖

- 已读取本次两批改动涉及的全部文件 diff（非仅 `git status`），逐文件核对措辞与前后一致性。
- **需求边界**：两批改动均未扩大到审查范围外的文件；`.system/imagegen/`、`.system/openai-docs/` 经确认属于官方分发的只读参考快照（`imagegen/SKILL.md` 内新增说明已明确此点），非本次遗漏。
- **一致性交叉校验**：全仓库 `grep` 确认 `flow.svg`/`sequence.svg`/`bug_requires_svg_diagrams`/`bug_flow_svg`/`bug_sequence_svg` 已无遗留引用（仅剩的 3 处命中均为新文案中"不另建独立 flow.svg/sequence.svg"的说明性提及）；`path-map.yaml` 新 key `bug_doc_embeds_diagrams_in_markdown` 在仓库内无其他消费方引用旧 key，重命名不留断链。
- **skill 合规**：核对 `mcp-installation-rules`（新增"平台判定与 Claude Code MCP 配置分支"）、`subagent-dispatch-rules`（新增"平台能力矩阵"）、`imagegen`（新增"脚本路径解析规则"）三处新增 `##` 标题均已同步反映到 `skill-dictionary/data.js` 与 `字典.md` 的 `sections` 列表与 `generated_at` 时间戳；未发现遗漏重跑生成脚本的情况。
- **重复逻辑 / 安全风险 / 日志**：本轮均为纯 Markdown/YAML 规则文档与生成产物变更，无可执行代码、无鉴权/日志/数据库改动，不适用。
- **注释质量双 skill**：本轮无函数、字段、结构体等代码实体改动，`comment-placement-granularity-rules` 与 `comment-completion-gate-rules` 的检查项不适用。

## 未覆盖/剩余风险

- 未对"平台判定澄清批"新增的判定分支做 forward-test（未模拟"当前 agent 为 Claude Code，遇到 MCP 缺口"场景验证子 agent 是否会正确停下确认而非瞎猜）；相较 WSL 批次涉及新增操作性指引，本批更偏"事实澄清 + 术语替换"，改动前后都不会让 agent 执行更危险的动作（改动前是错误默认执行 Codex 命令，改动后是停下来问用户），风险方向是收敛而非发散，判断可不做 forward-test 直接放行；如需更高把握，可后续用一次真实 Claude Code 会话触发 MCP 安装场景验证。
- 未逐字核对 `字典.md`（HTML/Markdown 渲染产物）与 `data.js` 的完整 diff 是否存在生成脚本之外的手工改动痕迹，仅核对了本次涉及的 3 个 skill 的 `sections` 字段与 `generated_at`；该文件由脚本生成，风险较低。

## 判定

两批改动各自主题内聚、边界清晰、无遗留断链引用，`skill-dictionary` 重新生成核对无误，未发现 P0/P1 阻断项，审查范围内允许提交；建议按主题拆分为两次独立提交（平台判定澄清批、Bug 域图示规范批），便于后续按主题回溯。
