---
schema_version: 1
doc_id: "REVIEW-RTP-003"
doc_type: "review"
source_ids: ["SRC-RTP-006", "BUG-RTP-20260726-001", "CYCLE-RTP-07", "EVIDENCE-RTP-CHAIN-01"]
status: "accepted"
version: "v1.0"
current_slice: "首次持久化与立即同步实现审查"
updated_at: "2026-07-26 15:00:00"
reader_level: "business_general"
writing_style: "plain_chinese"
appendix_policy: "preserve_existing_or_one_terminal_appendix"
review_acceptance_gates:
  - stage: "review"
    applicability: "applicable"
    reason: "本轮修改投影入口、测试和相邻执行路由，需要确认职责、顺序和失败保护。"
    basis: "CYCLE-RTP-07、RULE-RTP-016..022"
    required_by_source: true
    required_now: true
    completed_validation: ["69 项投影回归", "三个 Skill quick_validate", "UTF-8", "git diff --check"]
    substitute_validation: ["仅静态核验真实 Desktop 可见性，不能替代宿主证据"]
    manual_follow_up: "由宿主执行一次真实首个领域动作并确认列表可见。"
    pass_standard: "写盘成功后的下一动作只能是 update_plan；失败保留 projection 并停止领域写。"
  - stage: "acceptance"
    applicability: "limited"
    reason: "真实 Codex Desktop 首显证据受宿主自动化边界限制。"
    basis: "用户 29 分钟无悬浮窗反馈与 TEST-RTP-024"
    required_by_source: true
    required_now: true
    completed_validation: ["本地行为回归", "文档 strict profile"]
    substitute_validation: ["调用序列、payload、失败关闭和 session 隔离测试"]
    manual_follow_up: "在允许操作 Codex Desktop 的宿主中验证持久化后立即出现 active/blocked 列表。"
    pass_standard: "真实宿主确认首显；宿主不可用时只能保持 LIMITED/HOST_BLOCKED。"
---

# 任务悬浮窗首次持久化实现审查

结论：本轮实现满足进入最终验收的最低质量条件，未发现 P0/P1 阻断；影响：默认执行任务现在可以在首个领域动作前形成当前会话投影并获得立即同步所需的 payload；范围：投影脚本、投影测试、相邻执行规则和会话状态文档；非范围：Codex Desktop 产品源码、后台定时器、外部服务和 Git 历史；变化：首次显示由十分钟异常入口前移到持久化后立即同步；完成标准：本地行为、规则和文档门禁通过，并明确真实宿主证据边界；术语说明：`UI_SYNC_BLOCKED` 表示投影已保存但宿主 UI 调用失败；验证状态：本地回归和静态审查通过，真实 Desktop 首显仍受限；图片资产决策：N/A，原因是本轮不交付图片，证据由行为测试、投影 JSON 和规则文档承担。

## 文档信息

- 来源对象：`BUG-RTP-20260726-001`
- 实施周期：`CYCLE-RTP-07`
- 当前审查切片：首次持久化、立即同步、session 隔离和完成收口

## 发现

发现:
- [P2] `task-plan-rehydration-rules/scripts/task_plan_projection.py:1-1877` - 投影 Owner 文件已超过 500 行
  证据: 当前文件 1877 行，本轮新增首次持久化、会话解析和完成收口逻辑；该文件原本已是集中式 Owner。
  风险: 后续继续叠加入口会增加审查和回归成本。
  建议: 后续周期按“会话解析/状态迁移/合成与超时/CLI 编排”拆分为同一包内模块；本周期不拆分是为了保持 v4 schema 和用户已有工作树最小变更。
  关联 skill: `package-structure-rules`、`code-minimal-change-rules`

## 未发现阻断项

- 未发现 P0/P1 阻断项。
- Python 仅使用标准库，未新增外部依赖、数据库、网络或权限路径。
- `active/blocked/inactive`、显式 session 优先、冲突失败关闭、原子写入和 UI 失败停止边界均有代码与测试证据。

## 统一判定

- 审查结论: 通过
- 审查范围: `task-plan-rehydration-rules/scripts/task_plan_projection.py`、`task-plan-rehydration-rules/tests/test_task_plan_projection.py`、`autonomous-execution-rules/references/continuation-and-pause.md`、周期 07 需求/验收/实施/Bug 文档及项目四件套相关增量。
- 是否允许提交: 否（当前轮未获得 Git 历史写入授权）
- 阻断问题: 无

## 完成标准

- 投影脚本和测试在 Python local 环境真实运行通过。
- 已命中的实现、注释、Skill 合规和文档门禁均有可追溯证据。
- P0/P1 为零；真实 Desktop 证据缺失只影响最终放行状态，不伪报为 UI 通过。

## 已覆盖

- Diff 范围: 当前工作树所有可见改动已盘点；实现审查重点展开投影脚本、测试和执行路由，未跟踪周期文档按入口、追踪、门禁和状态抽样核验。
- 已读规则: `implementation-review-rules` 及其质量、格式、语法/引用、归位/依赖和共享证据契约；注释双 Owner；`artifact-delivery-gate-rules` 白话与条件门禁。
- 已联动 skill: `task-plan-rehydration-rules`、`autonomous-execution-rules`、`context-compression-rules`、`skill-execution-compliance-gate-rules`、`skill-evolution-rules`、`skill-audit-rules`。
- 已执行命令: `py -3 -X utf8 -B task-plan-rehydration-rules/tests/test_task_plan_projection.py`、`py_compile`、三个 `quick_validate.py`、四类工程文档 strict profile、字典生成、`git diff --check`、UTF-8 回读和残留 lock/tmp 扫描。

## 注释核对

- 函数注释核对清单: `resolve_session_id`、`_deactivate_projection_with_payload`、`build_completion_payload`、`_is_projection_input`、`ensure_start_projection`、测试 `setUp`/清理及本轮修改测试方法均具备 `[参数]`、`[返回]`、北京时间 `最近修改时间`、改动原因和就近步骤注释；结论 PASS。
- 字段/结构体字面量注释核对清单: 本轮没有新增配置、DTO、结构体或跨层结构体字面量；字段/结构体字面量位点 0 个，结论 PASS。
- 补丁逻辑注释核对清单: session 冲突失败、完成 payload 一次性返回、start 与 timeout 分流、UI 同步顺序均有就近中文原因注释；结论 PASS。

## 运行路径与验证降级

- 首次路径：`ensure-start` 原子写入当前 session，返回可直接传给 `update_plan` 的 payload；69/69 行为回归覆盖创建、幂等、隔离和错误。
- 完成路径：失活与 Goal complete 在写 inactive 前返回一次性全完成 payload，失活后禁止重放；专项回归通过。
- 真实宿主路径：本地测试不能证明 Codex Desktop 实际显示列表，因此只保留 `LIMITED/HOST_BLOCKED`，不把静态结果写成 UI 通过。

## 必要后续动作

1. 在允许的真实宿主中完成 `TEST-RTP-024`，并在同一最终验收文档更新首显证据；未取得证据时保持受限状态。
2. 后续周期按职责拆分超过 500 行的投影 Owner 文件，拆分后重跑同一套 69 项回归和文档门禁。

## 执行附录

- 代码长度：脚本 1877 行，测试 1941 行；本轮新增逻辑集中在会话解析、ensure-start、完成收口和对应测试。
- 质量扫描：未发现 `print()`、`pdb`、`breakpoint()` 调试残留；未命中 Go 测试资产；未新增依赖、API、数据库或网络连接。
- 编码与清理：目标代码/文档 UTF-8 回读通过；`git diff --check` 通过；未发现残留 `.lock`/`.tmp` 文件。

## 追踪附录

- 主链：`SRC-RTP-006 -> DEC-RTP-010/011/012 -> REQ-RTP-010..013 -> RULE-RTP-016..022 -> AC-RTP-014..017 -> CYCLE-RTP-07 -> TASK-RTP-22..28`。
- 证据：`TEST-RTP-019..025`、投影回归 69/69、三个 Skill quick_validate、需求/验收/周期/Bug strict profile、字典生成和 `git diff --check`。
- 证据索引：`EVIDENCE-RTP-CHAIN-01`。
- 审查状态：实现审查通过；P2 文件拆分风险已记录；真实 Desktop 首显继续由最终验收按 `LIMITED/HOST_BLOCKED` 管理。
