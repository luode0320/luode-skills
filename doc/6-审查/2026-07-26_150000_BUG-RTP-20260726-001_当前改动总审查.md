---
schema_version: 1
doc_id: "REVIEW-RTP-004"
doc_type: "review"
source_ids: ["SRC-RTP-006", "BUG-RTP-20260726-001", "CYCLE-RTP-07", "TASK-RTP-28", "EVIDENCE-RTP-CHAIN-01"]
status: "accepted"
version: "v1.0"
current_slice: "TASK-RTP-28 当前改动总审查"
updated_at: "2026-07-26 15:00:00"
reader_level: "business_general"
writing_style: "plain_chinese"
appendix_policy: "preserve_existing_or_one_terminal_appendix"
review_acceptance_gates:
  - stage: "review"
    applicability: "applicable"
    reason: "本轮修改任务投影入口、执行路由、测试和项目文档，必须确认范围、顺序、失败保护与兼容边界。"
    basis: "CYCLE-RTP-07、TASK-RTP-28 与 project-change-review-rules。"
    required_by_source: true
    required_now: true
    completed_validation: ["69 项投影回归", "三个 Skill quick_validate", "py_compile", "git diff --check", "UTF-8 回读"]
    substitute_validation: ["实现审查文档中的注释双 Owner PASS 与静态 UI 边界证据"]
    manual_follow_up: "N/A"
    pass_standard: "目标 diff 无 P0/P1，规则、实现、测试和文档使用同一持久化后立即同步口径。"
  - stage: "acceptance"
    applicability: "limited"
    reason: "真实 Codex Desktop 首个领域动作后的悬浮列表首显仍受宿主自动化边界限制。"
    basis: "本轮非范围不含 Desktop 产品源码，TEST-RTP-024 需要真实宿主确认。"
    required_by_source: true
    required_now: true
    completed_validation: ["本地行为回归", "规则与文档静态核验"]
    substitute_validation: ["调用顺序、payload、失败关闭、session 隔离和失活重放保护测试"]
    manual_follow_up: "在允许操作 Codex Desktop 的宿主中执行一次首个领域动作，确认 active/blocked 列表真实可见并记录回放证据。"
    pass_standard: "真实宿主确认首显；宿主不可用时保持 LIMITED/HOST_BLOCKED，不升级为正式 UI 放行。"
---

# 任务悬浮窗首次持久化当前改动总审查

结论：本轮目标改动未发现必须阻断合并的高风险问题，可以进入受限最终验收；影响：默认执行任务会在首个领域动作前保存当前会话投影，并为宿主提供紧邻的任务列表同步数据；范围：首次持久化、会话隔离、失活收口、执行路由、相关测试和项目规则；非范围：Codex Desktop 产品源码、后台定时器、外部服务、其它并行会话改动和 Git 历史；变化：任务列表的首次显示不再等待十分钟，十分钟只保留为漏投影的异常修复；完成标准：目标改动无 P0/P1、行为回归和文档门禁通过，真实桌面首显证据缺失时明确保持受限；术语说明：任务投影是写入项目当前文档、供任务列表恢复的步骤状态；验证状态：本地回归、语法、Skill 校验和差异检查已通过，真实桌面首显仍需宿主补验。

## 审查结论

- 目标 diff 未发现 P0/P1 阻断，可进入受限最终验收。
- 真实 Desktop 首显尚未取得，不能把本地行为测试写成 UI 已显示。
- 图片资产决策：N/A。原因：本轮不交付图片或界面素材；证据：目标写集仅包含规则、脚本、测试和文档，首显判断保留宿主行为证据边界。

## 完成标准

- 目标改动边界可追溯，规则、实现、测试和文档对“持久化后立即同步”使用同一口径。
- 投影回归、语法检查、Skill 校验、差异检查和审查文档 profile 均通过。
- 真实宿主证据缺失时，最终状态保持 `LIMITED/HOST_BLOCKED`。

## 发现

发现:
- [P2] `task-plan-rehydration-rules/scripts/task_plan_projection.py:1-1696` - 投影 Owner 文件体量偏大
  证据: 当前文件 1,696 行，本轮继续在同一 Owner 中加入会话解析、首次持久化、完成收口和 CLI 编排；测试文件当前 1,806 行。
  风险: 后续继续叠加入口会提高审查、定位和回归成本，但当前行为边界已有回归覆盖。
  建议: 后续周期按“会话解析、状态迁移、合成与超时、CLI 编排”拆分为同一包内模块；拆分时保持 v4 registry、旧版本读取兼容和同一锁语义，并重跑现有回归。
  关联 skill: `package-structure-rules`、`code-minimal-change-rules`

## 未发现阻断项

- 未发现 P0/P1 阻断项。
- `ensure-start`、`write`、失活和 Goal 完成入口均以当前 `session_id` 为边界；显式值与 `CODEX_THREAD_ID` 冲突、缺失或非法时失败关闭。
- 持久化成功后的下一步 payload、`UI_SYNC_BLOCKED` 失败保护、Plan Mode 排除和十分钟异常修复口径已同步到 Owner 与相邻规则。
- 本轮未新增数据库、HTTP API、第三方连接、权限入口或敏感信息持久化。

## 统一判定

- 审查结论: 通过
- 审查范围: `task-plan-rehydration-rules/scripts/task_plan_projection.py`、`task-plan-rehydration-rules/tests/test_task_plan_projection.py`、`task-plan-rehydration-rules/SKILL.md`、`task-plan-rehydration-rules/references/task-plan-projection-contract.md`、`autonomous-execution-rules/SKILL.md`、`context-compression-rules/SKILL.md`、`skill-hit-check-rules/SKILL.md`、周期 07 相关需求/实施/验收/Bug 文档和项目四件套中的本轮增量。
- 是否允许提交: 否（当前轮未获得 Git 历史写入授权）
- 阻断问题: 无

## 注释门禁

- 函数注释核对清单: PASS。`resolve_session_id`、`_deactivate_projection_with_payload`、`build_completion_payload`、`_is_projection_input`、`ensure_start_projection` 及本轮新增/修改测试方法均具备 `[参数]`、`[返回]`、北京时间 `最近修改时间`、改动原因和就近步骤注释；证据见同源实现审查文档 `2026-07-26_150000_BUG-RTP-20260726-001_实现审查.md`。
- 字段/结构体字面量注释核对清单: PASS。本轮字段/结构体字面量位点 0 个，没有新增配置、DTO、跨层映射或服务依赖对象。
- 补丁注释核对清单: PASS。session 冲突失败、start/timeout 分流、一次性完成 payload、UI 同步顺序和失败停止边界均有就近中文原因注释。

## 已覆盖

- Diff 范围: 已盘点工作树状态、未暂存 diff、暂存 diff 和未跟踪文件；审查展开范围仅限 `BUG-RTP-20260726-001 / CYCLE-RTP-07` 目标写集，未把其它并行任务误算为本轮成果。
- 已读规则: `F:/luode-skills/AGENTS.md`、`project-change-review-rules/SKILL.md`、`project-change-review-rules/references/shared-evidence-and-specialized-contracts.md`、审查清单与报告模板、`artifact-delivery-gate-rules` 白话和验收门禁契约、`artifact-storage-rules` 路径与复用规则、注释双 Owner 及其必要 references。
- 已联动 skill: `skill-hit-check-rules`、`parallel-task-dispatch-rules`、`reasoning-summary-structure-rules`、`project-memory-rules`、`project-style-rules`、`task-plan-rehydration-rules`、`autonomous-execution-rules`、`context-compression-rules`、`comment-placement-granularity-rules`、`comment-completion-gate-rules`、`artifact-delivery-gate-rules`、`project-change-review-rules`。
- 已执行命令: 投影测试、`py_compile`、三个 `quick_validate.py`、`git diff --check`、UTF-8 回读与目标文件行数核对；当前报告的 review profile 与 strict profile 均已通过。

## 未覆盖/剩余风险

- 未运行测试: 真实 Codex Desktop `TEST-RTP-024` 未能在本线程执行；目标投影回归没有遗漏，不能用本地脚本替代宿主首显证据。
- 未展开文件: 工作树中其它会话的 Browser Use Cloud、Plan Mode 和通用上线测试资产未纳入本轮逐行审查；它们保留给各自来源对象的审查文档。
- 仍需确认: 真实宿主是否在首次持久化后实际显示列表；在确认前，周期 07 的验收状态保持 `LIMITED/HOST_BLOCKED`。

## 必要后续动作

1. 在允许操作 Codex Desktop 的真实宿主中完成首显补验，并将回放证据写入同一来源的最终验收文档；未取得证据前不得把受限状态写成正式 UI 通过。
2. 后续周期拆分超过 500 行的投影 Owner 文件，保持当前会话锁、原子写入、payload 一致性和 69 项回归不变。

## 执行附录

- 目标投影回归：`py -3 -X utf8 -B F:\luode-skills\task-plan-rehydration-rules\tests\test_task_plan_projection.py`，`Ran 69 tests`、`OK`。
- 语法检查：`py -3 -X utf8 -m py_compile ...task_plan_projection.py ...test_task_plan_projection.py`，退出码 0。
- Skill 校验：`quick_validate.py task-plan-rehydration-rules`、`autonomous-execution-rules`、`context-compression-rules` 均输出 `Skill is valid!`。
- 差异检查：`git -C F:\luode-skills diff --check` 退出码 0；仅有现存换行转换警告，没有空白错误。
- 关键实现证据：`resolve_session_id` 位于脚本第 127 行；完成收口位于 `_deactivate_projection_with_payload` 第 913 行与 `build_completion_payload` 第 1037 行；首次持久化入口 `ensure_start_projection` 第 1518 行；CLI 路由在第 1740 行以后。
- 关键规则证据：`task-plan-rehydration-rules/SKILL.md` 第 15、21、24、39-41、121-124 行；`autonomous-execution-rules/SKILL.md` 第 36-40 行；`context-compression-rules/SKILL.md` 第 31-32、43、54 行；`skill-hit-check-rules/SKILL.md` 第 18-20 行。
- 真实宿主证据：未取得；依据验收门禁契约记为 `LIMITED/HOST_BLOCKED`，不宣称悬浮窗已在 Desktop 中出现。

## 追踪附录

- 主链：`SRC-RTP-006 -> DEC-RTP-010/011/012 -> REQ-RTP-010..013 -> RULE-RTP-016..022 -> AC-RTP-014..017 -> CYCLE-RTP-07 -> TASK-RTP-22..28`。
- 证据：`TEST-RTP-019..025`、投影回归 69/69、三个 Skill quick_validate、py_compile、`git diff --check`、UTF-8 回读、同源实现审查文档和当前报告 review profile。
- 范围隔离：其它并行任务保持原工作树状态；本报告未执行 reset、checkout、格式化、提交、推送或任何外部连接。
- 审查结论变更：初始状态为待审查；完成目标 diff、规则、注释 Owner、行为回归和文档门禁核对后为通过；真实 Desktop 首显仍由最终验收单独保持受限。
