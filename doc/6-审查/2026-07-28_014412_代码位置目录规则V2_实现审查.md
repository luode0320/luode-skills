---
schema_version: 1
doc_id: "REVIEW-PSR-V2-001"
doc_type: "review"
source_ids: ["REQ-PSR-V2-001", "REQ-PSR-UTILS-001", "REQ-PSR-BUSINESS-RPC-004", "REQ-PSR-ADOPT-001", "REQ-PSR-IP-001", "REQ-PSR-GOVERNANCE-001", "REQ-PSR-CLAUDE-001", "REQ-PSR-DATABASE-002"]
status: "accepted"
version: "v1.6"
current_slice: "TASK-13-03 数据存储目录与公开查询兼容审查收口"
updated_at: "2026-07-31"
reader_level: business_general
writing_style: plain_chinese
appendix_policy: preserve_existing_or_one_terminal_appendix
review_acceptance_gates:
  - stage: review
    applicability: applicable
    reason: 本轮修改目录规则、公开 CLI、隔离脚本和测试资产，必须审查一致性。
    basis: REQ-PSR-CLI-003、REQ-PSR-BUSINESS-RPC-004、REQ-PSR-ADOPT-001、REQ-PSR-IP-001、REQ-PSR-GOVERNANCE-001、REQ-PSR-CLAUDE-001、REQ-PSR-DATABASE-002。
    required_by_source: true
    required_now: true
    completed_validation: ["TEST-PSR-UTILS-001", "TEST-PSR-RPC-001", "TEST-PSR-RPC-002", "TEST-PSR-RPC-003", "TEST-PSR-ADOPT-001", "TEST-PSR-IP-001", "TEST-PSR-GOVERNANCE-001", "TEST-PSR-CLAUDE-001", "TEST-PSR-DATABASE-001", "TEST-PSR-DATABASE-002", "TEST-PSR-DATABASE-003"]
    substitute_validation: []
    manual_follow_up: N/A
    pass_standard: 未发现 P0/P1，且目录、CLI、隔离检查和测试证据一致。
---

# 代码位置目录规则 V2 实现审查

结论：本轮目录规则实现通过审查；影响：后端代码生成、查找、跨业务调用、项目治理文件初始化和旧项目渐进采用将得到唯一且可检查的位置；范围：根 `utils/`、源码根 `util/`、业务域 `rpc/`、数据存储连接与模型、独立 SQL、Catalog、CLI、测试与对应文档；非范围：真实业务仓库、数据库、网络 RPC、前端工具目录和 Git 历史；变化：`database/connection/` 承载关系型数据库、Redis 和 Mongo 的连接初始化，`database/model/` 固定分类为 `db/`、`redis/`、`mongo/`，Java 映射明确落入三个模型叶子目录，字段 SQL 固定为 `field/{create,update,delete}/`，并使公开 `database-*` artifact 名称与 Catalog 内部字段兼容；完成标准：目录、Catalog、CLI、测试和文档对同一规则给出一致结论；术语说明：Catalog 是程序读取的目录规则清单；验证状态：48 项本地行为测试、Python 编译、关键 CLI 查询、文档 profile、Skill 校验和本轮只读审查均已通过。图片资产决策：N/A。原因：本轮没有界面或视觉验收对象。证据：范围只包含本地规则与命令行行为。

## 发现

- P2：`package-structure-rules/scripts/placement_catalog.py` 已达 793 行，本周期继续增加数据存储检查逻辑。当前实现通过复用 Catalog 条目避免新增平行事实源，但检查、命令分发与清单解析仍集中在同一 CLI 文件。该项不影响本周期的公开行为或验收结果，且拆分会扩大本周期的公开接口回归面；后续仅在该文件继续增长并需要新增独立检查域时，再在独立周期提取内部模块，保持 `placement_catalog.py` 的 CLI 接口不变。

## 未发现阻断项

- 根 `utils/` 不含直接文件规则，源码根 `util/` 与业务域 `rpc/` 均为扁平语言源码文件入口，职责没有重叠。
- `utils/ip/` 只承载通用 IP 技术处理和归属查询适配，未吸收代理信任、风控、业务黑白名单或地域策略。
- 三类项目根的五个必需治理文件均以 Catalog 文件节点登记并直接渲染于项目根；`init` 创建 `AGENTS.md` 与 `CLAUDE.md` 在内的五个必需文件，未启用时不创建 `PROJECT_STYLE.md`，且不改写任何正文。
- `CLAUDE.md` 声明 `content_must_match: AGENTS.md`；strict 只读拒绝两份正文漂移，`bootstrap_agents.sh --target both` 以 `AGENTS.md` 为唯一同步源且重复执行保持一致。
- `database/connection/` 只承载数据存储连接与客户端初始化；`database/model/` 仅允许 `db/`、`redis/`、`mongo/` 子目录；五个 SQL 叶子目录均只接受直接 `.sql` 文件。
- Java 物理映射已从被禁止的 `database/model/` 根修正为 `database/model/db/`，并同时列出 Redis 与 Mongo 对应叶子目录；回归测试覆盖这三个路径。
- 公开 `database-connection`、`database-sql`、`database-migration` 查询名称均可返回唯一 Catalog 条目，不要求调用方了解内部下划线命名。
- Catalog、`query`、`init`、`render`、strict/legacy `check`、确定性隔离脚本和 CodeGraph fixture 都指向同一条 RPC 边界。
- JSON fixture 覆盖成功、解析失败、校验失败和业务失败；失败响应不会跨域泄漏异常、实体或仓储模型。
- adoption 只承认固定位置、唯一 Catalog ID、已存在目录和历史快照；禁止路径、目录交叠、路径越界和新增遗留源码均不能被清单放行。
- 本轮代码函数均含 `[参数]`、`[返回]`、最近修改时间和就近步骤注释；没有新增结构体字面量或配置映射位点。
- `placement_catalog.py` 仍保持单一公开 CLI；数据存储检查只复用 Catalog 声明的目录与扩展名，没有新增平行规则源或多余抽象。

## 审查结论

- 审查结论: 通过
- 审查范围: `package-structure-rules/`、`micro-business-architecture-rules/`、V2 测试 fixture 与文档、Skill 字典和项目四件套中的本轮增量。
- 是否允许提交: 否（当前轮没有 Git 提交授权；审查本身未发现 P0/P1）。
- 阻断问题: 无

## 通过标准

- 目录职责、Catalog、CLI、隔离门禁、测试和文档必须对同一规则给出一致结论，且审查中不存在 P0/P1。

## 已覆盖

- Diff 范围: 目录规则、机器 Catalog、公开 CLI、微业务隔离脚本、相邻规则引用、测试和研发文档；未修改真实业务项目。
- 已读规则: `package-structure-rules`、`micro-business-architecture-rules`、注释、实现审查、测试、当前改动审查与最终验收规则。
- 已联动 Skill: `comment-placement-granularity-rules`、`comment-completion-gate-rules`、`implementation-review-rules`、`functional-validation-rules`、`project-change-review-rules`。
- 已执行命令: 48 项本地 `unittest`、`py_compile`、数据存储和既有关键 `query`、render、init、strict、Git Bash bootstrap、普通 YAML adoption 临时 fixture、文档 profile、目标 Skill 校验与 `git diff --check`。

## 未覆盖/剩余风险

- 未运行测试: 无；本轮公开行为均由本地临时 fixture 覆盖。
- 未展开文件: 工作树存在用户和其它会话的无关改动，未纳入本次审查。
- 仍需确认: `placement_catalog.py` 的内部拆分时机为 P2 可维护性风险，不影响本周期放行；浏览器、第三方与真实基础设施验证均不适用，依据 `BOUND-PSR-003`。

## 执行附录

- `python -m unittest discover -s doc/5-tests/2026-07-28_014412_代码位置目录规则V2/package-structure-rules -p "test_*.py"`：48 项通过。
- `query --artifact database-connection`、`query --artifact database-sql --category field --operation delete`、`query --artifact database-migration --category field --operation create`：均唯一返回规范路径。
- `python -m py_compile package-structure-rules/scripts/placement_catalog.py micro-business-architecture-rules/scripts/micro_business.py doc/5-tests/2026-07-28_014412_代码位置目录规则V2/package-structure-rules/test_business_rpc.py`：通过。
- `query --artifact business-rpc`、`query --artifact utils --category discovery --technology polaris`、`query --artifact utils --category ip`、`query --artifact source-util --language go`、三类 `query --artifact project-governance --category claude`：均唯一返回规范路径。
- `bootstrap_agents.sh --target both`：临时样本中以 `AGENTS.md` 覆盖同步 `CLAUDE.md`，两次执行后字节一致。
- `check --policy adoption --adoption-manifest doc/1-架构/3-目录规则收敛清单.yaml`：已采纳路径与遗留快照通过，新增遗留源码、目录、嵌套遗留根和无效清单失败，哈希不变。
- Windows Python 3.14 的五个文档 profile 与 `python -X utf8 -B .system/skill-creator/scripts/quick_validate.py` 两个目标 Skill：均通过。

## 追踪附录

- 需求与决策：`REQ-PSR-UTILS-001`、`REQ-PSR-SOURCE-UTIL-002`、`REQ-PSR-CLI-003`、`REQ-PSR-IP-001`、`REQ-PSR-GOVERNANCE-001`、`REQ-PSR-CLAUDE-001`、`REQ-PSR-BUSINESS-RPC-004`、`REQ-PSR-ADOPT-001`、`REQ-PSR-DATABASE-002`、`RULE-PSR-DATABASE-003`、`DEC-PSR-IP-001`、`DEC-PSR-GOVERNANCE-001`、`DEC-PSR-CLAUDE-006`、`DEC-PSR-RPC-003`、`DEC-PSR-RPC-004`、`DEC-PSR-ADOPT-001`。
- 实施与测试：`TASK-05-01` 至 `TASK-13-03`、`TEST-PSR-UTILS-001` 至 `TEST-PSR-UTILS-004`、`TEST-PSR-IP-001`、`TEST-PSR-GOVERNANCE-001`、`TEST-PSR-CLAUDE-001`、`TEST-PSR-RPC-001` 至 `TEST-PSR-RPC-003`、`TEST-PSR-ADOPT-001`、`TEST-PSR-DATABASE-001` 至 `TEST-PSR-DATABASE-003`。
- 关键实现：`placement_catalog.py:check_path`、`placement_catalog.py:load_adoption_manifest`、`placement_catalog.py:check_adoption_path`、`placement_catalog.py:check_business_rpc_path`、`micro_business.py:check_isolation`。
