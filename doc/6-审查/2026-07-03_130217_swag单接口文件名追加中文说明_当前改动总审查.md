# swag 单接口文件名追加中文说明当前改动总审查

审查结论: 通过

审查范围: `swag-openapi-maintainer-rules/SKILL.md`、`swag-openapi-maintainer-rules/references/naming-rules.md`、`swag-openapi-maintainer-rules/references/discovery-rules.md`、`swag-openapi-maintainer-rules/references/description-rules.md`、`swag-openapi-maintainer-rules/references/validation-rules.md`、`swag-openapi-maintainer-rules/scripts/validate_openapi_yaml.py`、`PROJECT_MEMORY.md`、`PROJECT_STYLE.md`

是否允许提交: 是

阻断问题: 无

## 审查摘要

- 已把单接口 YAML 文件名从“纯路径名”升级为“路径名 + 中文简要说明”规则。
- 已明确中文简要说明来源、字符清洗、长度截断和 `summary_source` 回退留痕。
- 已要求 manifest 同步记录 `summary`、`summary_source` 和新文件名，支撑增量刷新与旧文件清理。
- 已把校验脚本扩展到文件名规则、manifest 一致性和 `summary_source` 检查，避免只改文档不改收口。

## 验证记录

- `quick_validate.py swag-openapi-maintainer-rules`：已实际执行，通过。
- `python -m py_compile swag-openapi-maintainer-rules/scripts/validate_openapi_yaml.py`：已实际执行，通过。
- `git diff --check`：已实际执行，通过；仅 `skill-dictionary/data.js` 存在 CRLF 提示，不阻断本次改动。
- 最小样例校验：已实际执行。
  - 合法样例通过：`orders_history_订单历史.yaml` 命名和 manifest 映射校验通过。
  - 非法样例已正确阻断：文件名未带中文后缀、单接口 `tags`、header 缺中文说明、响应 schema 缺中文说明。
  - `summary_source: unresolved` 回退样例通过：允许回退纯路径文件名，并要求 manifest 显式留痕。
