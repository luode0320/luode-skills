# subagent 并发上限与空闲回收当前改动审查

- 对应需求文档: 无正式需求文档；来源为对话内确认的功能改进决定，已在 Plan Mode 中形成正式实施计划并获用户批准（计划文件：`C:\Users\luode\.claude\plans\crispy-scribbling-puffin.md`）
- 对应实施总览: 未单独落盘 `doc/3-实施/` 文档（任务体量小，实施计划已在 Plan Mode 计划文件中完整承载，本轮为其真实执行记录）
- 对应验收标准: 内嵌于实施计划的最小任务清单完成条件中，无独立验收标准文档
- 对应最终验收: 待用户确认后另行处理
- 对应测试记录: 无独立 `doc/5-tests/` 目录；真实测试记录见下方“变更摘要”与“验证证据”
- 审查时间: `2026-07-04`

## 审查范围

- `subagent-dispatch-rules/SKILL.md`
- `subagent-dispatch-rules/references/delegation-decision-matrix.md`
- `subagent-dispatch-rules/references/launch-plan-schema.md`
- `subagent-dispatch-rules/scripts/generate_subagent_plan.py`
- `subagent-dispatch-rules/references/examples/launch-plan-output.json`（更新）
- `subagent-dispatch-rules/references/examples/launch-plan-input-batched.json`（新增）
- `subagent-dispatch-rules/references/examples/launch-plan-output-batched.json`（新增）
- `skill-dictionary/data.js`、`字典.md`（生成产物刷新）

## 变更摘要

| 类型 | 内容 |
| --- | --- |
| 策略规则 | `SKILL.md` 新增“并发上限与空闲回收（强制）”章节：同一会话活跃 subagent 数上限 5，超限排队；定义结果已收回/判定放弃/用户要求取消三类空闲信号必须立即 `close_agent`。 |
| 主流程联动 | `SKILL.md`“进入后先做什么”“主 agent 可见公告”“真实启动证据”“执行通过/驳回标准”四处同步补充并发上限与回收要求。 |
| 决策矩阵 | `delegation-decision-matrix.md` 判定顺序新增并发上限检查，并新增小节说明“排队”与“不可委派”的区别，避免与 SKILL.md 口径重复漂移。 |
| 脚本能力 | `generate_subagent_plan.py` 新增 `MAX_CONCURRENT_AGENTS=5` 常量与批次拆分逻辑，输出新增 `max_concurrent`/`batch_count`/`threads[*].batch_index` 字段。 |
| 示例回归 | 更新既有 2 线程示例输出，新增 7 线程批次示例输入输出，验证跨批切分正确。 |
| 字典刷新 | 因 `SKILL.md` 新增 `##` 标题，重跑生成脚本刷新 `skill-dictionary/data.js`、`字典.md`。 |

## 审查结论

发现:
- 未发现 P0/P1 阻断项。
- [P3] `PROJECT_MEMORY.md`（未在本轮改动范围内）- 该文件已记录 `generate_subagent_plan.py` 的既有行为（第 119/121/287 行），但未提及本轮新增的并发上限 5、`batch_index` 等参数。
  证据: `grep -n "generate_subagent_plan" PROJECT_MEMORY.md` 命中 3 处，均未提新字段。
  风险: 长期记忆与脚本当前能力存在轻微滞后，非阻断，不影响当前功能正确性。
  建议: 后续单独一轮按 `project-memory-rules` 增量补充，不在本轮范围内处理（本轮 `PROJECT_MEMORY.md` 已存在未提交的其他改动，不应借本次提交夹带处理，避免与其他任务边界混淆）。
  关联 skill: `project-memory-rules`

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 需求边界 | 通过 | 改动全部落在 `subagent-dispatch-rules/` 与 `skill-dictionary/` 生成产物，未触及 `parallel-task-dispatch-rules` 等计划外文件，符合已批准实施计划的“明确不在范围”。 |
| 缺陷风险 | 通过 | `batch_index = index // MAX_CONCURRENT_AGENTS + 1` 在 threads 已校验非空的前提下无除零风险；`batch_count` 用 `math.ceil` 计算，已用 2 线程与 7 线程真实运行验证边界正确（5/2 分批）。 |
| 遗漏风险 | 通过 | SKILL.md、决策矩阵、schema 文档、脚本、示例五处字段口径已交叉核对一致；已额外检查 `PROJECT_MEMORY.md`/`README.md`/`编码skill.md` 对脚本的既有描述均为概述性文字，未因新字段产生错误陈述（见上方 P3）。 |
| 安全风险 | 通过 | 无外部输入解析、无敏感信息、无新依赖；仅新增标准库 `math`。 |
| 重复逻辑 | 通过 | 复用既有 `compact_text`/`normalize_string_list`/`normalize_thread`，未新增重复工具函数。 |
| skill 合规（注释双闸门） | 通过 | `build_plan` 为本轮唯一修改的函数，已补齐 `[参数]`/`[返回]`/`最近修改时间`（含改动原因）及方法体 `1.`-`6.` 步骤编号；新增常量 `MAX_CONCURRENT_AGENTS` 已补一行来源说明注释；本轮无补丁/兜底分支，补丁位点 0 个；`data.js`/`字典.md` 为生成产物，不适用注释门禁。 |
| 实现质量（`implementation-review-rules` 最低测试前收口） | 通过 | `python -m py_compile` 通过；`import math` 已实际使用，无未用导入；文件位置未变，仍在 `subagent-dispatch-rules/scripts/` 原目录。 |
| 真实运行验证 | 通过 | 对 2 线程与新增 7 线程样例各真实运行一次 `generate_subagent_plan.py`，输出与仓库保存的期望 JSON 逐字段 diff 一致（忽略必然变化的 `generated_at`）；`skill-dictionary/generate_dictionary.py` 真实运行后确认 `data.js` 收录新章节标题 2 处。 |
| 用户改动保护 | 通过 | 会话开始前工作树已存在的 `AGENTS.md`/`CLAUDE.md`/`PROJECT_MEMORY.md`/`PROJECT_STYLE.md`/`README.md`/`implementation-planning-rules/*`/`project-agents-bootstrap/*`/`vercel-react-best-practices/AGENTS.md` 等改动，本轮未触碰、未回退，仅在其基础上新增独立提交范围。 |

## 风险与注意事项

- 并发上限是主 agent 自律约束，当前平台工具集不提供“查询活跃 agent 数”的强制拦截能力；已在 `SKILL.md` 中明确写出该限制，避免被误解为工具硬限制。
- 三类空闲信号无固定超时数字，判定仍带一定主观性；若后续发现判定过宽或过严，需要用户再给出明确阈值。
- `PROJECT_MEMORY.md` 尚未同步新字段（见上方 P3），留作后续独立任务处理。

## 审查状态

- 审查结论: 通过
- 审查范围: `subagent-dispatch-rules/SKILL.md`、`subagent-dispatch-rules/references/delegation-decision-matrix.md`、`subagent-dispatch-rules/references/launch-plan-schema.md`、`subagent-dispatch-rules/scripts/generate_subagent_plan.py`、`subagent-dispatch-rules/references/examples/launch-plan-output.json`、`subagent-dispatch-rules/references/examples/launch-plan-input-batched.json`、`subagent-dispatch-rules/references/examples/launch-plan-output-batched.json`、`skill-dictionary/data.js`、`字典.md`
- 是否允许提交: 是
- 阻断问题: 无

本轮改动可以进入最终收口；`PROJECT_MEMORY.md` 补充新字段作为独立后续事项，不阻断本次提交。
