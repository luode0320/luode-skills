---
reader_level: business_general
writing_style: plain_chinese
appendix_policy: preserve_existing_or_one_terminal_appendix
review_acceptance_gates:
  - stage: review
    applicability: applicable
    reason: 本轮新增和修改了 skill 规则、校验脚本与测试资产。
    basis: implementation-review-rules 与 project-change-review-rules 的测试前审查要求。
    required_by_source: true
    required_now: true
    completed_validation:
      - 本审查文档
      - git diff --check
      - Python 编译
    substitute_validation: []
    manual_follow_up: N/A
    pass_standard: 无 P0/P1 阻断且改动位点注释、目录、编码和语法检查通过。
  - stage: functional_validation
    applicability: applicable
    reason: 校验脚本行为发生变化，必须验证输入输出和退出码。
    basis: 七个离线 fixture 用例。
    required_by_source: true
    required_now: true
    completed_validation:
      - 7/7 离线用例
    substitute_validation: []
    manual_follow_up: N/A
    pass_standard: 七个用例按预期结果通过。
  - stage: third_party
    applicability: not_applicable
    reason: 本轮只升级文档生成规则和离线校验，不连接真实第三方服务。
    basis: 计划明确禁止联网抓取和业务项目真实生成。
    required_by_source: false
    required_now: false
    completed_validation: []
    substitute_validation:
      - 离线上游 manifest fixture
    manual_follow_up: N/A
    pass_standard: N/A
---

# 需求-swag第三方接口文档能力升级实现自审

结论：任务 1-4 的实现、离线功能验证、字典刷新和项目记忆同步均已完成；影响：规则、校验脚本和交接资产已具备可复用证据；范围：本轮改动文件与七个离线用例；非范围：真实业务项目生成、联网第三方验证和 Git 提交；变化：新增 B1 上游目录与树级校验；完成标准：实现自审无 P0/P1 且测试、字典和记忆证据可回溯；术语说明：B1 表示每个上游服务独立维护一套 OpenAPI 资产；验证状态：已完成本地真实运行验证。

任务 1-4 的实现、离线功能验证、字典刷新和项目记忆同步已完成。当前不允许提交 Git，原因是本轮计划明确不提交且用户未在当前消息授权提交。

审查结论: 通过
审查范围: `swag-openapi-maintainer-rules/SKILL.md`、5 个 references、新增 `references/third-party-discovery-rules.md`、`scripts/validate_openapi_yaml.py`、`doc/5-tests/2026-07-14_121425/` 测试资产
是否允许提交: 否（计划明确不提交）
阻断问题: 无

## 主要结论

- 上游接口契约已冻结为 B1：每个 `swag/<vendor-slug>/` 独立维护 `openapi.yaml`、manifest 和单接口 YAML。
- `source_type: upstream`、`upstream`、`base_url`、`coverage`、`source_client_file`、`discovery_confidence` 已在规则与脚本中一致落地。
- 根目录与上游目录校验隔离，根 `valid` 不被上游字段错误污染；命令退出码仍能反映任一已识别上游 scope 失败。
- 裸文件名守卫覆盖 `/`、`\\`、`..`，陌生目录按 YAML 存在性进入 `tree_warnings`，无 YAML 目录静默跳过。
- 测试资产位于当天时间戳目录，中文说明目录只放 README，Python 资产位于 ASCII 镜像目录；没有修改生产业务项目。

## 检查证据

### 格式、语法与编码

- Python 语法检查：`validate_openapi_yaml.py`、`fixtures_builder.py`、`validate_openapi_yaml_third_party_test.py` 均已通过 AST/运行入口检查。
- UTF-8：规则文档、Python 脚本和测试 README 均以 UTF-8 回读，中文显示正常。
- `git diff --check`：通过。
- 未引入数据库、网络、业务服务连接或真实密钥。

### 功能路径

| 路径 / 入口 | 触发方式 | 验证方式 | 状态 |
| --- | --- | --- | --- |
| `python .../validate_openapi_yaml_third_party_test.py` | 运行离线测试主程序 | 7 个正反例、退出码、JSON、错误 scope 和 warning 断言 | 已真实运行验证 |
| `python .../validate_openapi_yaml.py --swag-dir <fixture>/swag` | CLI 校验真实入口 | 测试主程序为每个用例调用 CLI 并解析 stdout JSON | 已真实运行验证 |
| `validate_swag_dir(swag_dir)` | 兼容旧单目录入口 | 无上游 fixture 与旧返回结果逐键比对 | 已真实运行验证 |
| `python skill-dictionary/generate_dictionary.py` | 任务 4 字典同步 | 复核 84 个已实现 skill、0 个缺失计划项及新增 reference | 已真实运行验证 |

## 注释双闸门核对

### 函数注释核对清单

| 文件 | 函数 / 方法 | `[参数]` | `[返回]` | 最近修改时间 | 改动原因 | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| `swag-openapi-maintainer-rules/scripts/validate_openapi_yaml.py` | `validate_swag_dir` | 有 | 有 | 有 | 有 | 通过 |
| `swag-openapi-maintainer-rules/scripts/validate_openapi_yaml.py` | `validate_swag_tree` | 有 | 有 | 有 | 有 | 通过 |
| `swag-openapi-maintainer-rules/scripts/validate_openapi_yaml.py` | `main` | 有 | 有 | 有 | 有 | 通过 |
| `doc/5-tests/.../fixtures_builder.py` | 6 个构造函数 | 有 | 有 | 有 | 有 | 通过 |
| `doc/5-tests/.../validate_openapi_yaml_third_party_test.py` | 10 个测试/辅助函数 | 有 | 有 | 有 | 有 | 通过 |

### 步骤与补丁注释核对

- 树级校验、scope 元数据校验、裸文件名守卫和 JSON/stderr 日志均有就近中文步骤或原因注释。
- fixture 构造和 CLI 测试入口均有步骤编号与中文原因注释。
- 本轮无结构体字面量、第三方 SDK 参数对象或生产配置字段映射位点；字段/结构体字面量注释位点 0 个。
- 本轮无未解释的临时 debug 日志；过程日志属于测试程序验收要求，并输出到可观察位置。

## 目录、依赖与最小改动审查

- 未修改 `api-swagger-rules`、`api-request-rules`、业务项目或上线测试基线双索引。
- 未新增公共工具或重复封装；测试辅助仅存在于当前测试时间戳目录。
- 未涉及 Go、数据库、HTTP 服务启动、浏览器联调或生产配置。
- 校验脚本当前共 555 行，超过 500 行阈值。按本轮计划的最小改动边界，本轮保留单入口以避免无关拆分；后续若继续增加校验域，应将树级编排与单目录校验拆为独立模块。本风险不影响本轮功能验收。

## 审查剩余风险

- `source_type` 曾在计划草稿中出现两种写法；本轮以计划总览和 manifest 样例的 `upstream` 为唯一实现口径，规则、脚本和 fixture 已一致。
- 当前仅验证离线 fixture，不代表在真实业务项目中已发现并生成所有上游接口；真实业务项目生成不在本轮范围。
