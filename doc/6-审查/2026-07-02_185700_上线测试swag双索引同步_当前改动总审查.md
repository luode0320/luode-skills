# 上线测试 swag 双索引同步当前改动总审查

审查结论: 通过

审查范围: `project-release-test-rules/SKILL.md`、`project-release-test-rules/references/openapi-inventory-sync-rules.md`、`project-release-test-rules/references/interface-inventory-schema.md`、`project-release-test-rules/references/output-artifacts.md`、`project-release-test-rules/references/report-format.md`、`project-release-test-rules/references/test-data-construction-rules.md`、`project-release-test-rules/references/reusable-script-toolbox.md`、`project-release-test-rules/scripts/generate_release_test_plan.py`、`swag-openapi-maintainer-rules/SKILL.md`、`PROJECT_MEMORY.md`、`PROJECT_STYLE.md`

是否允许提交: 是

阻断问题: 无

## 审查摘要

- 已将 `swag/.swag-manifest.yaml` 与 `doc/5-tests/基线/interface-inventory.yaml` 定义为当前代码接口事实的双索引。
- 已补充三方对账规则：当前代码扫描结果、swag manifest、interface inventory 的 `HTTP method + path` 必须一致。
- 已补充 `interface-sync-report.yaml` 产物要求和 `sync-interface-contract-assets` 通用脚本入口。
- 已补充 OpenAPI 示例作为低优先级参数候选来源，且明确不能替代真实业务数据复验。

## 风险核对

- P0: 未发现。
- P1: 未发现。
- P2: 新增同步脚本依赖项目路由扫描能力，复杂框架下仍可能需要项目适配；规则已要求当前代码为真相源，且同步报告会标记待刷新或待确认。

## 验证记录

- `python -m py_compile project-release-test-rules/scripts/generate_release_test_plan.py`: 通过。
- `generate_release_test_plan.py --help`: 通过，已出现 `sync-interface-contract-assets` 子命令。
- `sync-interface-contract-assets` 最小 smoke test: 通过，能识别 schema 漂移并将可复用参数标记为 `stale`。
- `sync-interface-contract-assets` 缺失 manifest smoke test: 通过，能输出 `missing_manifest: true` 与 `requires_dual_refresh: true`。
