---
schema_version: 1
doc_id: REVIEW-PMW-001
doc_type: review
source_ids:
  - BUG-PLAN-WAIT-20260726-001
  - IMP-PMW-001
  - CYCLE-PMW-01
  - TASK-PMW-03
status: accepted
version: v1.0
current_slice: TASK-PMW-03 总结闸门与专项回归
updated_at: 2026-07-26 06:00:00
reader_level: business_general
writing_style: plain_chinese
appendix_policy: preserve_existing_or_one_terminal_appendix
review_acceptance_gates:
  - stage: review
    applicability: applicable
    reason: 本轮修改总结 Owner 与专项行为测试，必须确认未决状态、空返回和总结消费方的实现边界。
    basis: RULE-PMW-001..004、AC-PMW-001..007、TASK-PMW-03
    required_by_source: true
    required_now: true
    completed_validation:
      - 专项行为回归 10/10
      - Python AST 与 UTF-8 回读
      - Skill quick_validate
      - 当前测试资产和规则差异检查
    substitute_validation: []
    manual_follow_up: N/A
    pass_standard: 空/缺失返回保持 WAITING_DECISION 并串行重发；所有终态忽略陈旧返回；总结消费方拒绝冻结集合内全部可见输出。
  - stage: functional_validation
    applicability: limited
    reason: 规则层和本地状态模型已验证，但真实 Desktop 选择框尚未完成两个以上空答案周期及最终选择恢复。
    basis: AC-PMW-005、TEST-PMW-013、宿主可见链路尚无可回放证据。
    required_by_source: true
    required_now: true
    completed_validation:
      - 本地专项行为回归
    substitute_validation:
      - 纯本地状态机与消费方行为模型
    manual_follow_up: 在更新后的 Plan Mode 宿主中跨越两个以上空答案周期，再在当前活动选择框提交完整选择。
    pass_standard: 每次宿主空答案立即重发相同未决选择框，等待期间无总结，最终选择后才继续。
  - stage: browser_integration
    applicability: limited
    reason: 本轮不修改 Desktop 产品源码，但真实宿主选择框仍是用户可见验收所需环境。
    basis: BUG-PLAN-WAIT-20260726-001 的真实 Desktop 验收安排。
    required_by_source: true
    required_now: true
    completed_validation: []
    substitute_validation:
      - 本地状态机回归
    manual_follow_up: 使用真实 Plan Mode 宿主补做两个空答案周期和最终选择恢复。
    pass_standard: 用户可见链路与本地契约一致，且无并存选择框。
  - stage: third_party
    applicability: not_applicable
    reason: 本轮只修改 Skill、测试和文档，不调用第三方接口或业务服务。
    basis: Bug 范围明确排除数据库、HTTP/RPC 上游和其它外部服务。
    required_by_source: false
    required_now: false
    completed_validation: []
    substitute_validation: []
    manual_follow_up: N/A
    pass_standard: N/A
---

# Plan Mode 永久等待实现审查

结论：规则与本地行为模型已达到进入最终验收的实现质量要求；影响：空/缺失返回不会再被测试模型误判为宿主故障，所有终态不会因陈旧返回重发，冻结输出集合在未决状态下均不会写入；范围：专项测试状态机、总结消费方、推荐标记、活动选择框和输出闸门文案；非范围：Codex Desktop 产品源码与真实宿主联调；变化：补齐了审查发现的测试缺口并保持最小修改；完成标准：实现层无 P0/P1，自动回归和基础静态检查可复核；术语说明：活动选择框指宿主当前可交互的唯一决策调用；验证状态：实现审查通过，真实 Desktop 仍为 LIMITED。

## 审查结论

- 审查结论: 通过
- 审查范围: `doc/5-tests/2026-07-26_040607/plan_mode_wait_loop/test_plan_mode_wait_loop.py`、`implementation-planning-rules/references/plan-output-gate.md`、同轮专项 README，以及相关规则/项目状态改动。
- 是否允许提交: 否（当前轮未获得 Git 提交授权）
- 阻断问题: 无 P0/P1。
- 图片资产决策: N/A；原因：本轮只交付规则、测试和文档；证据：没有 Markdown 图片引用，状态与时序由文字和状态模型表达。

## 完成标准

实现层完成标准为：空/缺失返回保持 `WAITING_DECISION` 并重发，停止/阻断/已解决/已代选状态均不因陈旧返回重发，单活动框与推荐标记均有断言，未决状态不写入 `commentary`、`limited_plan`、`pending_summary`、`proposed_plan`、`final`、`summary`、`final_answer`、`task_complete`、`result_and_conclusion` 或中文“结果与结论”；以上均已由本地回归与静态检查证明。真实 Desktop 仍按 `TEST-PMW-013` 单独验收。

## 发现与处理

- 先前审查指出的 `None`、空对象和 `answers: null` 分支已统一回到 `WAITING_DECISION` 并立即串行重发；`tool_error` 仍单独进入 `HOST_BLOCKED`。
- 总结测试改为调用 `emit_output` 消费方，拒绝时不写入 `state.outputs`；历史 fixture 改为真实回放并断言消费方拒绝。
- 输出消费闸门扩展为完整冻结集合，历史回放逐类验证空答案后均被拒绝，避免 `commentary` 或计划包裹绕过总结闸门。
- `Question.recommended_option`、`DecisionState.active_dialogs` 和重发辅助函数已建模推荐标记与单活动框不变量。
- `STOPPED` 增加终态短路，停止后收到空答案不会新增选择框。
- `HOST_BLOCKED`、`DECISION_RESOLVED` 和 `USER_DELEGATED` 同样增加陈旧返回短路；`plan-output-gate.md` 已明确完整冻结输出集合。

## 已覆盖检查

- 最小改动：仅触达本 Bug 规则、总结闸门、专项测试、项目文档和审查/验收证据，无生产产品源码变更。
- 可读性与职责：状态迁移、重发、输出闸门和历史回放职责分离；未引入新依赖、网络调用或后台定时器。
- 错误与边界：空返回、缺失答案、未知问题 ID、部分答案、明确授权、明确停止、工具故障、重复停止和全部终态陈旧返回均有断言。
- 编码与语法：UTF-8 回读通过；Python AST 通过；`git diff --check` 退出码为 0。
- 目录与测试资产：测试脚本和 fixture 位于同一时间戳目录下的 ASCII 子目录，不污染生产目录。
- 安全与范围：未记录密钥、token、私有输入或外部响应；不连接数据库、缓存、消息队列或非 local 服务。

## 函数注释核对清单

- [通过] 文件: `doc/5-tests/2026-07-26_040607/plan_mode_wait_loop/test_plan_mode_wait_loop.py` | 函数: `DecisionState.pending_questions` | [参数]: 是 | [返回]: 是 | 最近修改时间: 是 | 改动原因: 是
- [通过] 文件: 同上 | 函数: `assert_true`、`read_text`、`build_decision_call`、`validate_decision_call` | [参数]: 是 | [返回]: 是 | 最近修改时间: 是 | 改动原因: 是
- [通过] 文件: 同上 | 函数: `resend_decision_call`、`is_directive`、`process_tool_return` | [参数]: 是 | [返回]: 是 | 最近修改时间: 是 | 改动原因: 是
- [通过] 文件: 同上 | 函数: `allow_final_summary`、`emit_output`、`history_allows_final_summary`、`new_state` | [参数]: 是 | [返回]: 是 | 最近修改时间: 是 | 改动原因: 是
- [通过] 文件: 同上 | 函数: `test_no_auto_resolution_field`、`test_empty_answers_are_unbounded`、`test_resend_preserves_questions`、`test_partial_answers_are_preserved`、`test_delayed_choice_resolves` | [参数]: 是 | [返回]: 是 | 最近修改时间: 是 | 改动原因: 是
- [通过] 文件: 同上 | 函数: `test_explicit_delegation_and_stop`、`test_host_fault_blocks_summary`、`test_waiting_state_rejects_final`、`test_historical_empty_then_summary_is_rejected`、`test_rule_text_contract`、`main` | [参数]: 是 | [返回]: 是 | 最近修改时间: 是 | 改动原因: 是

## 字段/结构体字面量注释核对清单

- [通过] 文件: 同上 | 结构体: `Question` | 字段范围: 问题 ID、文案、选项、推荐选项 | 跨层映射/关键配置: 是，承载重发冻结载荷 | 逐字段/字段组中文说明: 是 | 裸露关键字段: 否
- [通过] 文件: 同上 | 结构体: `DecisionState` | 字段范围: 已答答案、状态、调用历史、输出、活动框数量 | 跨层映射/关键配置: 是，承载状态机不变量 | 逐字段/字段组中文说明: 是 | 裸露关键字段: 否
- [通过] 文件: 同上 | 对象: `build_decision_call` 的问题字典 | 字段范围: `id`、`question`、`options`、`recommended` | 跨层映射/关键配置: 是 | 逐字段/字段组中文说明: 是 | 裸露关键字段: 否

## 补丁注释核对清单

- [通过] 文件: 同上 | 函数: `process_tool_return` | 位点: 全部终态短路、空/缺失返回分支、活动框关闭 | 做了什么: 是 | 为什么要加: 是，防止陈旧返回重发和空答案错误终止
- [通过] 文件: 同上 | 函数: `resend_decision_call` | 位点: 单活动框断言与新调用创建 | 做了什么: 是 | 为什么要加: 是，防止并发选择框
- [通过] 文件: `emit_output` / `history_allows_final_summary` | 位点: 冻结输出集合拒绝与逐类历史回放 | 做了什么: 是 | 为什么要加: 是，防止可见中间输出绕过空答案闸门
- [通过] 文件: `implementation-planning-rules/references/plan-output-gate.md` | 位点: `WAITING_DECISION` 文案边界 | 做了什么: 是 | 为什么要加: 是，消除受限说明与循环禁言的歧义

## 受影响运行路径

1. 选择框仍打开：`OPEN_DIALOG` 只返回 `wait`，活动框数量保持 1。
2. 宿主返回 null/空对象/缺失答案：关闭旧框、保持 `WAITING_DECISION`、创建一个同载荷新框。
3. 部分答案：保存已答项，只发送剩余问题；全部答案后进入 `DECISION_RESOLVED`。
4. 明确停止：进入 `STOPPED`，后续任何返回均不再重发。
5. 总结消费：未决或阻断状态拒绝 `commentary`、`limited_plan`、`pending_summary`、`proposed_plan`、`final`、`summary`、`final_answer`、`task_complete`、`result_and_conclusion` 及中文“结果与结论”等冻结输出写入；已解决/已代选状态才放行计划收敛。

## 验证证据

| 检查 | 命令/入口 | 结果 |
| --- | --- | --- |
| 专项行为回归 | `python3 -X utf8 -B doc/5-tests/2026-07-26_040607/plan_mode_wait_loop/test_plan_mode_wait_loop.py` | 10/10 PASS |
| Skill 快速校验 | Windows Python 执行 `.system/skill-creator/scripts/quick_validate.py` 两个目标 Skill | 两个 `Skill is valid!` |
| AST | Python `ast.parse` 读取专项脚本 | PASS |
| YAML | Python `yaml.safe_load` 读取 `agents/openai.yaml` | PASS |
| 差异检查 | `git diff --check` | 退出码 0；仅有既有换行提示 |
| 工程文档、审查与验收 | 八份关联 profile | `valid: true`、`status: LIMITED`、`errors: []` |

## 限制与重验入口

真实 Desktop 选择框尚未取得两个以上空答案周期和最终选择恢复证据，因此本审查不把本地模型通过升级为用户可见功能 PASS。重验入口固定为 `CYCLE-PMW-01 / TASK-PMW-04 / TEST-PMW-013`；宿主不可用时保持 `LIMITED/HOST_BLOCKED`，不输出总结替代品。

## 验收结论

实现审查项通过；真实宿主项受限。当前结论允许继续进入受限最终验收，不允许把整体 Bug 标记为正式 PASS。

## 验证结论

本地自动回归、规则文本、AST、YAML、UTF-8 和差异检查均通过；真实 Desktop 未完成，验证状态为 LIMITED。

## 执行附录

- 未执行 Git commit、push、rebase、reset 或 checkout。
- Obsidian bridge 仍返回 `VAULT_NOT_REGISTERED`；未绕过为直接读取或写入 vault。
- WSL Python 缺 `PyYAML`，因此 Skill profile 改用本机 Windows Python 复验；专项行为回归使用纯标准库，未连接任何服务。

## 追踪附录

`RULE-PMW-001..004`、`AC-PMW-001..007` → `TASK-PMW-03` → `reasoning-summary-structure-rules/SKILL.md` 与专项测试 → `TEST-PMW-001..010` → 本审查 `REVIEW-PMW-001`；真实宿主证据仍由 `TEST-PMW-013` 承接。
