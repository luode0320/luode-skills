---
schema_version: 1
doc_id: "REVIEW-RTP-TIMEOUT-IMPL-001"
doc_type: "review"
source_ids: ["CHG-RTP-001", "REQ-RTP-006", "REQ-RTP-007", "CYCLE-RTP-05", "TASK-RTP-11", "TASK-RTP-12", "TASK-RTP-13"]
status: "accepted"
version: "v1.0"
current_slice: "TASK-RTP-13 实现审查"
updated_at: "2026-07-25 18:33:58"
reader_level: "business_general"
writing_style: "plain_chinese"
appendix_policy: "preserve_existing_or_one_terminal_appendix"
review_acceptance_gates:
  - stage: review
    applicability: applicable
    reason: 本轮修改任务投影规则、Python CLI 和测试，必须完成测试前实现审查。
    basis: CYCLE-RTP-05 与 TASK-RTP-13。
    required_by_source: true
    required_now: true
    completed_validation: ["EVD-TASK-RTP-11-REVIEW", "EVD-TASK-RTP-12-REVIEW", "EVD-TASK-RTP-13-REVIEW"]
    substitute_validation: []
    manual_follow_up: N/A
    pass_standard: 无 P0/P1，注释闸门通过，语法、引用、职责边界和真实行为测试均通过。
  - stage: third_party
    applicability: not_applicable
    reason: 本轮只验证本地文件和 Python 进程，不调用第三方业务接口。
    basis: REQ-RTP-006 与 REQ-RTP-007 的范围边界。
    required_by_source: false
    required_now: false
    completed_validation: []
    substitute_validation: []
    manual_follow_up: N/A
    pass_standard: N/A
---

# 超时任务悬浮窗实现审查

结论：实现审查通过；影响：简单任务可以先不显示悬浮窗，但主动执行严格超过十分钟后会被升级为可见任务列表；范围：超时规则、暂停扣减、精确与兜底投影、并发保护、命令行入口和自动化测试；非范围：Codex Desktop 产品代码、后台定时唤醒和 Git 提交；变化：新增受控超时升级入口，并补齐正式周期表的任务列解析；完成标准：无 P0/P1，57 项回归、语法编译和 Skill 校验全部通过；术语说明：精确投影是从唯一正式计划恢复真实任务，兜底投影是在证据不足时使用固定三步安全列表；验证状态：本地实现、测试与审查证据齐全，审查通过；图片资产决策：N/A，原因是审查对象只有规则、Python 脚本和文档，证据是本轮写集不含图片或界面资产。

## 审查结论

- 审查结论: 通过
- 审查范围: `task-plan-rehydration-rules` 的规则、契约、Python 脚本和测试，以及 `autonomous-execution-rules` 的相邻路由改动
- 是否允许提交: 否，当前轮没有 Git 写历史授权
- 阻断问题: 无

实现保持单一 Owner：计时、会话隔离、原子写入、exact/fallback 选择和 payload 都由 `task-plan-rehydration-rules` 负责；相邻 Skill 只负责在可执行检查点调用，不复制投影实现。

## 完成标准

- 严格边界为有效主动执行时间 `> 600` 秒；599 秒、600 秒及暂停扣减后不超过 600 秒均不升级。
- Plan Mode、等待用户、`blocked`、`manual_handoff` 四类暂停不计时。
- 当前会话已有活动投影或阻断 Goal 时不覆盖；失活投影可以由到期升级替换。
- exact 证据不足时固定使用三步 fallback；损坏 registry、非法时间、敏感字段和超限文件保持原字节不变。
- 同一文件锁覆盖活动检查、合成和条件写入，避免并发检查重复覆盖。

## 发现与处理

| 级别 | 发现 | 处理与结论 |
|---|---|---|
| P0 | 无 | 未发现数据破坏、安全越权或主流程不可用风险 |
| P1 | 无 | 会话隔离、严格时间边界、阻断 Goal 和失败不写入均有测试证据 |
| P2 | 正式周期表的任务 ID 位于第二列，旧解析器只读首列 | 已按表头定位“任务 ID”“唯一目标”，并补标准标题兼容测试，57 项回归通过 |
| P3 | `task_plan_projection.py` 已超过 500 行 | 本轮只做手术式兼容修复；已给出不改变公开 CLI 的可执行拆分方案，不扩大本轮重构范围 |

## 500 行文件拆分方案

该方案是实现审查要求的兼容性拆分依据，不属于本轮执行范围：

1. 将 schema、常量和纯校验函数移入 `task_plan_projection_schema.py`，原模块继续重导出公开符号。
2. 将 registry 读取、文件锁、原子写入、迁移和 Goal 状态迁移移入 `task_plan_projection_registry.py`。
3. 将 Markdown 任务提取、exact/fallback 合成和超时升级移入 `task_plan_projection_synthesis.py`。
4. 原 `task_plan_projection.py` 保留兼容导出与 CLI 分发；每一步先跑现有 57 项测试，禁止一次性搬迁后统一验证。

## 注释与实现质量

- 注释放置核对：修改的 Markdown 小节提取、任务表解析和对应测试均在函数头保留 `[参数]`、`[返回]`、北京时间最近修改原因，并在分支入口使用就近编号注释。
- 字段/结构体字面量核对：N/A。原因是本轮没有新增跨层 DTO 或结构体字面量；JSON 投影字段由既有 schema 白名单和测试约束。
- 补丁位点核对：标准周期标题兼容分支明确说明“为什么要兼容”，没有把解释集中到远离代码的位置。
- 重复工具核对：复用既有 `_extract_markdown_section`、`_parse_markdown_table`、`synthesize_projection`、文件锁和原子写入，没有新增同语义公共工具。
- 七天冻结核对：本轮目标文件正在同一活动变更中持续修改，不属于“最近修改超过七天的高复用旧行为”直接改写场景。

## 执行附录

- `python -X utf8 -B -m py_compile task-plan-rehydration-rules/scripts/task_plan_projection.py task-plan-rehydration-rules/tests/test_task_plan_projection.py`：通过。
- `python -X utf8 -B task-plan-rehydration-rules/tests/test_task_plan_projection.py`：57 项通过。
- 两个受影响 Skill 的 `quick_validate.py`：通过。
- 真实运行验证状态：本地 Python API 与 CLI 子进程行为已真实执行；Desktop 产品级后台唤醒不在本轮承诺和范围内。

## 追踪附录

| 来源 | 实现 | 测试 | 审查证据 | 判定 |
|---|---|---|---|---|
| `REQ-RTP-006`、`RULE-RTP-007/008` | 严格时间与暂停扣减 | `TEST-RTP-009` | `EVD-TASK-RTP-12-REVIEW` | 通过 |
| `REQ-RTP-007`、`RULE-RTP-009/010` | exact/fallback、锁内写入与 payload | `TEST-RTP-010..012` | `EVD-TASK-RTP-13-REVIEW` | 通过 |
