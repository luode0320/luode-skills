# WSL 项目内文件引用路径规则当前改动总审查

审查结论: 通过
审查范围: WSL 路径引用规则、仓库自举模板、当前 AGENTS 受管章节、长期记忆/风格回写、skill 字典刷新产物
是否允许提交: 是
阻断问题: 无

## 发现

未发现 P0/P1 阻断项。

## 已覆盖

- `windows-wsl-execution-rules/SKILL.md`：已增加“面向用户输出的项目内文件引用”路径形式，明确 Windows 桌面访问 WSL 项目时使用 `\\wsl.localhost\<distro>\...`。
- `windows-wsl-execution-rules/references/path-mapping.md`、`recommended-workflow.md`、`command-templates.md`：已覆盖普通文本路径、Markdown 链接、审查证据路径、截图说明和最终总结路径。
- `project-agents-bootstrap/scripts/bootstrap_agents.sh` 与 `project-agents-bootstrap/SKILL.md`：已将规则写入自举模板，后续项目级规则同步会带上该口径。
- `AGENTS.md` 与 `vercel-react-best-practices/AGENTS.md`：自举脚本已同步已有规则文件中的 `Windows / WSL 执行规则` 章节。
- `PROJECT_MEMORY.md` 与 `PROJECT_STYLE.md`：已回写“项目内文件引用路径”事实和输出风格示例。
- `skill-dictionary/data.js` 与 `字典.md`：已通过字典生成脚本刷新。

## 验证证据

- `bash project-agents-bootstrap/scripts/bootstrap_agents.sh --repo .`：退出码 0，已同步根目录与子目录已有 `AGENTS.md`；过程中出现 `wsl: Processing /etc/fstab with mount -a failed.` 警告，但脚本仍完成受管章节同步。
- `python skill-dictionary/generate_dictionary.py`：退出码 0，输出 `implemented_total: 80`、`planned_missing: 0`、`seed_total: 23`。
- `bash -n project-agents-bootstrap/scripts/bootstrap_agents.sh`：退出码 0。
- `rg -n "用户可访问文件引用|项目内文件引用|Markdown 链接、普通文本路径|命令用运行环境路径" ...`：已在目标规则、模板、长期记忆、长期风格和当前 AGENTS 中检索到新口径。
- `codegraph sync; codegraph status`：索引同步完成，状态为 `[OK] Index is up to date`。

## 未覆盖/剩余风险

- 本轮未做运行时功能测试；该变更属于规则与文档模板改动，不影响可执行业务路径。
- 工作树中存在本轮开始前已出现的 URL 路由相关未跟踪资产和字典刷新连带变更；本审查只确认本轮 WSL 文件引用规则链路无阻断项。
- 自举脚本同步了部分既有受管章节的 UTF-8 写入规则差异；这些差异与当前路径引用目标相邻但不是本轮核心目标，后续若需收口应按对应 UTF-8 规则任务单独审查。

## 判定

本轮针对“项目在 WSL 且用户从 Windows 桌面访问时，所有面向用户的项目内文件引用都应使用可打开的 `\\wsl.localhost\...` 路径”的规则改动已落到主规则、reference、自举模板、当前仓库规则、长期记忆和长期风格中。未发现会阻断提交的 P0/P1 问题。
