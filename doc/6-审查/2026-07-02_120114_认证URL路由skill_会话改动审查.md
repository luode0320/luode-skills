# 认证 URL 路由 skill 会话改动审查

发现:
- [P1] authenticated-url-routing-rules/SKILL.md:100 - “正文为空”分支可能绕过安全策略拒绝
  证据: `页面标题可读但正文为空` 分支先把“安全策略限制”列为可能原因，随后允许“尝试一次截图或 DOM 快照”。
  风险: 当前已实测微信文档正文读取被浏览器安全策略拒绝，且策略禁止 workaround、raw CDP、alternate browser surfaces 等绕过；该分支会让后续 agent 在无法区分“纯懒加载为空”和“策略拒绝为空”时继续尝试截图或 DOM，可能违反同一安全边界。
  建议: 将该分支拆开：只有在没有出现安全策略拒绝信号、且页面状态只是懒加载 / iframe / canvas / 富文本渲染时，才允许一次截图或 DOM 快照；一旦命中安全策略拒绝，必须直接进入“已登录标签页可打开但正文读取被安全策略拒绝”分支并停手。
  关联 skill: authenticated-url-routing-rules

- [P1] 编码skill.md:616 - 新 URL 路由 skill 未并入主规划，主分流仍会把浏览器任务导向 `agent-browser`
  证据: `字典.md` 将 `authenticated-url-routing-rules` 标为“扩展种子”，核心职责仍是“当前已在仓库中，但尚未并入主规划域表”；`编码skill.md` 的测试域默认分流仍写“需要用真实浏览器打开页面、点击、输入、登录、截图、抓取页面数据或查看页面网络请求”时进入 `agent-browser`。
  风险: 用户要求的是“只要用户提供 URL，默认优先走 Chrome Plugin 复用真实 Chrome 登录态”。当前 skill 虽然存在，也能被当前会话命中，但主规划 / 字典仍把它当体系外 seed，且主分流没有把“用户提供 URL / 已登录网页 / 在线文档读取”先路由到该 skill；后续 agent 按主规划执行时仍可能先走 `agent-browser`，重现登录态丢失问题。
  建议: 把 `authenticated-url-routing-rules` 正式加入 `编码skill.md` 的主规划域，建议落在总控层或测试域浏览器分流前置位置，并明确优先级：用户提供 URL 且涉及打开 / 读取 / 分析 / 在线文档 / 已登录页面时，先进入 `authenticated-url-routing-rules`；只有该 skill 判定 Chrome Plugin 不可用或用户明确要求普通浏览器自动化时，才转给 `agent-browser`。修改后重新运行 `python skill-dictionary/generate_dictionary.py`。
  关联 skill: authenticated-url-routing-rules

- [P2] doc/6-审查/2026-07-02_114217_认证URL路由skill_当前改动总审查.md:11 - 现有审查报告范围小于当前工作树
  证据: 该审查文档只覆盖 `authenticated-url-routing-rules/`、`PROJECT_MEMORY.md`、`skill-dictionary/data.js`、`字典.md`、`doc/5-tests/2026-07-02_114217/`；但当前 `git status --short` 还显示 `AGENTS.md`、`PROJECT_STYLE.md`、`project-agents-bootstrap/`、`windows-encoding-rules/`、`windows-wsl-execution-rules/` 等修改。
  风险: 若把旧审查报告作为“当前工作树可提交”依据，会漏审一批仓库级规则和 Windows / WSL 执行规则改动；这些文件会影响全局 agent 行为，风险高于普通文档。
  建议: 提交前不要沿用 11:42 的审查报告作为全量放行证据；要么把本次提交范围限制在认证 URL 路由 skill 相关文件，要么为当前完整工作树另跑一次包含全部修改文件的总审查。
  关联 skill: project-change-review-rules

未发现阻断项:
- 不适用；本轮发现 P1，需要先修复或重新审查后再允许提交。

统一判定:
- 审查结论: 阻断
- 审查范围: `authenticated-url-routing-rules/`、`doc/5-tests/2026-07-02_114217/`、`doc/6-审查/2026-07-02_114217_认证URL路由skill_当前改动总审查.md`、`PROJECT_MEMORY.md`、`skill-dictionary/data.js`、`字典.md`、`编码skill.md` 相关主规划分流
- 是否允许提交: 否
- 阻断问题: P1 安全策略分支冲突；P1 新 URL 路由 skill 未并入主规划导致默认路由不稳定

已覆盖:
- 当前 diff 范围: 认证 URL 路由 skill、agents 元数据、测试证据、既有审查记录、字典生成产物、长期记忆相关变更。
- 已读规则: `AGENTS.md`、`skill-hit-check-rules`、`parallel-task-dispatch-rules`、`project-change-review-rules`、`skill-audit-rules`、`skill-compliance-gate-rules`、`reasoning-summary-structure-rules`、`artifact-delivery-gate-rules`。
- 已联动 skill: `project-change-review-rules`、`skill-audit-rules`、`skill-compliance-gate-rules`。
- 已执行命令: `git status --short`、`git diff --stat`、`git diff --name-only`、`git diff --check`、`python .system\skill-creator\scripts\quick_validate.py authenticated-url-routing-rules`、`python skill-dictionary\generate_dictionary.py`、`rg` 定位主规划和字典条目。

未覆盖/剩余风险:
- 当前工作树还有多份仓库级规则文件改动未展开逐行审查，包括 `AGENTS.md`、`PROJECT_STYLE.md`、`project-agents-bootstrap/`、`windows-encoding-rules/`、`windows-wsl-execution-rules/`。本报告只审查与“认证 URL 路由 skill”目标直接相关的改动。
- `skill-dictionary/data.js` 和 `字典.md` 为生成产物，只检查了新 skill 条目、统计变化和生成命令结果，未手工逐行审查完整生成物。

必要后续动作:
1. 修正 `authenticated-url-routing-rules/SKILL.md` 中“页面标题可读但正文为空”分支，避免在安全策略拒绝场景继续尝试截图或 DOM。
2. 将 `authenticated-url-routing-rules` 纳入 `编码skill.md` 主规划分流，并重新生成 `skill-dictionary/data.js` 与 `字典.md`。
3. 提交前重新执行当前改动总审查，确保审查范围与实际提交范围一致。
