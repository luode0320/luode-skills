---
schema_version: 1
template_version: 1
doc_id: "STYLE-FLATDOC-20260811-01"
doc_type: style_regression
source_ids:
  - "REQ-FLATDOC-20260811-001"
  - "CYCLE-FLATDOC-01"
status: accepted
version: "v1.0"
current_slice: "CYCLE-FLATDOC-05"
updated_at: "2026-08-11 14:45:07"
reader_level: business_general
writing_style: plain_chinese
appendix_policy: preserve_existing_or_one_terminal_appendix
---

# 6-review 风格回归：Bug 与测试文档扁平化

结论：本轮核对规则文档、引擎代码和新增测试的写法与归位；影响：不代替业务正确性判断和发布放行；范围：`artifact-storage-rules` 真相源、Bug 域与测试域 9 个 skill、上线接口测试子体系、收口闸门与仓库入口；非范围：`doc/4-bugs/` 与 `doc/5-tests/` 历史文件正文、`doc/5-tests/基线/` 数据资产、需求覆盖与发布放行；变化：Bug 与测试产物从「时间戳子目录 + README」改为单个扁平 md，上线接口测试机器产物移出 `doc/`；完成标准：STYLE: PASS；术语说明：「扁平 md」指 `YYYY-MM-DD_HHmmss_<中文主题>.md` 这种直接放在根目录下、不再套任务子目录的单文件；验证状态：新增 8 个用例全部通过，根 `test/` 逐目录全量回归改动前后失败数完全一致，历史零污染核验为空。

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 关联任务 | TASK-FLATDOC-01..05 |
| 关联真实测试 | TEST-FLATDOC-001..008 |
| 实现证据 | EVIDENCE-FLATDOC-01..05 |
| 检查时点 | 真实测试通过后 |
| 检查人 | Claude Opus 5 |

图片资产决策：N/A + 原因 + 证据。原因：本轮是规则文本、术语和路径落点的改动，没有 UI、截图、原型或外观基线需要视觉留证；证据：改动清单里的 83 个文件全部是 Markdown、YAML 和 Python，`git status --short` 中无任何 `doc/data/images/` 变更。

## 检查范围

本轮把 `doc/4-bugs/` 与 `doc/5-tests/` 的产物形态，从「时间戳子目录 + `README.md` + `evidence/artifacts` 子目录」统一改成与 `doc/2-需求/`、`doc/6-review/` 同构的单个扁平 md。风格回归只看写法、位置、格式、注释、日志和目录归位。

纳入检查的对象：

| 分组 | 文件数 | 说明 |
| --- | --- | --- |
| 单一真相源 | 5 | `artifact-storage-rules/` 的 `SKILL.md` 与 4 个 references，`path-map.yaml` 由 v8 升到 v9 |
| Bug 域 | 30 | 5 个 Bug skill，加上 `parallel-task-dispatch-rules` 与 `skill-evolution-rules` 的术语 |
| 测试域 | 26 | 4 个测试 skill，加上两个 Bug skill 里的测试落点表述 |
| 上线接口测试 | 11 | 3 个 Python 模块与 8 个规则文件 |
| 收口闸门与仓库入口 | 9 | 交付闸门、代码收口闸门、Git 示例、`README.md`、`项目设计.md`、`编码skill.md`、`PROJECT_*` |
| 新增与扩展测试 | 2 | `asset_location_test.py` 扩 5 个用例，`release_layout_test.py` 新建 3 个用例 |

### 范围外说明

历史 `doc/4-bugs/` 与 `doc/5-tests/` 的子目录形态一律只读归档，本轮不迁移、不改写；`doc/5-tests/基线/` 作为跨轮复用的机器数据资产库保持原样，已在真相源中登记为唯一豁免；`test/shared/legacy_doc5_tests_manifest.json` 历史指纹基线不动。

## 真实测试前置证据

风格回归发生在真实测试通过之后。本轮真实测试的执行情况：

新增与扩展的 8 个用例全部通过：

```
test_path_map_declares_flat_bug_and_test_docs ... ok
test_bug_domain_rules_drop_subdirectory_terminology ... ok
test_bug_naming_template_is_flat_markdown ... ok
test_test_domain_rules_drop_subdirectory_terminology ... ok
test_test_naming_template_is_flat_markdown ... ok
test_task_skeleton_keeps_doc_flat_and_artifacts_outside_doc ... ok
test_write_report_splits_doc_report_from_machine_artifacts ... ok
test_write_report_without_doc_path_keeps_legacy_single_root ... ok
```

根 `test/` 逐目录全量回归，改动前后失败数完全一致，说明本轮没有引入新的失败：

```
test/artifact-delivery-gate-rules   基线 Ran 65 failures=1 errors=1  ->  改后 Ran 65 failures=1 errors=1
test/package-structure-rules        基线 Ran 35 failures=8          ->  改后 Ran 35 failures=8
test/task-plan-rehydration-rules    基线 Ran 71 failures=5          ->  改后 Ran 71 failures=5
test/test-asset-governance          基线 Ran  9 failures=2          ->  改后 Ran 14 failures=2
其余 14 个测试目录                    全部 OK
```

历史零污染核验，输出为空即通过：

```bash
git status --short -- doc/4-bugs doc/5-tests test/shared/legacy_doc5_tests_manifest.json
```

Skill 字典刷新退出码 0，`implemented_total: 69`，`skill-dictionary/data.js` 与 `字典.md` 已同步。

## 6-review 结论

STYLE: PASS

本轮未发现需要修复的风格问题。规则术语、路径示例、单一真相源边界、代码注释、测试归位和文件编码全部符合既有约定，改动范围严格停在目录形态与术语上，没有扩散到无关内容。

### 完成标准

风格回归的完成标准是：术语在全域统一、路径示例形态一致、目录与命名只在单一真相源定义、新增代码带齐函数头与步骤注释、测试落在根 `test/` 镜像、全部改动 UTF-8 落盘、历史零污染核验为空。以上七项全部满足，判定 PASS。

## 检查清单

| 检查项 | 结论 | 依据 |
| --- | --- | --- |
| 术语一致性 | PASS | `Bug 根目录` 改 `Bug 主文档`、`时间戳目录` 改 `时间戳测试主文档`，机器扫描无残留 |
| 路径示例格式 | PASS | 示例路径统一为 `YYYY-MM-DD_HHmmss_<中文主题>.md`，与需求域和 `6-review` 域同构 |
| 单一真相源边界 | PASS | 目录与命名模板只在 `path-map.yaml` 定义，下游 skill 只引用不复制 |
| 豁免项显式声明 | PASS | `doc/5-tests/基线/` 与 `test/release-artifacts/` 在 `directories` 中带 `_note` 写明理由 |
| Python 注释与函数头 | PASS | 改动函数均带 `[参数]`、`[返回]`、`最近修改时间`，关键分支有编号步骤注释 |
| 测试代码归位 | PASS | 新增测试位于根 `test/` 的 ASCII 镜像目录，命名 `*_test.py`，未污染生产代码 |
| 文件编码 | PASS | 全部改动以 UTF-8 落盘，中文无乱码 |
| 最小改动 | PASS | 只改目录形态与术语，未顺手重构 Bug 域和测试域的其它规则内容 |
| 历史零污染 | PASS | `git status` 对三个历史路径的输出为空 |
| 日志与控制台输出 | N/A + 原因 + 证据 | 原因：本轮改动不含运行期日志语句；证据：`report.py` 与 `cli.py` 的改动只涉及路径派生与文件落点，未新增或修改任何日志调用 |

## 问题与修复

本轮未发现需要修复的风格问题。

| 序号 | 问题 | 严重度 | 修复动作 | 状态 |
| --- | --- | --- | --- | --- |
| 无 | N/A + 原因 + 证据：原因是九项风格检查全部一次通过；证据是上文检查清单中无任一 FIX_REQUIRED 项，且残留扫描命令无输出 | N/A | N/A | N/A |

## 执行附录

### 形态变化对照

| 维度 | 改前 | 改后 |
| --- | --- | --- |
| Bug 产物 | `doc/4-bugs/<时间戳目录>/README.md` | `doc/4-bugs/YYYY-MM-DD_HHmmss_问题中文简介.md` |
| 测试产物 | `doc/5-tests/<时间戳目录>/README.md` 加 `evidence/` 与 `artifacts/` | `doc/5-tests/YYYY-MM-DD_HHmmss_<测试任务中文主题>.md`，证据内联正文 |
| 上线测试机器产物 | `doc/5-tests/<时间戳>_上线前项目接口测试/ascii-artifacts/` | `test/release-artifacts/<时间戳>_release-interface-test/` |
| 基线数据资产 | `doc/5-tests/基线/` | 保持不变，登记为唯一豁免子目录 |
| 历史产物 | 无明确约定 | 子目录形态只读归档，不迁移 |

### 关键代码改动

- `project-interface-release-execution-rules/scripts/generate_release_test_plan.py`：`ensure_release_task_root(task_root, title)` 改为 `ensure_release_task_root(artifacts_root, doc_path)`，删除中文子目录及其 `README.md`，改为写 `doc/5-tests/` 扁平 md 骨架；`init-release-test-task` 的参数由 `--task-root` 与 `--title` 改为 `--artifacts-root` 与 `--doc`。
- `release_test_engine/cli.py`：默认布局下同时派生中文主报告路径与机器产物根，两者分别落在 `doc/5-tests/` 和 `test/release-artifacts/`。
- `release_test_engine/report.py`：`canonical_layout: bool` 改为 `doc_report_path: str | Path | None`，去掉 `ascii-artifacts` 中间层；新增 `_relative_hint` 计算主报告到产物根的相对指引。未传 `doc_report_path` 的旧调用方行为保持不变。

### 已知既有问题

以下问题在本轮改动前已存在，改动前后表现一致，不属于本轮范围。

| 位置 | 现象 | 判定 |
| --- | --- | --- |
| `test/run_python_tests.py` | 带连字符的镜像目录不可 import，全量入口在改动前后同样报 `ImportError` | 既有，超出本轮范围 |
| `test/test-asset-governance` | `credential-policy` 缺被测目录、历史指纹与基线不符 | 既有，改动前后一致 |
| `test/package-structure-rules` | 8 项 `source_policy` 与互斥目录断言失败 | 既有，改动前后一致 |
| `test/task-plan-rehydration-rules` | 5 项断言失败 | 既有，改动前后一致 |
| `test/artifact-delivery-gate-rules` | 1 个 failure 加 1 个 error | 既有，改动前后一致 |

### 追踪矩阵

| 任务 | 真实测试 | 证据 |
| --- | --- | --- |
| TASK-FLATDOC-01 单一真相源 | TEST-FLATDOC-001 | EVIDENCE-FLATDOC-01 |
| TASK-FLATDOC-02 Bug 域 | TEST-FLATDOC-002、TEST-FLATDOC-003 | EVIDENCE-FLATDOC-02 |
| TASK-FLATDOC-03 测试域 | TEST-FLATDOC-004、TEST-FLATDOC-005 | EVIDENCE-FLATDOC-03 |
| TASK-FLATDOC-04 引擎布局 | TEST-FLATDOC-006、TEST-FLATDOC-007、TEST-FLATDOC-008 | EVIDENCE-FLATDOC-04 |
| TASK-FLATDOC-05 闸门与入口 | TEST-FLATDOC-001..008 全量回归 | EVIDENCE-FLATDOC-05 |
