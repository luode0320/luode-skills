---
schema_version: 1
template_version: 1
doc_id: "STYLE-DVT-20260818-01"
doc_type: style_regression
source_ids:
  - "REQ-DVT-20260818-001"
  - "CYCLE-DVT-01"
status: accepted
version: "v1.0"
current_slice: "CYCLE-DVT-01"
updated_at: "2026-08-18 14:45:00"
reader_level: business_general
writing_style: plain_chinese
appendix_policy: preserve_existing_or_one_terminal_appendix
---

# 6-review 风格回归：目录树多版本改造 · 去除 rpc

结论：本轮仅核对规则文档、脚本落点和测试资产的写法与归位；影响：不代替业务正确性判断和发布放行；范围：`package-structure-rules` 与 `micro-business-architecture-rules` 两个 skill 的文档/脚本修改、工程文档与收口证据；非范围：业务运行时正确性、发布放行、迁移历史 `doc/3-实施/` 文档；变化：catalog 删 `business-rpc`、`router`/`controller`/`entity`/`service` 改版本级（`<source-root>/<domain>/<artifact>/<v?>`）；`micro_business.py` 改四类版本化骨架 + 单文件 init + `--detect-new`、移除 `--with-rpc` 与域级 crontask；完成标准：STYLE: PASS；术语说明：风格回归是对代码、规则文档和测试资产写法的检查；验证状态：本次相关真实测试通过后执行，字典刷新与项目记忆同步完成。

## 文档信息

| 字段 | 内容 |
| --- | --- |
| 关联任务 | `TASK-DVT-01` 至 `TASK-DVT-06` |
| 关联真实测试 | `TEST-DVT-01` 至 `TEST-DVT-06` |
| 检查时点 | 真实测试通过后 |

## 检查范围

本轮检查：

- 两个 skill 的 reference/脚本/templates 是否彻底去除 `rpc/` 与 `business/<domain>` 口径；
- `init.<ext>` 单文件化、四类版本化目录 `<v?>` 语义（`router/`/`controller/`/`entity/`/`service/` 各自内含）是否在目录树、脚本、reference 三处一致；
- 域级 `crontask/` 是否已移除；
- 新增与修改文件是否 UTF-8、中文是否无乱码；
- 文档是否使用扁平 md 命名并符合白话摘要结构；
- 字典刷新是否最小化且可重复，`PROJECT_MEMORY.md` 机器索引区是否同步去 rpc。

### 范围外说明

不迁移历史 `doc/3-实施/` 文档、不新增旧版本冻结检查、不执行 Git 提交、不改动其他兄弟 skill；`utils/rpc/`（gRPC/Thrift 客户端适配）与 `integration/contracts/rpc`（前后端联调 RPC 契约）是独立概念，不属本次移除范围。

## 真实测试前置证据

三份工程文档机器校验 `valid: true`；`placement_catalog.py` business-rpc 查询失败关闭、router/controller/entity/service 返回版本级条目；`micro_business.py` 脚手架产出四类版本化骨架（无 crontask）、check 拒绝跨域、`--detect-new` 三态判定；字典刷新退出码 0。对应证据 `EVD-TASK-DVT-01-TEST` 至 `EVD-TASK-DVT-06-TEST`。

## 6-review 结论

STYLE: PASS

本轮未发现需要修复的风格问题。两个 skill 的 rpc/business 口径已彻底清除，域级 crontask 已移除，`init.<ext>` 单文件化与四类版本化目录语义在 catalog、脚本、reference 三处一致，`PROJECT_MEMORY.md` 机器索引区已同步去 rpc 并改名，新增文档符合白话摘要结构，文件编码与字典刷新符合仓库既有约定。

### 完成标准

风格回归完成标准为：rpc/business 口径清除、版本目录语义三处一致、改动最小、全部文件 UTF-8、文档为扁平 md 且通过白话摘要校验、字典刷新成功。以上标准全部满足，判定 PASS。

## 检查清单

| 检查项 | 结论 | 依据 |
| --- | --- | --- |
| rpc/business 口径清除 | PASS | 两个 skill 无残留 `rpc/`、`business/<domain>`、`--with-rpc`、`business_rpc_root` |
| 版本目录语义一致 | PASS | `placement_catalog.yaml/.py`、`project-layout-v2.md`、`micro_business.py`、reference 均用 `<source-root>/<domain>/<artifact>/<v?>`（四类各自内含） |
| init 单文件化 | PASS | `init/` 目录已收敛为 `init.<ext>` 单文件，`scaffold` 产出 `init.go` |
| 最小改动 | PASS | 仅改两个 skill 与收口文档，未新增 skill、未迁移历史文档 |
| 文件编码 | PASS | 新增与修改文件 UTF-8，中文无乱码 |
| 文档形态 | PASS | 需求/实施/周期/测试/6-review 使用扁平 md，符合白话摘要结构 |
| 字典刷新 | PASS | `generate_dictionary.py` 退出码 0，`data.js`/`字典.md` 无 rpc 残留 |
| 未引入无关改动 | PASS | 未新增 skill、未改 `tool-manifest.yaml`、未执行 Git 提交 |

## 问题与修复

| 序号 | 问题 | 严重度 | 修复动作 | 状态 |
| --- | --- | --- | --- | --- |
| 无 | N/A + 原因 + 证据：原因是八项风格检查全部一次通过；证据是上文检查清单中无任一 FIX_REQUIRED 项 | N/A | N/A | N/A |

图片资产决策：N/A + 原因 + 证据：本轮是规则文档、脚本与测试资产改动，没有 UI、截图、原型或位图需要视觉留证。

## 执行附录

### 关键改动

- `package-structure-rules/`：`SKILL.md`、`references/placement-catalog.yaml`、`placement-catalog.schema.json`、`project-layout-v2.md`、`go-package-layout.md`、`java-layer-layout.md`、`node-python-module-layout.md`、`structure-general.md`、`lookup-and-reference-contract.md`、`backend-util-layout.md`、`scripts/placement_catalog.py`
- `micro-business-architecture-rules/`：`SKILL.md`、`references/{directory-layout,isolation-and-communication,boundaries,trigger-and-marker,md-convention}.md`、`scripts/micro_business.py`、`templates/{business-readme-template,business-index-template}.md`
- 仓库级：`PROJECT_MEMORY.md`、`编码skill.md`、`skill-dictionary/data.js`、`字典.md`
- 工程文档：需求、实施总览、实施周期、测试主文档、本 6-review

### 验证命令

- 文档校验：`python -B artifact-delivery-gate-rules/scripts/validate_engineering_docs.py --profile style_regression --doc "doc/6-review/2026-08-18_目录树多版本改造_去除rpc_6-review.md" --root D:\谷歌云盘\luode-skills`
- Catalog：`python -B package-structure-rules/scripts/placement_catalog.py query --artifact business-rpc`（应非 0）、`query --artifact router` / `--artifact controller` / `--artifact entity` / `--artifact service`（版本级）
- 脚手架：`python -B micro-business-architecture-rules/scripts/micro_business.py scaffold order --root <tmp>`
- 字典刷新：`python -B skill-dictionary/generate_dictionary.py`
- 全量测试：`python -B test/run_python_tests.py`

## 追踪附录

- 来源：`REQ-DVT-20260818-001`
- 周期：`CYCLE-DVT-01`
- 任务：`TASK-DVT-01` 至 `TASK-DVT-06`
- 测试：`TEST-DVT-01` 至 `TEST-DVT-06`
- 风格证据：`EVD-TASK-DVT-01-STYLE` 至 `EVD-TASK-DVT-06-STYLE`
