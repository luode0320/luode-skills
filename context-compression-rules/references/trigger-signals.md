# 压缩后触发信号

## 强触发

- 刚完成“压缩上下文 / 自动压缩上下文 / 上下文太多”后的摘要重组。
- 系统已执行自动压缩，并准备进入压缩后的继续执行回合。
- 用户明确表示“已经压缩好了，按压缩结果继续”。

## 弱触发

- 压缩后即将进入新阶段，担心主域 skill 记忆缺失。
- 压缩摘要可继续执行，但缺少最近上下文与 skill 候选补载。

## 不触发

- 尚未发生上下文压缩。
- 用户只是在讨论“是否要压缩”，还没真正压缩完成。

## 触发后第一动作

- 先联动 `recent-context-bootstrap-rules`，补齐最近 3 天前情和 skill 索引来源：
  - 系统的所有 skills
  - 当前项目根目录下 `./.skills`
- 恢复并声明“编码许可状态”：
  - `confirmed`：已明确允许开始/继续实现代码
  - `not-confirmed`：明确未允许开始/继续实现代码
  - `unknown`：无法确认，默认不得直接进入编码，需先走关键节点确认
