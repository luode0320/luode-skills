# 响应变体规则

## 用途

用于处理错误响应、分页结构、兼容字段和版本字段。

## 错误响应

- 错误码、错误消息、可选上下文字段位置应稳定。
- 错误响应结构应与成功响应有清晰区分。

## 分页响应

- 列表数据与分页元信息分开表达。
- 页码、页大小、总数、是否还有下一页等字段应集中放置。

## 兼容字段与版本字段

- 兼容字段应有明确保留理由，不要长期堆积。
- 版本字段只在确有版本演进需要时引入，不为“以后可能会用”预埋过多结构。
