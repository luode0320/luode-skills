# Browser Use API 密钥模板
# 复制本文件为 ~/.browser-use/.env 并填入真实 key（不要提交 .env 到仓库）：
#   cp .env.example ~/.browser-use/.env
#   echo 'BROWSER_USE_API_KEY=your-key' >> ~/.browser-use/.env
# 获取 key: https://www.browser-use.com/ 控制台（或 Browser Use Cloud 设置页）
# 注意: key 原值禁止出现在聊天、日志、仓库配置或任何输出中。
BROWSER_USE_API_KEY=your-key-here
