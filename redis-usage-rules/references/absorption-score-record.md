# 吸收 8 维评分记录（darwin-rubric 棘轮验证）

> 归属：`redis-usage-rules`。记录 self-dev-redis__skillhub 吸收的棘轮验证结果。评分视角：**独立子 agent**（Explore 子代理独立评分，未接触主 agent 评分，2026-08-19）。

## 基线（外部源 self-dev-redis__skillhub）：49.6 / 100

| 维度（权重） | 分 | 加权 | 理由 |
| --- | --- | --- | --- |
| Frontmatter 质量（8） | 7 | 5.6 | name=redis 过泛；description 有触发词但缺"何时不用"边界 |
| 工作流清晰度（15） | 3 | 4.5 | SKILL.md 仅三节配置说明，无步骤序号，流程全埋在 657 行脚本 |
| 边界条件覆盖（10） | 7 | 7.0 | 脚本层有 cli 缺失、连接三分诊、SKIP 行、TTL 特判 |
| 检查点设计（7） | 5 | 3.5 | 仅 flush-confirm 一个确认点，MONITOR/KEYS/export 无确认 |
| 指令具体性（15） | 4 | 6.0 | 说明层零指令，命令示例只存在于代码 |
| 资源整合度（5） | 2 | 1.0 | SKILL.md 从未引用 scripts/script.sh，资源存在但未整合 |
| 整体架构（15） | 3 | 4.5 | 单体脚本塞全部职责，与仓库分层风格不一致 |
| 实测表现（25） | 7 | 17.5 | 命令真实可执行，但宿主无 redis-cli 且缺 agent 指引 |

## 新分（吸收后 redis-usage-rules）：87.5 / 100

| 维度（权重） | 分 | 加权 | 理由 |
| --- | --- | --- | --- |
| Frontmatter 质量（8） | 9 | 7.2 | 名称规范、触发词+触发信号+排除项齐备，<1024 字 |
| 工作流清晰度（15） | 9 | 13.5 | "先做什么"与"默认流程"各 5 步有序号，输入输出明确 |
| 边界条件覆盖（10） | 9 | 9.0 | PING 三分诊、cli 缺失分平台安装、none 回退、100 条截断、导入覆盖确认 |
| 检查点设计（7） | 9 | 6.3 | FLUSHDB 双确认、生产实例 MONITOR/KEYS/导出均需确认，独立成节 |
| 指令具体性（15） | 9 | 13.5 | 完整连接命令、TYPE 分发映射、备份行格式、TTL/命中率口径、速查代码块 |
| 资源整合度（5） | 9 | 4.5 | SKILL.md 显式引用两份 references，路径可达，含吸收映射 |
| 整体架构（15） | 9 | 13.5 | 主文件+2 操作参考+2 元数据，与 database-query-rules 同构，无冗余 |
| 实测表现（25） | 8 | 20.0 | 本环境无法真机实测（见受限项），但全部标准 redis-cli 语法与源实现一致，静态可审查 |

## 棘轮结论

- 新分 87.5 **严格高于**基线 49.6，涨幅 +37.9 分（+76.4%），8/8 维度全部超越 → **保留吸收内容**。
- 单轮涨幅远超 1 分阈值，无早停问题。

## 实测受限项（N/A + 原因 + 证据）

- **效果真机实测不可用**：本机 Windows 无官方 redis-cli；WSL Ubuntu-24.04 探测 `command -v redis-cli` 返回 REDIS_CLI_MISSING（2026-08-19）。按计划最大推进边界不安装系统级依赖。
- **降级替代**：命令清单静态审查 PASS——新文档中的 `--no-auth-warning`、`YES FLUSH` 双确认、`keyspace_hits/keyspace_misses` 命中率口径、`mem_fragmentation_ratio` 碎片率、`apt-get install redis-tools` 安装指引、`TYPE|key|ttl|值` 备份行格式均与源 script.sh 实现逐条一致（源 92/393-394/334-350/333/79/431-452 行）。
