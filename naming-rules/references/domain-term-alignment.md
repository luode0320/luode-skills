# 术语对齐

- 同一业务概念在页面、接口、数据库、测试中尽量保持一致。
- 如果不同层已有不同叫法，优先记录映射关系后再收敛。
- 内部代码命名默认保持驼峰命名族；如果外部协议、数据库或第三方字段使用蛇形或其他格式，应在映射层显式转换，不要把外部格式直接扩散到内部代码标识符。
- 同一概念在内部不同位置要尽量保持相同词序，例如确定使用 `refundOrder` 后，不要在相邻代码里再出现 `orderRefund` 作为同义替代。
- 重命名公共对象时，要同步检查调用方、测试和交付说明用词。
